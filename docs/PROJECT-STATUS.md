# PROJECT-STATUS.md — Where the Build Stands

The living tracker for Aalishaan Studio. Any fresh session — AI or human — reads this file **first** to know
exactly where the build stands and what to do next. It holds state, not plans: scope and exit gates live in
`docs/ROADMAP.md`; process lives in `docs/WORKFLOW.md`.

## 1. Right now

| Item | Value |
|---|---|
| Current stage | Setup Gate — S0.0 in progress (repository created by the owner and `main` protected by a ruleset, verified 2026-09-23; the first setup PR is prepared on `claude/s0.0-kickoff-decisions`; independent review and owner merge pending) |
| Active sprint | S0.0 — Kickoff: decisions lock & governing docs — Status: In Progress (started 2026-09-23) |
| Current branch | `claude/s0.0-kickoff-decisions` — created 2026-09-23 in place in `repo/` from `origin/main` `5d98958942377c242e88b48a03228baccc3010bb` (the owner's README commit on `86400studio/aalishaan-studio-app`); branch owner: Claude Code; one worker per branch |
| Next action | Owner: open the first setup PR from `claude/s0.0-kickoff-decisions` into `main` (the branch is pushed; Git policy for S0.0: Commit YES, Push YES — owner, 2026-09-23) and record its number here. GPT-6 Astra Codex then reviews the actual immutable range with `docs/code-reviews/S0.0-kickoff-decisions-review.md`; Claude fixes findings; the owner merges; S0.1 activates only after the merge is verified (§11 small docs branch). Owner questions still open are listed in §8. — *Prior:* execute the prepared `docs/sprint-prompts/S0.0-kickoff-decisions.md` |
| Preview / Production | not deployed yet (Vercel from S0.1) / https://[DOMAIN] on Vercel — domain is open decision D-04 |
| Last updated | 2026-09-23 — S0.0 execution in Claude Code: workspace and reference checks run (§6); S0.0 Gate 0 answered by the owner (repository on the owner's own GitHub account `86400studio`, public, no transfer — D-02); D-01–D-39 checked against their sources and their leans confirmed with the date, owner items kept as Gate 0 items (§8a, notes in §8c); setup and CI documentation reconciled (`TECHNICAL-INTEGRITY.md`, setup checklist); root `.gitignore` added (D-07) — *Prior:* 2026-09-23 — three approved additions (order-intake pause and waiting list; online-only payment with a WhatsApp contact route; Refunds & remedies on the Payment stage) built into the prototype as sample behaviour, verified in Chromium and frozen as revision E10; planned as decisions D-37–D-39, new sprint S2.21 (37 sprints) and amendments to S1.6, S2.4, S2.18 and S3.4; new strings NS-12–NS-16 (customer copy is a draft for owner approval); earlier the same day — verifier findings applied and the prototype checksum baseline frozen as E9 (`npm run check:baseline` → PASS); 2026-09-17 — final Admin (A2 — 86400 UI edition) installed at `prototype/admin/`, A1 removed, SOP source folder removed, all governing docs realigned to the final Admin (D-PRE-23, D-28–D-36); prototype revision named E9 (D-36) — *Prior:* 2026-09-16 prototype moved to `repo/prototype/`; SOP pack installed; governing docs filled; 36-sprint roadmap written and judged — *Prior:* 2026-09-15 prototype E8 published (see `prototype/PAGE-TRACKER.md`) |

### How to resume in a fresh AI session

1. Read this file, then the active sprint's brief and exit gate in `docs/ROADMAP.md`.
2. Read the agent-instructions file (`CLAUDE.md` / `AGENTS.md`) and the docs it points to for the task.
3. Verify the repo state yourself (package manifest, source tree, `git log`). If it disagrees with this file, report the mismatch; update this file only when the task authorizes it.
4. Work only inside the active sprint. Anything else → propose it under Open decisions or the backlog.
5. Before ending: update §1–§3 here, tick the roadmap checkboxes, and include this file in the PR.

Why this matters: this ritual makes the project session-stateless — anyone can cold-start and resume mid-sprint with zero verbal briefing.

## 2. Sprint board

Status legend: Not Started · In Progress · Blocked (say why) · Ready for Review · Approved · Done · Not Applicable (optional only; reason required).

| Sprint | Status | Branch | PR | Merged date | Notes |
|---|---|---|---|---|---|
| Setup Gate | In Progress | | | | S0.0 started 2026-09-23; S0.0–S0.2; exits when the S0.2 PR is merged and smoke-tested |
| S0.0 Kickoff: decisions lock & governing docs | In Progress | `claude/s0.0-kickoff-decisions` | | | Started 2026-09-23. The first setup PR (docs pack + prototype text sources; Code Check not yet required — D-26); its actual number and URL are recorded here when it is opened. Records: `docs/sprint-prompts/S0.0-kickoff-decisions.md`, `docs/code-reviews/S0.0-kickoff-decisions-review.md` |
| S0.1 Scaffold, Code Check, security headers, Vercel Preview & Sentry | Not Started | | | | Branch `claude/s0.1-setup-scaffold`; may land as S0.1a/S0.1b (D-25) |
| S0.2 Supabase isolation, migration baseline & proof harness | Not Started | | | | May land as S0.2a/S0.2b (D-25) |
| Stage 0 — fully working barebones store | Not Started | | | | S1.1–S1.9 |
| S1.1 Core schema, money model & catalogue seed | Not Started | | | |  |
| S1.2 Staff auth, MFA & server RBAC guard | Not Started | | | |  |
| S1.3 Public shell, tokens, Home & copy pages | Not Started | | | |  |
| S1.4 Catalogue browse pages from the database | Not Started | | | |  |
| S1.5 Artwork pages, bag API & Cart | Not Started | | | |  |
| S1.6 Checkout: server validation, pending order & Razorpay Order | Not Started | | | |  |
| S1.7 Razorpay verification, idempotent webhook & tri-state confirmation | Not Started | | | |  |
| S1.8 Pending-work worker, confirmation email, receipt & pending-order expiry | Not Started | | | |  |
| S1.9 Admin minimum, Track lookup & Stage 0 proof | Not Started | | | | May land as S1.9a/S1.9b (D-25) |
| Stage 1 — AF1 V1 slices | Not Started | | | | S2.1–S2.21 |
| S2.1 Store setup, Staff & Access and Connections foundation | Not Started | | | |  |
| S2.2 Orders workspace: attention, follow-ups, search, saved views, bulk assignment & failure queue | Not Started | | | |  |
| S2.3 Private files & evidence storage | Not Started | | | |  |
| S2.4 Refund engine & payment review | Not Started | | | |  |
| S2.5 Catalogue management, production files & publish gates | Not Started | | | | May land as S2.5a/S2.5b (D-25) |
| S2.6 Materials, stock & safe reservations | Not Started | | | |  |
| S2.7 Production readiness, jobs & production-start message | Not Started | | | |  |
| S2.8 Inspection, evidence & packing | Not Started | | | |  |
| S2.9 Shiprocket booking, serviceability, handover & dispatch message | Not Started | | | |  |
| S2.10 Tracking events, delivery exceptions, public Track milestones & delivery message | Not Started | | | |  |
| S2.11 Changes, holds & cancellations | Not Started | | | |  |
| S2.12 Customers, consent & privacy; newsletter live | Not Started | | | |  |
| S2.13 Support cases, saved replies, Contact form live & delivery-exception follow-ups | Not Started | | | |  |
| S2.14 Damage, returns, courier claims & linked replacements | Not Started | | | |  |
| S2.15 Invoices & credit notes | Not Started | | | |  |
| S2.16 Settlements, bank matching, disputes & finance exports | Not Started | | | |  |
| S2.17 Safe catalogue imports & remaining protected exports | Not Started | | | |  |
| S2.18 Messaging completeness | Not Started | | | |  |
| S2.19 Reports & weekly digest | Not Started | | | |  |
| S2.20 Monitoring, restore drill, peak test, readiness matrix & operating guides | Not Started | | | |  |
| S2.21 Order intake pause and waiting list | Not Started | | | | Added 2026-09-23 (D-37); closes Stage 1; Gate 0 = owner approval of NS-12–NS-14 |
| Launch Gate | Not Started | | | | S3.1–S3.4 |
| S3.1 Owner inputs applied | Not Started | | | | Multi-PR by design (D-25). Blocked by owner inputs OI-01–OI-12 until supplied (does not block earlier sprints) |
| S3.2 Launch Gate: feature list, full suite, fix loop & GO | Not Started | | | | Parent run plus fix PRs S3.2-Fn (D-25) |
| S3.3 Live provider connection, Production data & controlled verification | Not Started | | | |  |
| S3.4 Go-live & handover | Not Started | | | |  |

Retired sprints stay in the table, struck through, with the date, reason, and where the scope moved (backlog).

## 3. Last completed work

- 2026-09-23 — S0.0 execution (in progress, not complete): the workspace was confirmed as the original full-asset workspace (no Git history; E10 baseline 821/821, all 39 Admin manifest entries, §12 placeholder scan — §6). Gate 0 answered by the owner: the app repository is `86400studio/aalishaan-studio-app` on the owner's own personal GitHub account (a user account, not an organisation), public, with no transfer planned; the owner creates it in the browser and protects `main` with a ruleset; Git policy for this sprint Commit YES / Push YES (sprint branch only). Seven independent read-only reviewers checked D-01–D-39 against their sources, mapped the required documentation reconciliations and audited the D-07 import set; their accepted findings are the §8a stamps, the §8c notes and the edits to `TECHNICAL-INTEGRITY.md` (Node 24, required `test:unit`, `prototype/` excluded from formatting and linting, two-step protection under D-26), the setup checklist, `README.md`, `TECH-ARCHITECTURE.md`, `APPROVED-INPUTS.md`, `ENVIRONMENT-PARITY.md`, `HANDOFF.md` and `new-strings.md`, plus the root `.gitignore`. No owner approval of any lean, string or screen is inferred. The owner then created the repository (README commit `5d98958`) and its `main` ruleset, verified read-only from the GitHub API; `repo/` was initialised in place, and the branch was committed and pushed (S0.0 record, "Git bootstrap, commit, push and PR"). Independent review and the owner's merge are still to come.
- 2026-09-23 — Development-readiness assessment and `/sprint-prompt S0.0` preparation completed: planning supports kickoff; production foundations remain assigned to S0.0–S0.2. Saved `docs/sprint-prompts/S0.0-kickoff-decisions.md` and its paired review brief; owner-selected workflow is GPT-6 Astra Codex for prompt architecture and independent review, Claude Code for implementation/fixes, human owner for merge. Read-only checks passed for E10's 821-file baseline, handoff, site/routes, artwork mapping, 13 Admin state-guard groups and all 39 Admin manifest entries. The prompt includes S0.0 corrections for the inherited Node/test CI contract and setup checklist; those corrections are not yet implemented. Prompt structure, exact PowerShell verification snippets and links checked; separate prompt-quality review found no unresolved consequential defect. No application development, Git bootstrap, commit, push, deployment or independent implementation verdict occurred; S0.0 remains Not Started.
- 2026-09-23 — Three additions approved by the delivery lead ("Approved - please proceed" on the written proposal) were built into the prototype as sample behaviour and verified in Chromium at 1440 and 390 with no console errors beyond the pre-existing favicon 404: (1) Store setup › Order intake (Pause order intake / Resume ordering with reason, confirmation and audit entries INTAKE / WAITLIST; owner-only banner; Waiting list page with sample entries, reopening notices, removal and the draft customer copy) and the capacity notice + waiting-list form on the bag and checkout pages (validated; entries with a bag snapshot; single-use Resume your order link; checkout submit blocked while paused); (2) the online-only payment line with a WhatsApp link on checkout, two FAQ entries, privacy and cookies sentences and the Cookies clear control; (3) the owner-only Refunds & remedies block on the Payment stage opening the canonical refund record in place. Prototype re-frozen as **E10** (`npm run freeze:baseline -- --revision E10`; `check:baseline` PASS, 821 source files; the baseline file stamps its date in UTC, 2026-09-22); the vendor Admin manifest retained as `prototype/admin/CONTENTS-SHA256-86400-2026-09-17.txt` and `CONTENTS-SHA256.txt` regenerated; all prototype checks PASS (§6). Planned into the docs as D-37–D-39 (§8a), D-32 (j), sprint S2.21 with a brief, the S1.6 gate reason, the S2.4 remedies block, the S2.18 template, the S3.4 wording and the backlog item (`ROADMAP.md`), routes / `0025_intake_waitlist` / integrations (`TECH-ARCHITECTURE.md`), components (`DESIGN.md`), locked facts, NS-12–NS-16 (`new-strings.md`) and the approval row (`APPROVED-INPUTS.md`). Two independent read-only verifications (prototype code vs docs; cross-document consistency) reported eleven findings — three doc corrections, four code hardenings, eight consistency gaps — all applied the same day, the prototype re-frozen as E10 and every check re-run (§6). Nothing pushed; no owner sign-off inferred.
- 2026-09-23 — The realignment was verified by five independent reviews (Admin accuracy against the code, stale references, cross-document consistency, AF1 and view coverage, runnability) and their accepted findings were applied: the false "already re-frozen" statements corrected and the freeze actually performed; the §12 scan command's lost regex escapes restored; the S2.4 → S2.13 ordering gap closed (S2.4 lays the Support tab bar); the S2.16 → S2.19 entry path stated; export-catalogue, backup-status, global-search and recovery-status ownership stated once (S2.5/S2.17, S2.1/S2.20, S1.9/S2.2); AF1 credits tightened (C1 out of S1.9, R2 out of S2.2, G2 into S2.20); Gate 0 rows completed (S2.4 OI-08, S2.5 OI-04, S2.9 OI-03/OI-04); the Admin evidence widths fixed at 1440 / 1024 / 768 / 390 / 320; label quotes corrected (Packed checks, publish blockers); D-PRE-23 and D-36 wording completed; the retained-module map added to `TECH-ARCHITECTURE.md`; reviewer and sprint-prompt instructions given the Admin reference and the D-32 rule.
- 2026-09-17 — Pre-sprint alignment (no PR; no repository yet): the delivery lead supplied the final Admin package "Aalishaan Admin - 86400 UI edition" (Admin A2 plus the 86400 presentation layer). Its 36-file SHA-256 manifest verified; it was installed at `prototype/admin/` exactly as its README instructs (replacing the site's `admin` folder so "View website" and image paths resolve); the old Admin A1 folder and the generic SOP source folder `development/` were removed from the workspace (every SOP file had already been installed under this repository; both folders went to the Recycle Bin). In place, the package's 13 state-guard groups pass, and the prototype's own `check:admin`, `check:handoff`, `check` and `check:routes` pass. Five independent readers mapped the new Admin (navigation and all 53 tested views, state and storage, the 86400 design layer, the AF1 capability mapping, and stale A1 pointers in the prototype); the findings are recorded as D-PRE-23 and D-28–D-36, and `ROADMAP.md`, `TECH-ARCHITECTURE.md`, `DESIGN.md`, `APPROVED-INPUTS.md`, `docs/content/locked-facts.md`, `docs/content/new-strings.md` (NS-10, NS-11), `docs/HANDOFF.md`, `docs/templates/NEW-WEBSITE-SETUP-CHECKLIST.md`, `docs/testing-setup/SETUP-CHECKLIST.md`, `README.md` and `CLAUDE.md` were realigned (`AGENTS.md` and the `/sprint-prompt` skill followed on 2026-09-23). The prototype's current-state pointers were updated and its tracker records the change; the checksum baseline was re-frozen as revision E9 on 2026-09-23, once the realignment had been verified (`npm run freeze:baseline -- --revision E9`; `npm run check:baseline` → PASS: 816 source files; the baseline file stamps its date in UTC, 2026-09-22), and `admin/** -text` was appended to `prototype/.gitattributes` (D-36).
- 2026-09-16 — Pre-sprint setup (no PR; no repository yet): the approved prototype (E8, commit `b24dce1bf498a5c8131694fef5fb1602139e1653` of `86400studio/aalishaan-studio`) was moved to `repo/prototype/` unchanged; the SOP docs pack was installed per its copy map (root `README.md`, `CLAUDE.md`, `AGENTS.md`; `docs/`; `docs/templates/`; `docs/testing-setup/`; `docs/error-tracking/`; five skills under `.claude/skills/`); governing docs filled (`ROADMAP.md`, this file, `TECH-ARCHITECTURE.md`, `DESIGN.md`, `APPROVED-INPUTS.md`, `SECURITY-CHECKLIST.md` §9, `ENVIRONMENT-PARITY.md` §2–§4, `SUPABASE-MCP-SAFETY.md` §10, `docs/content/locked-facts.md`, `docs/content/new-strings.md`, the provider-adapted testing and error-tracking modules); the 36-sprint sequence was drafted from three independent alternatives and judged before synthesis, then verified by five independent reviews whose accepted findings were applied (session record, not a repository artefact). The next session must start at S0.0.

## 4. Next sprint

- S0.0 — Kickoff: decisions lock & governing docs. Depends on: the approved prototype — public E8 plus the final Admin and the E10 additions (revision E10, D-PRE-23, D-36–D-39) — and AF1 (D-PRE-00 as amended). Brief: `docs/ROADMAP.md` → "S0.0"; prompt prepared at `docs/sprint-prompts/S0.0-kickoff-decisions.md`; review brief at `docs/code-reviews/S0.0-kickoff-decisions-review.md` awaits the real PR and immutable SHAs (execution in progress since 2026-09-23). — *Prior:* record to be created by `/sprint-prompt`.

## 5. Blockers

| # | Blocker | Blocking what | Who unblocks | Since |
|---|---|---|---|---|
| 1 | ~~The GitHub repository `86400studio/aalishaan-studio-app` and its `main` ruleset are created by the owner in the browser (S0.0 Gate 0 answer, 2026-09-23)~~ — resolved 2026-09-23: repository and ruleset created by the owner and verified | ~~S0.0 Git bootstrap, commit, push and the first setup PR~~ | Owner | 2026-09-23 |
| — | Nothing else blocks S0.0. Owner inputs OI-01–OI-12 (§8b) are Gate 0 items for S3.1 only; OI-13 is owner-deferred to the post-launch backlog (D-PRE-12) and OI-14 (visual review, including the final Admin screens) is non-blocking; their absence does not block development (owner decision D-PRE-13, 2026-09-13). | S3.1 | Owner | 2026-09-13 |

## 6. Checks status

| Check | Last run | Result | Notes |
|---|---|---|---|
| typecheck | — | N/A | No application code yet; scripts exist from S0.1 |
| lint | — | N/A | As above |
| tests | — | N/A | Vitest + Playwright harness lands in S0.2; suite is mandatory (auth, database, payments) |
| build | — | N/A | From S1.3 on, record the count of generated static paths for the manifest entries (the S1.3–S1.7 route tests assert all 58 public entries and 66 aliases); an unexpected ±1 flags an accidental route add/delete — the Next.js route table itself lists dynamic segments once and is not the metric |
| deployed Preview | — | N/A | Vercel from S0.1; record provider, path, tested head SHA per sprint |
| prototype checks (reference only — every check re-run 2026-09-23 after the E10 additions and freeze; recorded in `prototype/PAGE-TRACKER.md`, E10 entry) | 2026-09-23 | pass | `prototype/`: `node admin/tests/check-state.cjs` and `check:admin` 13 Admin guard groups each; `check:handoff` 822 portable source files, 61 document links; `check` 60 pages, 5051 local references, 66 aliases; `check:routes` 60 pages, 66 aliases; `check:artworks` PASS; `check:baseline` PASS — E10, 821 source files (the baseline file stamps its date in UTC, 2026-09-22); browser verification of the additions in Chromium (Playwright) at 1440 and 390: Admin pause / resume / reopening notices / remove / banner / Waiting list / Payment-stage Refunds & remedies block and its pane / Operations exclusion; storefront notice, validation, entry storage, single-use resume link, blocked checkout submit while paused, payment-policy line and WhatsApp link; no console errors beyond the pre-existing favicon 404 |
| S0.0 setup checks (docs and reference only; typecheck, lint, tests and build N/A — D-26) | 2026-09-23 | pass (workspace) | From `repo/` on the original full-asset workspace: `node prototype/scripts/development-baseline.cjs` → PASS, E10, 821 source files; Admin manifest → all 39 entries match; §12 placeholder scan (bash form) → no unregistered token (the PowerShell form in the S0.0 prompt could not run because `rg` is not on PowerShell's PATH); root `.gitignore` probed with a scratch Git directory → `.env*`, other `prototype/assets/`, `node_modules/`, `qa-evidence/`, `dist/` and the workbook ignored, fonts/brand and `.env.example` not ignored. After the Git bootstrap: `git check-ignore` / `git check-attr` / env-tracking checks as expected, `main` ruleset read back from GitHub, fresh-checkout Admin verification — exact results in the S0.0 record ("Git bootstrap, commit, push and PR") |

## 7. Locked decisions (do not reopen)

Changes require a new, explicit superseding decision by the owner — never a silent edit. Sources are in `prototype/` (see `docs/APPROVED-INPUTS.md` §1 for the approval evidence table).

| ID | Date | Decision | Decided by | Supersedes / notes |
|---|---|---|---|---|
| D-PRE-00 | 2026-09-16 | The E8 prototype (public R13 + M1–M5, ~~Admin A1~~ Admin A2 — 86400 UI edition since 2026-09-17, D-PRE-23; local prototype revision E10 since 2026-09-23 — E9 plus the additions of D-37–D-39) and AF1 are the final approved development reference; development starts from them. Does NOT approve live-service readiness or the owner's commercial inputs. | Delivery lead (86400 Studio), citing the owner approvals below | Opens the Setup Gate |
| D-PRE-01 | 2026-09-13 | Payments provider is Razorpay; delivery provider is Shiprocket. Does NOT decide capture/refund/settlement configuration or status mapping (owner inputs OI-08). | Owner | AF1 §1; `PAGE-TRACKER.md` |
| D-PRE-02 | 2026-09-13 | Public design is LOCKED at R13: Home design, supplied About imagery, product R12 moodboards and all approved public revisions are preserved. Covers visual revisions, not live readiness. | Owner | `PAGE-TRACKER.md` "Approval scope retained"; `PROJECT-HISTORY.md` |
| D-PRE-03 | 2026-09-15 | Guest checkout without customer accounts for V1; saved artworks stay browser-local; optional accounts are a later backlog item. | Owner | `PAGE-TRACKER.md` 2026-09-15; AF1 §6 "customer accounts — not V1" |
| D-PRE-04 | 2026-09-15 | Mobile navigation (menu left, logo centred, cart right; Track order inside Menu) and Saved-artworks placement (Menu and cart drawer, not in filters) are SIGNED OFF. | Owner ("2 and 4 are sorted") | `PAGE-TRACKER.md` |
| D-PRE-05 | 2026-09-15 | E6/M4 mobile wall-to-materials animation and material-card styling SIGNED OFF; desktop unchanged. | Owner ("This is perfect") | `PAGE-TRACKER.md` M4 approval |
| D-PRE-06 | 2026-09-15 | E7/M5 final mobile refinements (promises and extra material heading removed on mobile; centred mobile footer brand; mobile-only Legal column) — merge and publication authorised; owner visual review is the recorded next action. | Owner | `PAGE-TRACKER.md` |
| D-PRE-07 | 2026-09-15 | E8 final logo (`assets/brand/aalishaan-studio.png`, 2172 × 724) approved for public header and footer; nothing else changed. | Owner | `PAGE-TRACKER.md` "Resume here" |
| D-PRE-08 | 2026-09-06 | A2 launch prices per finish: White ₹19,000, Black ₹21,000, Antique Gold ₹23,000 across all 22 artworks. Not deployment or payment activation. | Owner (R5 sign-off) | `PROJECT-HISTORY.md`; `data/pricing.json` |
| D-PRE-09 | 2026-09-13 | Preserve the A2 launch configuration: 22 artworks, three finishes, R12 moodboards; no COD, new sizes, finishes, custom orders or extra channels. The older "Version 2" attachment's catalogue/price/finish proposals do not override the storefront. | Owner (AF1 approval) | AF1 §1, §6 |
| D-PRE-10 | 2026-09-13 | Dedicated in-house Accounting, CRM and Customer Support tools come after V1 (planning on hold); ~~the Tools tab is one planned-tools page~~ (superseded 2026-09-17 by D-PRE-23: the final Admin has no Tools menu — the deferred-tools boundary and US23 are handled per D-29); no paid accounting/helpdesk subscription is a V1 dependency. V1 still includes invoices/credit notes, financial records, customer history and basic support. | Owner | AF1 §1, §6 |
| D-PRE-11 | 2026-09-13 | V2 options V2-01…V2-08 are conditional; each needs a recorded repeated problem, cost, benefit, smallest implementation and keep/remove criterion. US24 is a scope-control story, not a V1 requirement. | Owner (AF1 approval) | AF1 §6, §11 |
| D-PRE-12 | 2026-09-11 | Real social-profile URLs are deferred by the owner; the five footer icons have no destinations and no dummy links are created. | Owner ("I'll add link later on" — stored with encoding artefacts in the R10 record) | `PROJECT-HISTORY.md` R10 resume; `PAGE-TRACKER.md` |
| D-PRE-13 | 2026-09-13 | Shipping charges, tax treatment, outer framed dimensions and cancellation/return/refund terms stay listed as owner decisions required before live sales; their absence does not block development; never replace them with guessed terms, zero charges or example rule values. | Owner ("Keep them clearly listed as owner decisions required before live sales") | `PAGE-TRACKER.md`; `DEVELOPMENT-HANDOFF.md` |
| D-PRE-14 | 2026-09-14 | E1 is the frozen, reviewed development reference (public R13 + M1 + E1 fixes + Admin A1), superseded by the later E4–E8 revisions listed above and, for the Admin, by E9 (D-PRE-23); source/asset hashes in `data/development-baseline.json`. | Owner-authorised review; technical freeze | `DEVELOPMENT-HANDOFF.md` |
| D-PRE-15 | 2026-09-14 | Prototype publication authorised to `86400studio/aalishaan-studio` and GitHub Pages (https://86400studio.github.io/aalishaan-studio/). Not live sales; hosted Admin is a public fictional demo. | Owner | `PAGE-TRACKER.md` |
| D-PRE-16 | 2026-09-13 | AF1 (`prototype/docs/ADMIN-FINAL-FEATURES.md`) is the canonical Admin planning baseline: 26 capabilities, ~~six menus + Settings, Today as default~~ (AF1 §3's menu layout is superseded by the final Admin, D-PRE-23; capabilities, stories, journey and milestones are unchanged), eleven-stage order journey, four customer milestones, US01–US23. Approves the plan, not screens or live readiness. | Owner ("Perfect - Approved by me") | AF1 preamble |
| D-PRE-17 | 2026-09-11 | About the Studio article content and design SIGNED OFF (retained R8). | Owner ("Its perfect, lets keep as is") | `PAGE-TRACKER.md` |
| D-PRE-18 | 2026-09-12 | Product page button labels and order: Size guide, What you'll receive, The artwork's story, FAQs. | Owner | `PROJECT-HISTORY.md` (R12) |
| D-PRE-19 | 2026-09-06 | Home, Shop All, Collections, Art Styles and Product page groups SIGNED OFF at R5; Collections and Art Styles index-hero intros locked. | Owner | `PROJECT-HISTORY.md` |
| D-PRE-20 | 2026-09-07 | The prototype is an imagined site: no messages, payments, shipments or operational updates are real; the production build replaces every demo action with server behaviour and never ships fake success. | Owner | `PROJECT-HISTORY.md`; `DEVELOPMENT-HANDOFF.md` |
| D-PRE-21 | 2026-09-13 | ~~Admin A1 connected prototype build authorised as the current Admin design; W1 wireframe retained as historical reference only.~~ — **superseded 2026-09-17 by D-PRE-23**; the A1 files were removed from the workspace (they remain in the published prototype commit `b24dce1b…`); the W1 wireframe survives only inside the A2 package, whose `index.html` loads `wireframe/scope.js` at runtime. | Owner | `ADMIN-PROTOTYPE-A1.md` (historical); `PAGE-TRACKER.md` |
| D-PRE-22 | 2026-09-13 | Email is the minimum launch messaging channel; automated WhatsApp/SMS is optional, not assumed, and needs separate configuration and approval. | Owner (AF1 approval) | AF1 S3 |
| D-PRE-23 | 2026-09-17 | **Admin A2 — 86400 UI edition** (`prototype/admin/`; package manifest `prototype/admin/CONTENTS-SHA256.txt`) is the final Admin design reference. It supersedes Admin A1 and AF1 §3's menu layout: seven sections — Orders (landing page), Customers, Products, Support, Reports, Staff & Access, Connections — with Store setup and Access & backups in the owner menu, and global search, View website and Help & demo in the top bar. AF1's 26 capabilities and stories US01–US23 remain the canonical scope; `prototype/admin/CAPABILITY-MAP.md` maps every capability ID to its home in the final Admin. Does NOT record an owner visual sign-off of these screens (OI-14) and does not approve live readiness; the package itself states it is a local prototype. | Delivery lead (86400 Studio): "the new final admin pages" | Supersedes D-PRE-21; amends D-PRE-00, D-PRE-10, D-PRE-16. Consequences are decisions D-28–D-36 |

## 8. Open decisions (resolve here, then propagate)

Resolved rows are stamped **RESOLVED [DATE]** (or **ACCEPTED** for consciously-taken risks) — never deleted. An accepted risk must name its compensating control and where it's tracked. Every delivery decision carries a lean so work can proceed; rows marked **Owner** carry a delivery-lead interim lean and are Gate 0 items for their consuming sprint. S0.0 confirms the leans.

**Questions for the owner** (answers become RESOLVED rows or dated leans). Status at S0.0, 2026-09-23: the questions were consolidated into one request in the S0.0 report; the GitHub part of question (1) was answered the same day (D-02 RESOLVED); every other item stays a dated lean and a Gate 0 item at the sprint that consumes it (§8a status column, §8b). (1) Which account or organisation owns the app repository and the Vercel, Sentry, Supabase, Cloudflare Turnstile and Upstash accounts — owner-created now, or delivery-lead-created and transferred at handoff (D-02, D-03, S0.1/S0.2 Gate 0, `HANDOFF.md` §1)? (2) The production domain and www vs apex preference, which also fixes the email sending domain (D-04, D-06)? (3) Charsen display font — buy the commercial licence or approve a replacement face before S1.3 self-hosts it (D-13)? (4) Razorpay capture mode, and whether the Razorpay, Shiprocket and email accounts are shared with other sites (D-21, OI-08)? (5) Transactional email provider choice and who approves the interim milestone-1 wording (D-06, OI-09)? (6) The waiting-list customer copy and reopening-email subject drafted in prototype E10 (NS-12–NS-14), the checkout payment-policy line and FAQ answers (NS-15), the 90-day waiting-list retention lean, and whether payment-link orders for customers who ask to pay another way should be planned after launch (D-37, D-38)? Also requested: visual review of the E5/E7 mobile screens and the final Admin screens — A2, 86400 UI edition (OI-14) — including the owner's acceptance that its seven sections supersede the AF1 §3 menu layout approved on 2026-09-13 (D-PRE-23 amending D-PRE-16: no Today screen, no Tools page, refunds worked in Support); confirmation of customer tags and the composer's Promotional purpose (D-33); acceptance of the three visible storefront deviations at S1.3 (D-11, D-14, D-15 — D-11's facts were corrected at S0.0, §8c); and, added at S0.0, confirmation of the D-24 approval lane for honest technical strings.

### 8a. Delivery decisions (delivery lead, confirm at S0.0 unless marked Owner)

S0.0 (2026-09-23) checked every row below against its sources and stamped its status. "Lean confirmed" means the S0.0 implementation (Claude Code, under the executed S0.0 prompt, which instructs it to confirm unchanged delivery leans) found the lean sourced and consistent after read-only review; it is subject to the independent review of the S0.0 PR, takes effect when the owner merges that PR, and is not owner approval; Owner portions stay open as Gate 0 items at the sprint named in the status. The evidence and every correction the consuming sprint must carry are in §8c.

| ID | Decision needed | Options / current lean | Needed by | Status |
|---|---|---|---|---|
| D-01 | Stack lock | Lean: Next.js (App Router) + TypeScript strict + Tailwind + pnpm on Vercel; Supabase (Postgres, Auth with MFA for staff, Storage, RLS); Zod; Upstash rate limiting; Cloudflare Turnstile; Vercel Cron worker over a pending-work table; Vitest + Playwright; Sentry. Alternative considered: Prisma + custom Postgres (rejected: loses RLS as the last line of defence). | S0.0 | Lean confirmed 2026-09-23 (S0.0); the stack is locked when the S0.0 PR merges |
| D-02 | Repository strategy | Lean: new repo `86400studio/aalishaan-studio-app`; the prototype repo `86400studio/aalishaan-studio` stays frozen and published for side-by-side comparison. Alternative: continue in the prototype repo (rejected: its Pages workflow, baseline checksums and `pages/` folder collide with an app). | S0.0 | **RESOLVED 2026-09-23** (owner, S0.0 Gate 0): `86400studio/aalishaan-studio-app` on the owner's own personal GitHub account `86400studio` (a user account, not an organisation), public, no transfer planned — "Its my own account and I'll keep it here and no need to transfer as I'm the owner"; the prototype repository stays frozen and published |
| D-03 | Hosting profile and Production gating | Lean: Vercel; PR Previews; Production only from `main`; Production closed to sales until S3.4 via `SALES_MODE=off` **and** `business_rules.sales_open=false`, plus Deployment Protection on Production where the Vercel plan allows (verification of Production headers and smoke tests then use the automation-bypass header; if protection is unavailable, the noindex holding state and the sales gate are the closure). | S0.1 | Lean confirmed 2026-09-23 (S0.0) — notes §8c (Vercel plan; live webhooks while Production is protected) |
| D-04 | Production domain — **Owner** | Lean: `aalishaanstudio.com` (the mail domain used in the approved copy: `info@aalishaanstudio.com`); www vs apex decided at S3.4. `[DOMAIN]` tokens in `README.md`, `LAUNCH-CHECKLIST.md`, `SUPABASE-VERCEL-SETUP.md`, `HANDOFF.md`, `TECH-ARCHITECTURE.md` are filled when resolved. | S3.3 (DNS at S3.4) | Open (owner) — interim lean re-dated 2026-09-23 (S0.0); Gate 0 at S3.3 (sending domain) and S3.4 (DNS) |
| D-05 | Database/auth profile | Lean: Supabase, two projects `aalishaan-studio-test` / `aalishaan-studio-prod`; staff-only auth (email + password + mandatory TOTP MFA, no public sign-up); RLS default-deny; MCP Profile A (non-production only). | S0.2 | Lean confirmed 2026-09-23 (S0.0) — notes §8c |
| D-06 | Transactional email provider — **Owner account** | Lean: Resend (API for the outbox + SMTP for Supabase Auth emails); sending domain = D-04 with SPF/DKIM/DMARC at S3.3. Alternatives: Postmark, Amazon SES. Env names `EMAIL_API_KEY`, `EMAIL_FROM_ADDRESS` become provider-specific once chosen. | S1.8 | Lean (Resend) confirmed 2026-09-23 (S0.0); provider choice and owner-held account open — Gate 0 at S1.8 |
| D-07 | Prototype assets in the app repo | Lean: commit the prototype's text sources (HTML, CSS, JS, scripts, data, docs — a few MB) plus `prototype/assets/fonts/` and `prototype/assets/brand/` (≈0.5 MB: Charsen, licence file, E8 logo); git-ignore the rest of `prototype/assets/` (500+ MB). The pinned binary reference is the published prototype commit `b24dce1b…`; the S1.1 image-upload script reads a local checkout of that commit at a path given by a CLI argument or env name; the app serves optimised WebP from a Supabase Storage public bucket. Alternative: commit everything (rejected: clone time and repo limits). | S0.0 | Lean confirmed and applied 2026-09-23 (S0.0) by the root `.gitignore`; the workbook and orphan-image dispositions are in §8c |
| D-08 | Approved-copy handling | Lean: the frozen prototype pages and their build sources are the canonical copy (no `docs/content/page-copy/*.md` extraction set); each page sprint cites its exact source files and QA compares verbatim; new strings go to `docs/content/new-strings.md` for owner approval. Recorded deviation from `NEW-WEBSITE-SETUP-CHECKLIST.md` §3. | S0.0 | Lean confirmed 2026-09-23 (S0.0), with the E10 draft-copy carve-out in §8c |
| D-09 | Order-number format and confirmation route | Lean: `AS-` + gapless sequence from 1001 (prototype uses `AS-1001`) via `next_order_number()`; confirmation page at `/order/[token]` with an unguessable token; the prototype alias `/order/demo` redirects to `/track`. | S1.1 / S1.6 | Lean confirmed 2026-09-23 (S0.0) — notes §8c |
| D-10 | PDF library for receipts, invoices, credit notes | Lean: `@react-pdf/renderer` rendered server-side (Node runtime on Vercel). Alternative: `pdf-lib`. Decide once; S1.8 (receipt) and S2.15 (documents) share it. | S1.8 | Lean confirmed 2026-09-23 (S0.0) — notes §8c |
| D-11 | Decorative help-search banner (About hub, Help and Contact pages) | ~~Lean: remove the prototype's decorative `help-search` form from all three help-variant pages (`prototype/pages/about/index.html`, `pages/help/index.html`, `pages/contact/index.html` — it posts to the About page and performs no real search) and keep the working in-page FAQ filter on Help; no fake search ships. Visual deviation from the prototype — owner awareness at S1.3.~~ **Interim lean amended at S0.0 (2026-09-23) on corrected facts (§8c):** reproduce the approved design — keep the `help-search` form on the ten help-variant pages (About hub, Help, Contact and the seven policy pages) and port the About hub's working client-side search over the same static FAQ and policy content, so the form never submits to a search that does not exist; keep the in-page FAQ filter on Help; no fake search ships. Removing the form instead would be a visual deviation from the locked R13 design (D-PRE-02) that needs owner acceptance. | S1.3 | Interim lean amended 2026-09-23 (S0.0) on corrected facts (§8c) — confirmed or changed at the S1.3 Gate 0 |
| D-12 | Cookies, analytics, consent and the primary-metric instrument | Lean: no analytics or third-party scripts in V1; essential cookies only (staff session, Turnstile); no consent banner needed, Cookie Policy copy stays; the primary success metric (paid orders) is measured from the orders table (Reports R1). Revisit if analytics are added. | S0.0 | Lean confirmed 2026-09-23 (S0.0) with the clarification in §8c (no analytics, advertising or tracking scripts; the functional provider scripts belong to D-01) |
| D-13 | Charsen display font licence — **Owner** | The prototype ships `assets/fonts/charsen.otf` under "Demo for Personal Use" (`assets/fonts/iFonts-License.txt`). A commercial licence or an approved replacement face is required before launch. Interim lean (delivery lead): S1.3 self-hosts the current file on Preview only, flagged in `DESIGN.md`; S3.1 applies the owner's outcome; no Production launch with the demo licence. | S3.1 (ideally S1.3) | Open (owner; interim lean re-dated 2026-09-23 at S0.0) — interim lean at the S1.3 Gate 0; the outcome is a Gate 0 item at S3.1 and S3.4 |
| D-14 | Instrument Sans loading | Lean: self-host Instrument Sans (Open Font License) weights 400/500/600/700 on every page. The prototype loads it only on Home (other pages fall back to a system sans); consistent loading is a small visual difference on non-Home pages — owner awareness at S1.3. | S1.3 | Lean confirmed 2026-09-23 (S0.0) — notes §8c |
| D-15 | Focus-ring colour | Lean: one storefront focus ring (2 px crimson `#b51f2a`, offset 3 px) on every page; Admin keeps its green ring. The prototype's cascade varies by page. | S1.3 | Lean confirmed 2026-09-23 (S0.0) — notes §8c |
| D-16 | Background worker on Preview | Lean: worker route ~~`POST /api/jobs/run`~~ `GET /api/jobs/run` (method amended at S0.0, 2026-09-23 — Vercel Cron invokes with GET and sends `CRON_SECRET` as a Bearer `Authorization` header, §8c) guarded by `CRON_SECRET`; Vercel Cron on Production; on Preview a GitHub Actions schedule (every 5 min) pings the stable Preview alias, and Playwright triggers it explicitly in flow tests (Vercel Cron does not fire for Preview deployments). | S1.8 | Lean confirmed 2026-09-23 (S0.0), method amended to GET (§8c); sub-daily Vercel Cron needs a paid Vercel plan — S0.1 Gate 0 |
| D-17 | Stable Preview callback alias for sandbox webhooks | Lean: a dedicated Vercel alias assigned to the candidate deployment before payment/shipping proofs, serialised per run, using the Protection Bypass for Automation parameter on webhook URLs (secret redacted from evidence); recorded per run in `ENVIRONMENT-PARITY.md` §7.11. | S1.7 | Lean confirmed 2026-09-23 (S0.0) — notes §8c |
| D-18 | Timezone and calendar basis | Lean: Asia/Kolkata (IST) for working days, due dates, cut-offs, report date basis and the weekly digest; timestamps stored in UTC. | S2.1 | Lean confirmed 2026-09-23 (S0.0); applies from S1.8 (§8c) |
| D-19 | Performance budget | Lean: adopt the `QA-CHECKLIST.md` default (mobile Lighthouse ≥ 90, LCP ≤ 2.5 s, CLS ≤ 0.1, JS ≤ ~300 KB gzipped on the primary journey); first measured at S1.3. | S1.3 | Lean confirmed 2026-09-23 (S0.0) — notes §8c |
| D-20 | Pending-order expiry | Lean: a pending order expires 60 minutes after its Razorpay Order is created; a captured payment arriving after expiry sets `needs_review` and a readiness exception (AF1 O3) — never a silent refund or a silent confirmation. | S1.8 | Lean confirmed 2026-09-23 (S0.0) — notes §8c |
| D-21 | Razorpay capture mode — **Owner** | Auto-capture vs manual capture on the Razorpay account. Interim lean (delivery lead): assume auto-capture; the app treats only `captured` as paid either way, so the choice affects the review queue and settlement timing, not confirmation logic. | S1.7 | Open (owner; interim lean re-dated 2026-09-23 at S0.0) — Gate 0 at S1.7 (§8c) |
| D-22 | TEST-data hygiene | Lean: reset/seed scripts for the TEST project (S0.2); Preview test purchases are periodically reset; the Launch Gate runs from a deterministic fixture; PROD is never seeded with test identities. | S0.2 | Lean confirmed 2026-09-23 (S0.0) — notes §8c |
| D-23 | Sentry timing | Lean: install Sentry at S0.1 (the SOP module schedules it before launch) so every Production smoke test is observed; `ENVIRONMENT-PARITY.md` ratifies the deviation at S0.0. | S0.0 | Lean confirmed 2026-09-23 (S0.0); the ratifying sentence is in `ENVIRONMENT-PARITY.md` §4 |
| D-24 | Approval lane for new strings | Lean: honest technical states (a disabled form, an order still confirming, an estimate not yet available, a synthetic-rule flag — NS-01–NS-06, NS-08, NS-09, NS-10, the NS-13 validation and resume-link states, NS-16) may ship with delivery-lead approval and owner review at the next check-in; customer-facing commercial, policy or milestone wording (NS-07 and any policy text) is always owner-approved (OI-09). Recorded in `docs/content/new-strings.md` rule 1; owner to confirm. | S0.0 / S1.3 | Delivery-lead lean confirmed 2026-09-23 (S0.0); owner confirmation requested (§8) — Gate 0 item at S1.3 |
| D-25 | Multi-PR sprints | Lean: S0.1, S0.2, S1.9 and S2.5 may each land as two PRs `a`/`b` in sequence (S2.5a product editor and publish; S2.5b product and general images); S3.1 is several owner-input PRs; S3.2 is a parent run plus fix PRs `S3.2-Fn`. Every PR is recorded in the §2 Notes column. Deviation from `TECHNICAL-INTEGRITY.md` "one normal PR" for the Code Check and from `error-tracking/SETUP-CHECKLIST.md` Part 2 accepted because no behaviour code exists during setup. | S0.0 | Lean confirmed 2026-09-23 (S0.0) — notes §8c |
| D-26 | First PR before the Code Check exists | Lean: the S0.0 docs-only PR merges under PR-required protection without the Code Check status (S0.1 adds it and makes it required); the S0.0 sprint record marks the command/CI/Preview/Production exit-gate items N/A. Independent review still applies. | S0.0 | Lean confirmed and applied 2026-09-23 (S0.0): the owner's `main` ruleset has no required status check and was read back from GitHub on 2026-09-23 (S0.0 record); `TECHNICAL-INTEGRITY.md` and the setup checklist aligned; independent review still applies |
| D-27 | Disposition of manifest aliases the app does not port | Lean: `/order/demo` → redirect to `/track` (D-09); `/site-map` (the prototype page directory) not ported — `sitemap.xml` replaces it; `/admin/wireframe` (W1, historical) not ported — no redirect; `/404` and `/500` become the framework error pages; all other aliases redirect to their app routes (`TECH-ARCHITECTURE.md` §3d). | S3.4 | Lean confirmed 2026-09-23 (S0.0) — notes §8c |
| D-28 | O1 and US01 without a Today screen | Lean: follow the final Admin — no Today screen is built. O1/US01 are met on the Orders landing page: the **Attention** column, the **Needs attention** filter, the default **Priority first** sort, per-order **Follow-ups** (owner, follow-up date, status Open / Acknowledged / Snoozed / Resolved, outcome or next action), saved views and bulk assignment under More tools; requests without an order surface in Support. US01's "an empty queue says there is no outstanding work" is met by the honest empty state of the Needs attention filter. | S1.9 / S2.2 | Lean confirmed 2026-09-23 (S0.0) — notes §8c |
| D-29 | US23 and the deferred tools without a Tools menu | Lean: no Tools menu or planned-tools page is built — the final Admin redirects `#tools` to Help & demo › Scope. US23 passes when V1 operates without any paid accounting or helpdesk tool, no fake application actions exist, and stable IDs and exports are documented in `docs/DATA-CONTRACT.md` (S2.20); the production Help page carries the scope statement. Supersedes the Tools-page wording of D-PRE-10. | S1.9 / S2.20 | Lean confirmed 2026-09-23 (S0.0) |
| D-30 | Where refunds are worked | Lean: follow the final Admin — refunds and remedies are worked in **Support › Refunds** (an owner-only tab) on the one canonical refund record that belongs to the order and its payment; the order's More menu links there; settlements and disputes open from Reports. AF1 is unchanged in substance: Support recommends, and execution requires the finance permission, which only the owner holds at launch (D-35). | S2.4 | Lean confirmed 2026-09-23 (S0.0), as amended by D-39 — notes §8c |
| D-31 | Production form of "Help & demo" | Lean: the production Admin keeps a top-bar **Help** page with the operating guides (the A2 `settings/guides` screen has no link — production links it), the capability coverage / readiness view (G6) and the scope statement. The Guided demos, the 23 scenario starts, the scenario indicator and the "LOCAL PROTOTYPE" bar are prototype scaffolding and are not built; US01–US23 acceptance runs use seeded fixtures on Preview (S3.2). | S1.9 / S2.20 | Lean confirmed 2026-09-23 (S0.0) — notes §8c |
| D-32 | Prototype-only mechanisms are replaced, not ported | Lean: (a) the passphrase-encrypted browser vault → server-side private storage with role-checked, short-lived signed URLs, stored checksums and immutable versions (S2.3); the **Private files** tab keeps its layout — print originals by version, approval evidence, version approvals — without any passphrase UI; (b) the local backup/restore ZIP and "Request persistent storage" → managed database backups with point-in-time recovery and the S2.20 restore drill; Staff & Access shows a read-only backup and recovery status; (c) "Download replacement assets" ZIP → direct publish of public images to storage with ISR revalidation (S2.5); (d) "Publish in demo" → a server-validated publish; (e) role preview, the sample sign-in code, "Sign-out preview" and local staff records → real staff authentication with MFA and server RBAC (S1.2, S2.1); (f) simulated provider outcome selectors and demo tracking buttons → real provider events, sandbox on Preview (S1.7, S2.9, S2.10); (g) the single localStorage document `aalishaan-admin-a1-v1` and IndexedDB `aalishaan-admin-a2-files` → Postgres and Supabase Storage; (h) evidence recorded as file names → authenticated evidence uploads (S2.8); (i) sample HTML documents marked "SAMPLE — NOT A TAX DOCUMENT" → server-generated PDFs (S2.15); (j) the E10 storefront preview's read of the Admin's localStorage intake state, the `?intake=paused` / `?intake=open` query switch, the localStorage waiting list `aalishaanIntakeWaitlist` and its unsigned resume token → the `sales_open` rule version read server-side, the `intake_waitlist` table and signed single-use tokens (S2.21). | All Admin sprints | Lean confirmed 2026-09-23 (S0.0) — notes §8c |
| D-33 | Final-Admin refinements adjacent to AF1, built as designed with fences | Lean: customer **tags** are internal labels with filtering only ("Tags do not grant marketing permission") — no segments or campaigns (V2-04 stays deferred); the product **Research notes** field (the rendered label; the source string "Research keywords / planning notes" is rewritten by `prototype/admin/studio-ui.js` line 33) is an internal note with no public output; the contact **composer** (email, WhatsApp, SMS links) is a manual hand-off that logs "Compose opened - delivery not verified", and its **Promotional** purpose, which the composer always lists, is refused at submit unless that channel's consent is Granted (`prototype/admin/workspace.js` line 53); the **WhatsApp** and **SMS** cards in Connections are shown as optional and not configured — no integration in V1 (D-PRE-22); **Paused** and **Archived** are display labels of the stored Hidden and Retired states; **Delete unused draft** keeps its reference checks; the Reports tab is named **Products**. **Owner** to confirm tags and the Promotional purpose. | S2.5, S2.12, S2.13, S2.18, S2.19 | Lean confirmed 2026-09-23 (S0.0); owner confirmation of tags and the Promotional purpose open — Gate 0 at S2.12 (the Promotional purpose stays off until confirmed) |
| D-34 | General (shared) images library | Lean: build the fourteen shared image slots of the final Admin (seven size guides, seven craft and materials) writing only to the existing approved image positions; an upload is public and reaches the storefront after an explicit publish with ISR revalidation; no new public layouts. Lands in S2.5b. | S2.5 | Lean confirmed 2026-09-23 (S0.0) |
| D-35 | Staff roles and permissions at launch | Lean: the owner plus the two presets of the final Admin — **Operations** and **Customer support**. Refunds, settlements, disputes, documents, finance exports, Reports › Sales & money and Products, Staff & Access, Connections and Store setup are owner-only at launch, as in the final Admin. The server permission model keeps six permission areas (work, support, finance, catalogue, settings, export) so an assignable finance permission can be added without redesign once the owner decides roles and approval limits (OI-10). Supersedes every earlier "finance role / finance flag" wording. | S1.2 / S2.1 | Lean confirmed 2026-09-23 (S0.0) |
| D-36 | Prototype revision and byte integrity of the Admin package | Lean: the local prototype is revision **E10** = public E8 (published commit `b24dce1b…`) + Admin A2 — 86400 UI edition (installed 2026-09-17) + the E10 additions of 2026-09-23 (D-37–D-39; `prototype/PAGE-TRACKER.md` E10 entry, `prototype/admin/CHANGELOG.md`). The published GitHub Pages prototype still serves A1 at `/admin` until the owner decides to republish; the delivery agent pushes nothing. The Admin package is committed with its bytes preserved: the rule `admin/** -text` was appended to `prototype/.gitattributes` on 2026-09-23 (a root-level rule would be overridden by that nearer file); every file in `prototype/admin/` is LF, so `prototype/admin/CONTENTS-SHA256.txt` (regenerated for E10, 39 entries) keeps verifying after checkout; the retained vendor manifest `prototype/admin/CONTENTS-SHA256-86400-2026-09-17.txt` and `prototype/admin/tests/source-integrity.json` are the vendor's records of the 17 September package and no longer verify for the files E10 changed (`state.js`, `settings.js`, `workspace.js`, `index.html`, `CHANGELOG.md`, `README.md`, `TEST-RESULTS.md` — recorded in `prototype/admin/CHANGELOG.md`). | S0.0 | Lean confirmed 2026-09-23 (S0.0): `admin/** -text` is the last rule of `prototype/.gitattributes`, all 40 Admin files are LF and the 39-entry manifest verifies on the workspace; the fresh-checkout result is recorded in the S0.0 record ("Git bootstrap, commit, push and PR") |
| D-37 | Order-intake pause and waiting list (delivery lead, 2026-09-23: "Approved - please proceed") | Lean: the pause is a versioned business rule, not a switch — a `business_rules` version with `sales_open=false` and `sales_open_reason='capacity'`, effective-dated and audited, written from Store setup › **Order intake** (owner-only; reason and confirmation); `prelaunch` is the pre-launch reason (S1.6, S3.4). While paused the public bag and checkout replace the checkout link, the contact/delivery and payment panels and the pay button with the capacity notice and a waiting-list form (email or mobile required, name optional; Zod + Turnstile + Upstash; one entry per contact); the entry stores a bag snapshot, its source (cart / checkout), the consent kind "Service notification" with wording version, and a hashed single-use token; no pending order and no Razorpay Order is created while paused, while webhooks for orders already paid still confirm. Resuming sends one reopening notice per contact with an email address through the outbox (template `ordering-reopened`, S2.18 registry) carrying `/resume/[token]`, which restores the bag once; phone-only contacts are listed for manual contact (no SMS/WhatsApp automation — D-PRE-22). Owner-only banner across the Admin while paused; **Waiting list** page (send notices, remove entry with reason); the count appears in Reports › Operations only. Retention: entries removed once notified or after 90 days, whichever is first (lean; OI-10). Customer-facing copy is NS-12–NS-14 — drafted in prototype E10, owner approval required. The prototype's mechanisms are prototype-only (D-32j). **Owner** to approve the copy and the retention lean. | S1.1 (the `sales_open_reason` column in `0002_orders`), S1.6 (gate reason), S2.18 (template), S2.21 (feature) | Lean confirmed 2026-09-23 (S0.0); owner approval of NS-12–NS-14 and of the 90-day retention lean open — Gate 0 at S2.21 |
| D-38 | Payment policy: online only, no cash on delivery (delivery lead, 2026-09-23) | Lean: V1 accepts online payment only — UPI, cards, net banking and wallets through Razorpay (D-PRE-09 stands: no COD). Customers who want to pay another way are directed to contact the studio: the checkout Payment panel ends with the payment-policy line and a WhatsApp link (`https://wa.me/919137624394` — the studio number of `locked-facts.md` §7 — with a prefilled draft carrying the bag summary only, never personal details; a link, not an integration — D-PRE-22), and two FAQ entries ("Can I pay cash on delivery?", "What happens if ordering is paused?") join the Help page. Such a contact is captured by staff as an ordinary Support request (Enquiry); no order is created and no report or analytics view treats it as a sale. A payment-link or manually created order for these customers is a post-launch backlog item, not V1. Wording NS-15 — drafted in prototype E10, **owner** approval required (commercial wording). | S1.3 (FAQ, privacy / cookies sentences), S1.6 (checkout line) | Lean confirmed 2026-09-23 (S0.0); owner approval of NS-15 open — Gate 0 at S1.3 and S1.6 |
| D-39 | Refund entry point on the Payment stage (amends D-30) | Lean: the order's Payment stage shows, once a payment is captured and only to the owner, a **Refunds & remedies** block — captured amount, confirmed refunds, remaining balance, open remedies — whose "Open remedies" opens the same canonical refund record in place (`panel=remedies` inside the selected-order workspace; Operations and Customer support see an owner-only notice and are refused server-side). Support › Refunds remains the canonical workspace and the More-menu entry stays; there is no second refund record, no new state and no new permission. Lands in S2.4 with the refund engine. Staff-facing labels NS-16. | S2.4 | Lean confirmed 2026-09-23 (S0.0) |

### 8c. S0.0 confirmation notes (2026-09-23)

Seven independent read-only reviewers checked every §8a row against the sources it names; the S0.0 engineer spot-checked the corrections below. Each note is carried by the sprint named — its sprint prompt reads the note at Gate 0. Nothing here is owner approval.

| ID | Note (evidence) | Carried by |
|---|---|---|
| D-02 | Resolved by the owner at the S0.0 Gate 0. `86400studio` is a personal GitHub **user** account (GitHub API `type: User`), not an organisation; documents that call it an organisation mean this account. Public visibility lets `main` be protected on the Free plan. `charsen.otf` in the public repository is the same file (same Git blob) already public in the prototype repository and on GitHub Pages — see D-13. | S0.0 |
| D-03 | Registered at the S0.1 Gate 0 at S0.0: record the Vercel plan. From S0.1 until the S1.1 migration creates `business_rules`, Production is closed by `SALES_MODE=off` and the noindex holding page only. The Vercel plan is recorded nowhere, yet Production Deployment Protection and D-16's cron cadence depend on it. The lean does not say how live provider webhooks reach Production while it is protected (S3.3 registers them; protection is removed at S3.4). | S0.1 Gate 0 (plan); S3.3 |
| D-05 | The S2.20 restore drill uses a scratch TEST project and PROD point-in-time recovery depends on the Supabase plan; neither is recorded — the plan is registered at the S0.2 Gate 0 at S0.0. | S0.2 Gate 0; S2.20 |
| D-06 | No env name exists yet for the email provider's webhook signing secret (`TECH-ARCHITECTURE.md` routes that webhook with a provider signature). | S1.8 / S2.18 |
| D-07 | Applied by the root `.gitignore`: text sources, `prototype/assets/fonts/` and `prototype/assets/brand/` (including the unreferenced `aalishaan-temporary-logo.webp`) are committed; the rest of `prototype/assets/` (631 files, about 557 MB) stays local. `prototype/data/catalogue-source.xlsx` — the retained source workbook, not a text source and not approved as a source of truth (`prototype/README.md`, `prototype/PAGE-TRACKER.md`) — is excluded as the S0.0 prompt directs; its local copy stays. The four `prototype/pages/assets/images/*.webp` (0.8 MB, part of the E10 baseline, referenced by no page) sit outside `prototype/assets/` and are committed under the default rule. A Git clone is therefore a reduced checkout: `development-baseline.cjs` reports the ignored files as missing there and passes only on the full-asset workspace. Site imagery other than the catalogue (`assets/site/`, `rooms/`, `size-guides/`, `product-references/`, used from S1.3) has no stated route into the app yet. | S1.1 / S1.3 |
| D-08 | Confirmed with a carve-out: the frozen pages and build sources are canonical copy **except** the E10 draft strings NS-12–NS-15 (FAQ entries on Help and in the About hub’s search index, privacy and cookies sentences, checkout payment line, waiting-list copy), which are not approved until the owner approves them (`new-strings.md` rule 8). The Track placeholder "e.g. AS-PREVIEW-001" shows a format the app never issues (D-09). | S1.3 / S1.6 |
| D-09 | `AS-1001` is the final Admin's sample data; the storefront prototype uses `AS-PREVIEW-001`. S1.1's `0002_orders` does not yet name the column that holds the `/order/[token]` token (or its hash). | S1.1 / S1.3 |
| D-10 | A server-rendered PDF needs an embeddable, licensed font containing the rupee sign (U+20B9); Charsen cannot be used under its demo licence. | S1.8 |
| D-11 | **Facts corrected.** The `help-search` form is on ten pages — About, Help, Contact and all seven policy pages (`prototype/scripts/build-pending.cjs` lines 19–21) — and on the About hub it is a working client-side search over the FAQ answers and policy text (`prototype/shared/about-hub.js`); on the other nine pages it submits to About with `?q=`. Removing it therefore removes a working feature of the locked public design, not a fake. The lean was amended at S0.0 to reproduce the approved design — keep the form and port the About search (§8a); S1.3 confirms that or records removal on all ten pages as a deviation with owner acceptance. §10 #3, `TECH-ARCHITECTURE.md` and the `ROADMAP.md` S1.3 brief were corrected at S0.0; `DESIGN.md` still says "decorative" (outside the S0.0 file list — follow-up). | S1.3 Gate 0 |
| D-12 | Clarified, consistent with D-01: "no third-party scripts" means no analytics, advertising or tracking scripts; the functional provider scripts already in the stack — Razorpay Checkout, Cloudflare Turnstile and the Sentry browser SDK — are permitted under the narrowest CSP. The prototype's Cookie Policy text describes the browser preview; it stays until S3.1 replaces the policy pages, which then list essential storage (bag, saved artworks) and those services. `LAUNCH-CHECKLIST.md` reads the lean accordingly. | S1.3; S1.6; S3.1 |
| D-13 | Registered at S0.0 as a Gate 0 item of S3.1 and S3.4 (`ROADMAP.md`). "Preview only" needs a mechanism: once S1.3 merges, Production serves the same build unless the font is switched off by environment. | S1.3; S3.1; S3.4 |
| D-14 | Self-hosted Instrument Sans ships with its OFL licence text and a recorded source and version. | S1.3 |
| D-15 | Computed from the source colours, the crimson ring on the dark footer (about 2.7:1) and the Admin ring on white (about 2.6:1) fall below the 3:1 non-text contrast level; measure on rendered surfaces and raise a new decision if needed. | S1.3; S1.9 |
| D-16 | **Correction for S1.8** (external fact, vercel.com/docs/cron-jobs, read 2026-09-23): Vercel Cron invokes the path with an HTTP GET and sends an `Authorization` header carrying `Bearer` and `CRON_SECRET`; a POST-only `/api/jobs/run` would never run on Production. The lean and `TECH-ARCHITECTURE.md` were amended at S0.0: the route accepts that GET (the Preview GitHub Actions trigger and Playwright send the same request). On the Hobby plan cron jobs run at most once a day, so the worker cadence needs a paid Vercel plan (D-03) — registered at the S0.1 Gate 0. Cron schedules are in UTC. | S0.1 Gate 0 (plan); S1.8 |
| D-17 | Per-run evidence belongs in `ENVIRONMENT-PARITY.md` §12 and the sprint record (§7.11 is the rule). Putting the bypass secret in webhook URLs stores it in the provider sandbox dashboards — record that place or use a dedicated secret, with rotation. | S1.7 |
| D-18 | Applies from S1.8, not S2.1: the receipt, PDF and confirmation email render dates on the server (UTC on Vercel), so Asia/Kolkata is set explicitly there. | S1.8 |
| D-19 | Define whether Razorpay Checkout counts toward the JS budget of the primary journey before it is measured. | S1.6 |
| D-20 | Seed 60 minutes as the initial synthetic `business_rules` value (Store setup › "Checkout hold expiry (minutes)" in the final Admin), not a code constant. S1.8 sets `needs_review`; the readiness exception arrives with S2.4 / S2.7. | S1.1; S1.8 |
| D-21 | S1.7's Gate 0 asks the owner with no fallback; S1.7 records whether it proceeds in Test Mode on the interim lean. Under manual capture, D-20's expiry plus the absence of an in-app capture action would change the S1.7/S1.8 scope. The shared-accounts part of owner question 4 is asked before each account's first use. | S1.7 |
| D-22 | The S0.2 reset/seed scripts can only be skeletons (no product tables yet); later data sprints extend them, and Preview purchases need a reset owner and cadence. | S0.2; S1.1 |
| D-23 | Confirmed. Two files outside the S0.0 file list disagree: `docs/ENV-VARS-SAFETY.md` (two Sentry projects) and `docs/error-tracking/SETUP-CHECKLIST.md` (a wrong §9 citation, Production-only alerts, the test route removed "in the same sprint"). | S0.1 |
| D-24 | Only the delivery-lead lean is confirmed; the owner's confirmation of the lane was added to the owner request (§8) and registered at the S1.3 Gate 0. NS-11's "approved under rule 1" wording is ambiguous for an added staff sentence. | S1.3; S1.9 |
| D-25 | S0.1a (scaffold + Code Check, no behaviour) is exactly what `TECHNICAL-INTEGRITY.md` asks; the split, not the single PR, is the deviation. Branch names for `a`/`b` PRs are not set. | S0.1 |
| D-26 | Applied 2026-09-23: the owner created the ruleset "main protection" (enforcement active; target the default branch; Restrict deletions, Block force pushes, Require a pull request with 0 approvals; no required status check); its active rules on `main` were read back from the GitHub API, and its bypass list is empty on the owner's settings screen (S0.0 record). S0.1 adds the Code Check to the same ruleset (`TECHNICAL-INTEGRITY.md` "Setup"). | S0.1 |
| D-27 | `TECH-ARCHITECTURE.md` §3d still marks `/site-map`, `/404` and `/500` TBD and serves identity paths directly, while D-27 says all other aliases redirect. | S3.4 |
| D-28 | An empty "Needs attention" filter renders the generic "No matching records." in the final Admin; the "no outstanding work" state is a new string. The Follow-ups list shows the owner only inside Manage. | S2.2 |
| D-30 | Amended by D-39. In the prototype the Refunds tab link renders for every role and non-owners reach "Access restricted" — S2.4 decides hide versus deny. | S2.4 |
| D-31 | The Operating guides screen is still linked in two places in the source, but neither link is reachable in the final Admin. | S1.9 / S2.20 |
| D-32 | (e) also covers S1.9 ("Sign-out preview"); (f) is applied at S2.1, S2.4, S2.9 and S2.10 (not S1.7). The rendered labels are "Sample access code" and "Request persistent storage". | as listed |
| D-33 | Line reference corrected to `workspace.js` line 53 (E10 inserted a function at line 22). Its "needed by" also covers S1.1 and S2.1 (display labels, optional channel cards). `docs/content/locked-facts.md` cites the same shifted lines — outside the S0.0 file list. | S2.12; locked-facts follow-up |
| D-36 | Verified: 40 Admin files, all LF; the 39-entry manifest matches; against the vendor manifest exactly the seven named files fail; `tests/source-integrity.json` covers 18 files, three of which E10 changed. The full list of E10 Admin changes is in `prototype/PAGE-TRACKER.md` (E10 entry) as well as `prototype/admin/CHANGELOG.md`. | S0.0 |
| D-37 | OI-10 now lists S2.21 among its consumers (§8b). The prototype does not de-duplicate waiting-list entries; production does (S2.21 brief). | S2.21 |
| D-38 | The two E10 FAQ entries are also in the About hub’s search index (the hidden `data-search` text of its FAQ result card, used by `prototype/shared/about-hub.js`), so a search on About matches them; "omit until NS-15 is approved" covers that index as well as Help. That the support number takes WhatsApp rests on the NS-15 approval. | S1.3; S1.6 |

### 8b. Owner inputs required before live sales (Gate 0 for S3.1; from `prototype/docs/DEVELOPMENT-HANDOFF.md` and AF1 §7)

| ID | Input | Responsible | Consumed by | Status |
|---|---|---|---|---|
| OI-01 | Seller legal identity, address, registrations, policy contact | Owner with business advice | S2.15 documents (synthetic until then), S3.1 policies/footer | Open |
| OI-02 | Tax classification and whether displayed prices include it | Owner / accounting adviser | S1.6 checkout rules (synthetic), S2.15, S2.19, S3.1 | Open |
| OI-03 | Shipping charges, destinations, production/transit promises, working days | Owner / fulfilment lead with Shiprocket | S1.6 (synthetic), S2.9, S3.1 | Open |
| OI-04 | Outer framed dimensions per orientation/frame, packed dimensions, actual weights | Studio / fulfilment lead | S2.5 product facts, S2.9 parcel rates, S3.1 | Open |
| OI-05 | Production commitment point, cancellation/change cut-offs, damage reporting, return eligibility, remedy scope, refund timing | Owner with policy advice | S2.7, S2.11, S2.14, S3.1 policy pages | Open |
| OI-06 | Materials/BOM, capacity, reservation expiry, rework/waste rules, quality/packing evidence | Studio / operations lead | S2.6, S2.8, S3.1 | Open |
| OI-07 | Production files, rights evidence, physical sample approvals, packaging/protection evidence | Studio owner | S2.5 publish gates, S3.3 Production catalogue | Open |
| OI-08 | Razorpay capture/refund/settlement configuration; Shiprocket account, status mapping, dashboard fallback | Owner + integration developer | S1.7, S2.4, S2.9, S2.16, S3.3 | Open |
| OI-09 | Transactional sender/provider and the four milestone templates (+ exception templates, and the waiting-list reopening notice — NS-14, S2.21); optional channel consent | Owner / developer | D-06, S1.8, S2.18, S2.21, S3.1 | Open |
| OI-10 | Staff roles, financial approval limits, identity verification, retention/deletion and export rules | Owner | S2.1, S2.12, S2.21 (waiting-list retention, D-37), S3.1 | Open |
| OI-11 | Invoice/credit-note issue point, numbering and correction rules | Owner / accounting adviser | S2.15 (synthetic watermark until then), S3.1 | Open |
| OI-12 | Representative order volumes, response targets, recovery time and permitted data loss | Owner / developer | S2.20 peak and restore tests, S3.1 | Open |
| OI-13 | Real social-profile links (explicitly deferred) | Owner | Post-launch backlog | Deferred by owner (D-PRE-12) |
| OI-14 | Owner visual review of the E5/M3 and E7/M5 mobile screens, the E1 fixes and the final Admin screens — Admin A2, 86400 UI edition (no owner sign-off of these is recorded in `prototype/PAGE-TRACKER.md` or in the Admin package) | Owner | S1.3 and S1.9 (and every Stage 1 Admin sprint) reproduce the E10 reference and flag these areas in their Preview evidence; non-blocking | Open |

## 9. Env vars record (NAMES only — never values)

Names are proposed; S0.1/S0.2 make them real. `TECH-ARCHITECTURE.md` §6 is the authoritative list; this section mirrors it. Public prefix: `NEXT_PUBLIC_`.

| Name | Public / server-only | Feature it switches on | Set in |
|---|---|---|---|
| `NEXT_PUBLIC_SITE_URL` | Public | Canonical URLs, redirects, email links | Vercel environments (Production = D-04 domain) |
| `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` | Public | Browser Supabase client (public projection reads) | Vercel Preview/Development → TEST project; Production → PROD project |
| `SUPABASE_SECRET_KEY` | Server-only | Webhooks, worker, exports, admin server actions | Vercel (Sensitive), per environment |
| `NEXT_PUBLIC_RAZORPAY_KEY_ID` / `RAZORPAY_KEY_SECRET` / `RAZORPAY_WEBHOOK_SECRET` | Public id / server-only / server-only | Checkout.js, Razorpay Orders and refunds, webhook signature | Test mode in Preview/local; Live only in Production (S3.3) |
| `SHIPROCKET_API_EMAIL` / `SHIPROCKET_API_PASSWORD` / `SHIPROCKET_WEBHOOK_TOKEN` | Server-only | Serviceability, shipments, tracking webhook | Test account in Preview/local; Live only in Production (S3.3) |
| `EMAIL_API_KEY` / `EMAIL_FROM_ADDRESS` (provider-specific names once D-06 is resolved) | Server-only | Transactional email from the outbox; Supabase Auth SMTP | Isolated test destination in Preview/local; live domain in Production |
| `TURNSTILE_SECRET_KEY` / `NEXT_PUBLIC_TURNSTILE_SITE_KEY` | Server-only / Public | Bot protection on checkout, contact, newsletter, tracking lookup, the waiting-list join (S2.21) | All environments (test keys outside Production) |
| `UPSTASH_REDIS_REST_URL` / `UPSTASH_REDIS_REST_TOKEN` | Server-only | Rate limiting on public writes and staff login | TEST database outside Production |
| `CRON_SECRET` | Server-only | Worker route authentication (`/api/jobs/run`) | All environments |
| `SALES_MODE` | Server-only | `off` / `test` / `live` — checkout enablement (with `business_rules.sales_open`) | `off` everywhere until S3.4 (Preview `test` from S1.6) |
| `SENTRY_DSN` / `NEXT_PUBLIC_SENTRY_DSN` / `SENTRY_AUTH_TOKEN` | Server / Public / build-only | Error tracking and source maps | One Sentry project with `preview` and `production` environments (D-23) |
| Supabase project refs (TEST / PROD) — identifiers, not secrets | — | Recorded here by name when created | Pending S0.2 |
| `PLAYWRIGHT_BASE_URL` / `VERCEL_AUTOMATION_BYPASS_SECRET` | CI / server-only | Launch Gate and morning check targets; Preview protection bypass | GitHub Actions secrets and local shell only |

**Never do this:** never record a value, key, token, or connection string in this file — names and service states only.

## 10. Known issues

The launch sprint cannot pass while this section has unresolved bugs — except deferrals the owner has explicitly accepted (cite the accepting decision ID). Deferred hardening is marked **"required before scale"**.

| # | Severity | Where | Issue | Status |
|---|---|---|---|---|
| 1 | Launch-blocking (licence) | `prototype/assets/fonts/charsen.otf` | Display font licensed "Demo for Personal Use"; commercial licence or replacement needed before launch. | Open — D-13 (owner) |
| 2 | Visual fidelity note | Prototype pages other than Home | Instrument Sans is loaded only on Home in the prototype; the build self-hosts it everywhere (D-14). Owner awareness at S1.3. | Open — D-14 |
| 3 | Honesty | `help-search` form on `prototype/pages/about/index.html`, `pages/help/index.html`, `pages/contact/index.html` and the seven `pages/policies/*/index.html` | ~~The banner posts to the About page and performs no real search; the build removes it on all three pages and keeps the working in-page FAQ filter on Help (D-11).~~ Corrected at S0.0 (§8c): the form is on ten pages and runs a real client-side search on the About hub; the D-11 interim lean, amended at S0.0, ports that search, so no fake search ships. | Open — D-11 (S1.3 Gate 0) |
| 4 | Informational | Product build of "The Bridge of Blue Stone" | Retained editorial warning: story opening differs from its short hook; both are approved copy. Do not "fix" by changing copy. | Accepted (approved copy) |
| 5 | Boundary (not a bug) | Published prototype `/admin` | The hosted GitHub Pages demo still shows the superseded Admin A1 (D-36); the Admin reference is the local `prototype/admin/` (A2 — 86400 UI edition). Either version is a fictional demonstration with browser-side role checks; nothing from it is ported as access control (`SECURITY-CHECKLIST.md` §9). | Accepted — reference only |
| 6 | Content placeholder by design | 7 policy pages | Policy copy intentionally states final terms are pending until the owner supplies them (D-PRE-13); S3.1 replaces it. | Accepted until S3.1 |
| 7 | Documentation tension (by decision) | `prototype/docs/ADMIN-FINAL-FEATURES.md` §3, §6 and US23 | AF1 still describes six menus with a Tools page, a separate Settings area and a Today screen; the final Admin has seven sections and none of those (D-PRE-23). AF1 stays canonical for capabilities and stories; read its §3 through `prototype/admin/CAPABILITY-MAP.md` and D-28–D-31. | Accepted — D-PRE-23 |
| 8 | Final-Admin gaps the build must close | `prototype/admin/` | The Operating guides screen has no link (D-31 links it from Help); staff preset permissions are decorative in the prototype (D-35 enforces them on the server); embedded order panels do not re-check the role in the prototype (every server action re-authorises — `SECURITY-CHECKLIST.md` §9); retained A1 wording such as "Return to Today" still surfaces in places and is not copied. | Open — closed by S1.2, S1.9, S2.1, S2.20 |
| 9 | Prototype tooling (reference only) | `prototype/scripts/review-static.cjs`, `prototype/scripts/check-admin-model.cjs` | `review:static` still asserts the A1 route `#settings/journeys` shows "US23" (in the final Admin that route redirects to Help & demo, where the story list sits inside a closed details element) and needs a one-line update before that script is used again; `check-admin-model.cjs` writes its report under `preview/admin-a1/` (cosmetic). Neither affects the application build. | Open — prototype tooling, not app scope |
| 10 | Prototype-only mechanism (by design) | `prototype/shared/intake-e10.js`, `prototype/admin/e10-additions.js` | The E10 storefront preview decides "paused" by reading the Admin's localStorage document in the same browser or the `?intake=` query switch, keeps waiting-list entries in the localStorage key `aalishaanIntakeWaitlist` and shows an unsigned resume token — demonstration only. Production reads the `sales_open` rule version server-side, stores entries in `intake_waitlist` and signs single-use tokens (D-32j, S2.21). The customer-facing copy in those files, the checkout payment line and the two FAQ entries are drafts, not approved wording (NS-12–NS-15). | Accepted — D-32j; copy approval open (owner) |
## 11. Update rules

- [ ] Update this file **in the same branch/PR** as the work it describes — state and code merge atomically.
- [ ] If the sprint branch is already merged, tracker flips ride a tiny dedicated `docs/` branch.
- [ ] When code and this doc disagree, report the mismatch; correct it only within the authorized scope.
- [ ] Strike through, never delete: resolved decisions, closed blockers, and retired scope stay visible with dates.

## 12. Placeholder register

Bracketed tokens that intentionally remain in the docs pack and who fills them:

| Token(s) | Where | Filled by |
|---|---|---|
| `[DOMAIN]` | `README.md`, this file §1, `docs/content/locked-facts.md`, `docs/LAUNCH-CHECKLIST.md`, `docs/SUPABASE-VERCEL-SETUP.md`, `docs/HANDOFF.md`, `docs/TECH-ARCHITECTURE.md` §1 | D-04 resolution (owner), applied in S3.3/S3.4 |
| `[SPRINT_ID]`, `[SLUG]`, `[DATE]`, `[YYYY-MM-DD]`, `[HEAD_SHA]`, `[MERGE_BASE_SHA]`, `[BRANCH_NAME]` | `AGENTS.md`, `CLAUDE.md` (branch example), `docs/WORKFLOW.md`, `docs/CODEX-REVIEW-PROMPT.md`, `docs/SPRINT-PROMPT-TEMPLATE.md`, `docs/BROWSER-TOOLS.md`, `docs/HANDOFF.md` sign-off, `docs/sprint-prompts/README.md`, `docs/code-reviews/README.md`, `docs/testing-setup/00-START-HERE.md`, `.claude/skills/*/SKILL.md` | Per sprint / per record, by `/sprint-prompt` and `/close` |
| `[ID] · [PASS/FAIL/PENDING/N/A] · [DATE] · [CANDIDATE SHA] · …` | `docs/ENVIRONMENT-PARITY.md` §12 proof record format | Per proof, by the sprint that runs it |
| Everything under `docs/templates/`, `docs/testing-setup/templates/`, `docs/error-tracking/templates/` | Per-use skeletons | Per use — never pre-filled |

**Placeholder scan (the S0.0 acceptance command):** run from the repository root

```bash
grep -rnoP "\[[A-Z][A-Za-z0-9_/ .:-]{2,}\](?!\()" README.md CLAUDE.md AGENTS.md docs .claude/skills --include=*.md --exclude-dir=templates | grep -vE "\[(SPRINT_ID|SLUG|DATE|YYYY-MM-DD|HEAD_SHA|MERGE_BASE_SHA|BRANCH_NAME|DOMAIN|ID|PASS/FAIL/PENDING/N/A|CANDIDATE SHA|DEPLOYMENT/CALLBACK SHA|SANITIZED EVIDENCE LINK|OWNER/NEXT ACTION|EXACT_PATHS|A-Z|A-Za-z0-9_/ .:-|RESOLVED \[DATE\]|Setup Gate / Stage 0|Not Started / In Progress)"
```

Expected output: none (every remaining token is in the register above; markdown link text is excluded by the lookahead and the command's own regex fragments are allow-listed); the skills' own bracket tokens (`[EXACT_PATHS]` etc.) inside per-use templates are excluded by `--exclude-dir=templates`. The `/close` skill references `docs/THREAT-MODEL.md`, created in S1.2.

Next step → open the active sprint in `docs/ROADMAP.md` and run it via `docs/WORKFLOW.md`.
