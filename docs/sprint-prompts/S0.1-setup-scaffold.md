# S0.1 — Scaffold, Code Check, security headers, Vercel Preview & Sentry

Prepared 2026-09-23; handoff checked 2026-09-24 using `/sprint-prompt` Mode A and `docs/templates/CLAUDE-SPRINT-PROMPT-TEMPLATE.md`. **Prompt prepared; implementation Not Started.** *(Preparation-time status, kept as history.)* **Executed 2026-09-24 — In Progress: PR #3 open, Code Check required and proven blocked-then-green, Preview of `3f07d66` PASS; next: the independent review. See "Execution record — Claude Code, 2026-09-24" and the sections after it.**

Owner-selected workflow: **GPT-6 Astra Codex** prepares the prompts and independently reviews Claude Code's completed implementation; **Claude Code** implements and fixes findings; the human owner merges. Prompt preparation is not implementation or an implementation review verdict.

## A. Diagnosis

S0.0 and its closeout PR are merged. At preparation the clean local `main` is `ce41b346e2db8f3d750be0dc39d054df7a0091f9` (PR #2); the tracker still points to the earlier merge. There is no root application manifest, source tree, Code Check or deployment evidence. S0.1 turns the governed repository into a deployable, checked, sales-closed scaffold before S0.2 adds the database and integration/browser proof harness.

Account ownership and the development plan are already answered. Carry the S0.0 D-23 documentation corrections, require real unit tests now, and keep all hosted checks pending until measured on the actual deployment.

## B. Sprint goal & scope

Follow `docs/ROADMAP.md` S0.1: Next.js App Router, strict TypeScript, Tailwind, ESLint/Prettier, pinned pnpm and Node 24, the six-check required Code Check plus full-history secret scanning and Dependabot, placeholder-only env documentation, six deployed security headers, isolated Vercel PR Previews and Production from `main`, a noindex holding page with sales closed, Sentry with an authenticated temporary test route and received owner alerts, and an exercised Instant Rollback.

Use one PR by default, as permitted by D-25. The allowed S0.1a/S0.1b split is available only if planned and recorded before implementation; it is not needed for this prompt. AF1 coverage: G5 / US21 (setup evidence only). Migrations: none. Database and integration/Playwright harness belong to S0.2; design tokens, fonts and storefront pages to S1.3; Razorpay CSP to S1.6; sales opening to S3.4.

## C. Branch name

`claude/s0.1-setup-scaffold`, created from current `main` of `86400studio/aalishaan-studio-app`. Preparation writes the records only; it does not create this implementation branch, commit or push.

## D. Step checklist

1. Inspect current state, preserve work, create the implementation branch and record setup choices.
2. Scaffold the empty app, pinned toolchain and hermetic unit-test contract without touching the prototype.
3. Add the holding page, six headers, Sentry and its server-guarded test route with meaningful boundary tests.
4. Add Code Check, the blocking secret scan and Dependabot; run all local checks.
5. Prepare the PR; when Git actions are authorized, publish it, require Code Check and prove blocked-then-green behavior.
6. Wire the owner's Vercel/Sentry settings and prove the candidate Preview, headers, denied route and alert delivery.
7. Obtain independent review of the tested immutable head; fix and repeat evidence when needed; owner merges.
8. Smoke-test Production, confirm its Sentry alert, exercise rollback, then record closure and the S0.2 cleanup handoff.

## E. Ready-to-copy Claude Code prompt

~~~text
You are my senior implementation engineer for the Aalishaan Studio website. CLAUDE.md governs this task.

## Context
S0.0 is Done (PR #1) and its closeout PR #2 is present in local main at ce41b346e2db8f3d750be0dc39d054df7a0091f9 at preparation. S0.1 is active but not implemented. Reinspect actual Git state; the preparation SHA is not a future review head. The frozen E10 prototype and governing docs already exist; no root app/package/CI is present at preparation.
The owner selected GPT-6 Astra Codex for prompt architecture and independent review, Claude Code for implementation/fixes, and the human owner for merge. Do not self-approve the implementation.

## Read first
- CLAUDE.md.
- docs/PROJECT-STATUS.md — §1–§6 state; §7 locked decisions; §8a and §8c D-01, D-03, D-07, D-13, D-23–D-26; §8b owner inputs; §9 environment names; §11 update rules.
- docs/ROADMAP.md — Sprint S0.1, Setup Gate, Universal sprint exit gate and S0.2 boundary.
- docs/WORKFLOW.md; AGENTS.md; docs/TECH-ARCHITECTURE.md §2, §3c, §6–§7; docs/TECHNICAL-INTEGRITY.md.
- docs/SECURITY-CHECKLIST.md; docs/ENV-VARS-SAFETY.md; docs/ENVIRONMENT-PARITY.md §2–§4, §12; docs/ROLLBACK.md.
- docs/APPROVED-INPUTS.md; docs/DESIGN.md; docs/content/locked-facts.md; docs/content/new-strings.md.
- docs/error-tracking/00-START-HERE.md, ERROR-TRACKING-GUIDE.md and SETUP-CHECKLIST.md (all three under docs/error-tracking/).
- docs/templates/NEW-WEBSITE-SETUP-CHECKLIST.md; docs/HANDOFF.md; docs/QA-CHECKLIST.md; docs/BROWSER-TOOLS.md.
- docs/templates/PR-DESCRIPTION-TEMPLATE.md; docs/templates/VERCEL-PREVIEW-TEST-TEMPLATE.md.
- docs/sprint-prompts/S0.0-kickoff-decisions.md and docs/code-reviews/S0.0-kickoff-decisions-review.md, including the closeout review history.
- This sprint record and docs/code-reviews/S0.1-setup-scaffold-review.md.

## Sprint / Branch
- Sprint: S0.1 — Scaffold, Code Check, security headers, Vercel Preview & Sentry.
- Branch: claude/s0.1-setup-scaffold, created from current main.
- Before editing, confirm the branch and inspect git status. Preserve existing user changes.
- Work from repo/, the Git root containing CLAUDE.md, not its download parent or prototype/. Vercel's source root is the Git repository root, not a repo/ subdirectory inside that repository.
- Sync through the existing authenticated remote without resetting user work. Reuse the intended branch only after checking that nobody else owns it. Carry these prepared records into the branch without losing them.

## Goal
Produce an empty Next.js app with a genuinely required six-check Code Check and blocking secret scan, a tested isolated Preview, six working security headers and Sentry alerts. Production deploys only from main and remains protected where supported, noindex and sales-closed. S0.1 closes only after independent review, owner merge, Production smoke/alert and an evidenced Instant Rollback drill.

## Not this sprint
- Supabase projects, clients, migrations, RLS, /api/health, integration tests, Playwright installation/config/specs and morning-check workflow — S0.2.
- Product schema — S1.1; staff auth/MFA/RBAC — S1.2; storefront/design tokens/fonts — S1.3 onward; Admin — S1.9 onward.
- Checkout/payment providers and their CSP origins — S1.6 onward; worker/cron and transactional email — S1.8 onward.
- Domain/canonical-domain resolution, commercial terms, live-provider wiring and sales opening — their existing gates, S3.1/S3.3/S3.4.
- Prototype changes, imported prototype runtime JS, demo mechanisms, public forms, analytics, session replay and marketing copy — excluded.

## Files
Inspect:
- Every Read first file; README.md; .gitignore; any existing root manifest, lockfile, config, workflow or src/ file before scaffolding.
- prototype/.gitattributes, prototype/package.json, prototype/admin/CONTENTS-SHA256.txt, prototype/data/development-baseline.json and prototype/scripts/development-baseline.cjs as read-only references.

Allowed to change:
- package.json, pnpm-lock.yaml, .nvmrc, tsconfig.json, next-env.d.ts, next.config.ts, postcss.config.mjs, eslint.config.mjs, .prettierrc.json, .prettierignore, .gitignore, .env.example, vitest.config.ts — the S0.1 toolchain and configuration only.
- src/app/layout.tsx, src/app/page.tsx, src/app/robots.ts, src/styles/globals.css — the minimal noindex holding page only; no store shell, catalogue, brand-token port or font assets.
- src/app/api/sentry-test/route.ts, src/lib/server/sentry-test.ts, src/lib/security-headers.ts — the temporary authenticated diagnostic route and shared header configuration only.
- src/instrumentation.ts, src/instrumentation-client.ts, sentry.server.config.ts, sentry.edge.config.ts, src/app/global-error.tsx — SDK hooks and the minimal React render-error boundary as required by the verified chosen Next.js/Sentry versions; omit hooks not used by that version. The boundary uses only a generic registered technical fallback, not the S1.3 product 500-page design.
- tests/unit/sentry-test.test.ts, tests/unit/security-headers.test.ts — meaningful S0.1 guard/header tests; no database, integration or browser harness.
- .github/workflows/code-check.yml, .github/dependabot.yml — the required checks, full-history gitleaks scan and dependency-update configuration only.
- README.md — runnable scaffold orientation, exact scripts and names-only setup instructions.
- docs/TECH-ARCHITECTURE.md, docs/ENVIRONMENT-PARITY.md, docs/ENV-VARS-SAFETY.md — actual pinned stack, S0.1 route/header/env choices, scopes and measured hosting/Sentry evidence; narrowly reconcile D-23.
- docs/error-tracking/SETUP-CHECKLIST.md — reconcile D-23's one-project/environment model, alert timing, guarded test lifecycle and names-only owner setup; tick only evidenced items.
- docs/TECHNICAL-INTEGRITY.md — S0.1 setup evidence and actual compatible configuration details; preserve all six gates.
- docs/ROLLBACK.md, docs/HANDOFF.md, docs/templates/NEW-WEBSITE-SETUP-CHECKLIST.md — measured drill, account/project ownership and S0.1 checklist results only.
- docs/content/new-strings.md — register the minimal holding/error technical strings and actual approval evidence under D-24; no invented approval or commercial wording.
- docs/PROJECT-STATUS.md and docs/ROADMAP.md — S0.1 progress/evidence and unresolved prerequisites during work; Done/next-sprint changes only after all closure gates actually pass.
- docs/sprint-prompts/S0.1-setup-scaffold.md — preserve the executed prompt; append results, Preview record, deviations, drill/smoke and follow-ups.
- docs/code-reviews/S0.1-setup-scaffold-review.md — actual target/evidence plus the independent reviewer's returned record, saved by the builder.

If another file is needed, stop and explain why before editing it.

## Gate 0
Already confirmed by the owner on 2026-09-23: Vercel and Sentry use the owner's own accounts; Vercel Hobby is the development plan (PROJECT-STATUS §8c D-03/D-23). Reuse those answers; do not request ownership confirmation again. The S1.3 commercial-use plan decision and other future owner gates remain at their consuming sprint.
Before account-dependent configuration/testing, verify access and have the owner enter required values directly into the correct host scopes and identify the alert recipient. Do not start dependent external operations until those prerequisites are confirmed; independent local scaffold/config/tests may proceed. Prepare the exact names, scopes and settings first. Missing access is an evidenced pending item, not permission to invent a deployment or alert.
Use env variable names only; never request, read, or paste live values.

## Task / Steps
1. Inspect inputs, current main, status and local tools. Record the single-PR plan, minimal src/ layout, selected exact stable package versions and the Node 24 major. Use official provider/framework documentation to verify current compatibility and plan capabilities at execution, not remembered CLI flags. Mark S0.1 In Progress only when implementation starts. Explain the short security/env plan before edits.
2. Scaffold App Router + strict TypeScript + Tailwind + ESLint with pnpm create next-app using the verified version/options. This is an existing populated repo: do not scaffold destructively over it. If the generator refuses, generate in a bounded scratch directory inside the workspace, inspect it, and copy only allowed application/config files; do not replace governing files, .gitignore, prototype/ or create a second Git history. Remove default promotional page/assets/font downloads from the imported app. Pin pnpm through packageManager and Node 24 through engines, .nvmrc and CI. Add Prettier and Vitest. Define typecheck (tsc --noEmit), lint, format:check (prettier --check .), test:unit, test and build; test delegates to test:unit at S0.1. No optional/empty-test success and no test:e2e stub: Playwright lands in S0.2. ESLint, TypeScript and Vitest exclude the frozen prototype. Prettier ignores prototype/, docs/, the existing root Markdown governance files, .claude/ and generated/cache output; this sprint chooses to preserve existing documentation formatting. Keep the prompt/review exclusions explicit. Format only new/changed implementation/config files as needed.
3. Implement only the noindex holding page, shared six-header configuration and Sentry. Register any new visible technical string in new-strings.md and use its actual D-24 approval lane before Production; do not make up marketing text or a launch date. Use semantic minimal markup and existing documented design values only where needed; no token/font port. Apply CSP, HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy and Permissions-Policy on a catch-all rule. Document the exact policy; allow only what this scaffold actually needs (including its Next.js runtime and configured Sentry ingestion), no speculative Razorpay/Supabase origins, wildcard workaround or production unsafe-eval. Prove hydration and SDK delivery under the enforced CSP; report rather than conceal an unavoidable policy tradeoff. Keep noindex effective on Preview and closed Production.
   Wire @sentry/nextjs using documented env names, one project and development/preview/production tags based on trustworthy deployment context; NODE_ENV alone cannot distinguish Preview from Production. Include the compatible global error boundary for React render-error capture, with its minimal approved technical fallback; do not build S1.3's designed 500 page. Bind releases to the deployed commit. Keep source-map upload credentials build-only, map artifacts private, default PII collection/replay off, and strip authorization headers, cookies, token-bearing URLs and customer data before reporting. No staff context is needed before S1.2. Missing Sentry configuration may degrade observability per architecture but cannot count as an alert PASS.
   Register the concrete temporary route POST /api/sentry-test and new server-only SENTRY_TEST_TOKEN in TECH-ARCHITECTURE §3c/§6, ENVIRONMENT-PARITY §4 and PROJECT-STATUS §9; blank placeholder in .env.example, distinct owner-set tokens per scope. Require an exact Bearer credential server-side before any event is emitted; unset/empty token, absent/malformed/wrong credentials and other HTTP methods never emit. Do not use query strings, public env vars, a UI button or Deployment Protection as the application guard. Accept no user-supplied error content. Return only a generic non-sensitive result, disable caching and bound SDK flush behavior. Unit tests exercise the real handler/guard with mocked event delivery, including allowed plus all denied cases and no denied-path capture. Verify the headers without a tautological config snapshot. The route stays through the post-merge Production test and is removed in S0.2 at the latest, with token revocation recorded.
4. Add the Code Check workflow per TECHNICAL-INTEGRITY: pull_request on every PR, required job named Code Check, pinned packageManager install with --frozen-lockfile, consistent Node 24, typecheck, lint, format:check, test:unit, build and critical production audit. Add full-history gitleaks scanning with redacted findings and a pinned supported scanner/action; its failure must block the required Code Check (or be separately required, with evidence). No continue-on-error, path-filter bypass, --if-present, --passWithNoTests, untrusted pull_request_target checkout or blanket scan exclusions. PR unit tests/build need no real credentials and never contact Sentry/providers; source-map upload runs only in the authorized hosted build. Dependabot covers root pnpm dependencies and Actions without touching the frozen prototype package. Run the exact local verification below and inspect all new/untracked paths as well as the diff. Document only names/placeholders in .env.example, with future integrations labelled not configured; never require future providers to boot this scaffold. Preserve all D-07 ignores and exceptions.
5. Prepare the PR description and names/scopes/settings handoff. Commit/push only under the Git action policy; NO leaves a verified local diff and an explicit pending remote-evidence list. When authorized, push only this branch and open its PR against main. After Code Check has actually run once, have the owner add its exact emitted context to the existing main ruleset; retain required PRs, no bypass actors, no direct/force pushes or deletion. Prove on this same PR that a subsequent authorized commit is unmergeable while required checks are pending/failing and eligible only after success, subject to other gates. Record run/head links and owner-observed or read-back rule/merge-state evidence; do not deliberately introduce broken application code or weaken rules to demonstrate this. No green check icon alone proves enforcement.
6. Import the app repository into the owner's Vercel project aalishaan-studio-app, Git root '.', production branch main, isolated PR Previews. Owner sets SALES_MODE=off in Development, Preview and Production; configure Sentry env names, source-map upload and the route token in their correct scopes without reading values. Keep Production protection on where the verified plan permits; record exact Preview policy and any platform limitation. Use the provider deployment URL while D-04 is unresolved; no DNS or invented real domain. No database exists: closure is SALES_MODE=off plus a holding page with no sale/form/payment surface, not a claimed business_rules gate.
   Run ENVIRONMENT-PARITY §12 target/provenance preflight for the exact candidate deployment before the bounded synthetic Sentry event. Record applicable P12 and hosting portion of P10, environment/deployment/release, noindex, all six headers, desktop/mobile behavior, no unexpected console/CSP/runtime failures, denied-route response/no-event evidence and one authorized Preview Sentry issue with owner-confirmed alert receipt. Configure a deliberate Preview verification alert as well as the Production owner alert; tag them clearly, do not confuse Preview noise with Production. Protected deployment probes must reach the application using the sanctioned bypass/header or owner-authenticated browser; a platform login response is not app-header evidence. Keep credentials out of URLs, logs, screenshots and tool output; the owner performs value-bearing probes where tools cannot do that safely. Save the filled Preview-template fields in this sprint record. Refresh them for any substantive candidate change.
   Reconcile ENV-VARS-SAFETY and error-tracking/SETUP-CHECKLIST to D-23: one project, distinct environments, names-only owner entry, Preview alert before merge, Production from main after merge, guarded route removed by S0.2. Never follow stale two-project, paste-DSN, generic user-email context, Preview-promotion or same-sprint-removal instructions over the project-specific roadmap. P10's publicly reachable Production launch half remains Pending while Production is intentionally protected; database/payment/auth proofs are not claimed here.
7. Exit gate before merge: review the full diff and allowed paths, run local checks, confirm Code Check/scan/audit success and Preview evidence at the candidate head, and fill the paired Codex brief from actual PR data. Compute immutable merge-base/head after metadata commits; hand final literal SHAs to a separate GPT-6 Astra Codex session (a commit cannot contain its own SHA). Resolve Blocking findings; Should-fix deferrals need recorded owner/reason. Substantive code/config/lockfile/runtime-document changes require fresh checks, Preview and review at the new head. Only the owner merges. Do not claim Production smoke or S0.1 Done before merge.
8. After verified owner merge, smoke-test the main-derived Production deployment on desktop/mobile, sales-closed/noindex state, actual six application headers and unauthorized diagnostic denial. Owner triggers one bounded authorized synthetic event and confirms the Production issue/alert with environment/release provenance. Exercise Vercel Instant Rollback between two harmless, eligible main-derived deployments once available (a same-source redeploy may supply the second deployment if supported); do not promote the sprint Preview to Production or bypass main-only deployment. Record from/to deployment IDs, commit SHAs, actor/time, result, restored active deployment and smoke results in ROLLBACK.md; host rollback does not restore database data. If an eligible rollback target or access is missing, keep the drill and sprint open rather than logging a paper exercise. Retain/record the intended reviewed deployment at the end.
   Complete the allowed state/roadmap/checklist updates from evidence only. Activate S0.2 only after every S0.1 gate passes; hand off removal of the test route/helper/tests and revocation of SENTRY_TEST_TOKEN (retaining normal Sentry coverage) as S0.2 work. Post-merge tracker changes follow §11's small docs branch with separate Git authorization. Setup Gate as a whole remains open until S0.2 closes.

## Per-step protocol
1. Inspect the named inputs and relevant existing implementation before editing.
2. Make the smallest safe change inside the allowed file list.
3. Run the exact applicable commands and sprint-specific checks below.
4. Review the diff for scope, regressions, live env files, and secret exposure without printing values.
5. Commit or push only when the Git action policy below explicitly says YES.
6. Return the report below, then stop for the owner's next instruction.

Remote commands: proceed · pause · status · fix [thing] · skip to [n].
A skip requires an owner-approved deferral to a named future sprint/backlog item. Do not mark the sprint
complete while an exit criterion remains unmet.

## Locked inputs
- Approved copy: docs/APPROVED-INPUTS.md §2 maps prototype pages/build sources; docs/content/locked-facts.md and docs/content/new-strings.md govern claims and technical strings. No product copy is ported this sprint; the holding state needs its own registered approval, not reuse of draft E10 wording.
- Approved design/mockups: docs/DESIGN.md and frozen prototype/ (public E8, final Admin E10); no UI redesign or asset/font extraction in S0.1.
- Architecture/schema/spec: docs/TECH-ARCHITECTURE.md, docs/TECHNICAL-INTEGRITY.md, docs/SECURITY-CHECKLIST.md, docs/ENVIRONMENT-PARITY.md, docs/ROADMAP.md S0.1 and PROJECT-STATUS.md D-01/D-03/D-23–D-26.

Do not invent copy, facts, design values, access rules, data or approvals. If inputs conflict, stop and add an
open decision to docs/PROJECT-STATUS.md only when that file is allowed. The documented D-23 reconciliations above are already assigned work, not unresolved choices.

## Sprint-specific rules
- Dependency setup is explicitly part of Steps 2–4: Next.js/React/React DOM, TypeScript and their type packages, Tailwind and its required PostCSS adapter, ESLint/Next configuration, Prettier, Vitest, @sentry/nextjs and server-only where required. Record exact choices and resolve their required peers; no unrelated SDK, component library or dependency. Verification does not authorize ad hoc installs to hide failures.
- No formatter/linter/generator may rewrite prototype/ or verbatim prompt/review records. Do not run prototype generation/freezing or import its workflows into root CI. A reduced checkout is not expected to pass the full asset baseline.
- Node/pnpm/package versions and supported SDK hook filenames are verified at execution. Do not silently substitute a different stack/Node major or retain deprecated generated commands.
- The temporary Sentry route is a protected diagnostic, not a public customer write; no new Supabase/Upstash/Turnstile integration is justified by it. Authorization, no-event denial, scrubbing and removal are required.
- Do not disable security headers or monitoring to make the Preview look green. Distinguish scaffold limitations from unfinished acceptance checks.
- S0.0's Commit/Push permission and D-26 command/hosting exemptions do not carry into S0.1. Remote delivery remains pending until the current sprint's Git policy permits it.

## Safety
- Never open, read, copy, print, or modify .env.local or another live-value env file.
- Use env names and placeholder-only examples; never hardcode or echo a secret.
- Preserve auth, data, routing, security, and hosting behavior unless this sprint explicitly changes it.
- Do not add dependencies or change unlisted files without owner approval.

## Verification
- Typecheck: pnpm typecheck
- Lint: pnpm lint
- Tests: pnpm test:unit and pnpm test (same hermetic suite at S0.1; both must pass with actual tests).
- Production build: pnpm build
- Formatting: pnpm format:check
- Critical production vulnerabilities: pnpm audit --prod --audit-level=critical
- CI install: pnpm install --frozen-lockfile (CI/clean builder environment after the planned setup; reviewer never installs).
- Manual/responsive/form/data checks: holding page at 320/768/1440, desktop/mobile keyboard and no-JS readability, no horizontal overflow or broken assets, no unexpected browser/CSP/runtime failures, noindex, no public sale/form surface; no database/provider write other than the bounded Sentry proof.
- Preview before review; Production after merge: curl.exe -I $deploymentUrl (PowerShell) or curl -I "$deploymentUrl" (POSIX), with the literal confirmed deployment URL assigned first. While protected, add the sanctioned x-vercel-protection-bypass header via a safe owner-operated/private runner; never paste its value into a command transcript. Save sanitized status/header evidence from the application response for all six headers, plus a nonexistent path to confirm catch-all behavior. Use GET/browser checks too; a successful HEAD alone does not prove the app renders.
- Diagnostic tests: mocked unit tests for valid/absent/empty/malformed/wrong Bearer token and missing configured secret; invalid methods never emit; authorized emission is synthetic and bounded; unauthorized deployed request must produce no test event. Owner-operated authorized Preview and Production probes record only environment/release, issue reference, time and actual email receipt, never credentials or event payloads containing them.
- Hosting/CI: exact Code Check context required, pending/failure prevents merge, final head green, secret scan redacted and blocking, Vercel build SHA matches review head, Production sourced from main, SALES_MODE off per owner scope inspection, P10/P12 accurately qualified.
- Rollback: actual executed drill and post-rollback smoke, not just written instructions.

From repo/ (path/status checks do not read live env files):
  git status --short --branch
  git diff --name-status
  git diff --cached --name-status
  git ls-files --others --exclude-standard
  git diff --check
  git diff --cached --check
  git check-ignore .env.local .env.production
  git ls-files -- .env.local '.env*' '**/.env*'
  git log --all --format=oneline -- .env.local
  git diff --exit-code -- prototype/
  git diff --cached --exit-code -- prototype/
Expected: live env paths ignored, only the inspected placeholder .env.example may be tracked, no prototype changes. Inspect untracked candidates explicitly; diff alone misses new files. Scanner output must be redacted before it reaches a log/transcript.

After authorized commits and refreshing the main reference, record:
  git rev-parse HEAD
  git merge-base origin/main HEAD
  git diff --name-status origin/main...HEAD
Bind actual CI, Preview and review to those immutable SHAs. Do not hardcode this preparation's main as the future merge base.

Do not guess commands or install dependencies to make a check run. Report any check that cannot run.

## Git action policy
- Commit: NO (default).
- Push: NO (default).

An omitted or unfilled field means NO. Never push to main or another branch, merge, force-push, reset user
work, or skip hooks.

## Report
1. Outcome and scope completed.
2. Files changed.
3. Commands/checks and exact results.
4. Manual or Preview verification.
5. Risks, open decisions, and follow-ups.
6. Branch and actual commit/push status; include SHA/message if committed, otherwise suggest a message.
7. Roadmap/status bookkeeping completed or still required.
~~~

Before merge: complete this sprint record, test Vercel Preview, and obtain independent review against immutable merge-base and head SHAs. Substantive changes after review require a refreshed Preview and re-review.

## F. Codex review brief

Use [S0.1-setup-scaffold-review.md](../code-reviews/S0.1-setup-scaffold-review.md), filled from the canonical review template. The reviewer checks the implemented PR in a separate session. Actual PR/range, checks and Preview evidence are pending; this preparation grants no verdict.

## G. Checklists

Follow `docs/WORKFLOW.md` §3–§8, `docs/QA-CHECKLIST.md`, `docs/SECURITY-CHECKLIST.md`, `docs/ENVIRONMENT-PARITY.md` §12 and `docs/templates/NEW-WEBSITE-SETUP-CHECKLIST.md`. S0.1 has no S0.0 command/Preview/Production exemption. Production-only evidence necessarily follows the owner's merge.

## Outcome and execution record

*The five preparation-time bullets below are kept as history; the execution record follows them.*

- Preparation: prompt and paired review brief written; S0.0 carry-forwards, template structure, commands and paths checked. Implementation remains Not Started.
- Implementation branch / PR / merge-base / head: PENDING — branch name above is planned, not created by preparation.
- Typecheck / lint / format / unit tests / build / audit / Code Check / secret scan: PENDING — no root scaffold yet; none claimed as run.
- Preview / six deployed headers / P10 / P12 / Preview Sentry alert: PENDING.
- Independent implementation review: PENDING — supply the immutable implemented range and tested Preview first.
- Owner merge / Production smoke and alert / actual rollback drill: PENDING.
- Follow-up: remove the diagnostic route and revoke its token in S0.2; integration/Playwright harness in S0.2; commercial-use hosting plan decision before S1.3; tokens/fonts and their existing approvals in S1.3.

Append the exact executed results and deviations here. Retain the prompt used and each review's provenance; never overwrite history with a later success claim.

## Execution record — Claude Code, 2026-09-24

Section E above is preserved verbatim as the executed prompt. This record states what actually happened; nothing here is an independent review verdict. Git policy for this run: **Commit NO, Push NO** (the prompt's default; no other authorisation was given), so everything below is an uncommitted working tree on the local branch, and Steps 5–8 are prepared, not performed.

*History — superseded the same day: the owner then authorised Commit YES / Push YES for this branch ("Owner actions received"), and Steps 5–6 were performed ("Remote evidence", "Preview record"); Steps 7–8 remain.*

### Step 1 — state, branch and choices

- `git fetch origin`: local `main` = `origin/main` = `ce41b346e2db8f3d750be0dc39d054df7a0091f9`; the remote had no `claude/s0.1-setup-scaffold`. The branch was created locally from that `main` (`git switch -c`), carrying the prepared, uncommitted S0.1 records (`PROJECT-STATUS.md`, `ROADMAP.md`, `docs/sprint-prompts/README.md`, this record, the review brief). No root manifest, lockfile, `src/`, workflow or env file existed.
- Tools: Node v24.15.0, pnpm 11.3.0 (global), npm 11.12.1, Corepack 0.34.6, Git 2.54.0.windows.1, gh 2.98.0 (logged in as `86400studio`), agent-browser 0.33.0. The Playwright MCP server did not connect in this session (connection timeout), so browser checks used agent-browser. gitleaks is not installed; the official 8.30.1 release binary was downloaded to the session scratch directory (outside the repository) and its SHA-256 checked against the release checksums file.
- Plan: **one PR** (no D-25 split), minimal `src/` layout, **Node 24**. Versions were checked on 2026-09-24 against the npm registry and official documentation (nextjs.org, docs.sentry.io, vercel.com/docs, pnpm.io, GitHub release APIs) by five research agents, each re-checked by an independent verifier:

| Package | Pinned | Why this exact version |
|---|---|---|
| next / eslint-config-next | 16.3.6 | Current stable (2026-09-22); fixes the critical `next/og` advisory GHSA-vcvr-r3jv-pc5j |
| react / react-dom | 19.2.8 | The version `create-next-app@16.3.6` pins for Next 16.3 (npm `latest` is 19.3.0) |
| typescript | 5.9.3 | Generator's `^5`; TypeScript 7 is outside the typescript-eslint peer range (`<6.1.0`) |
| @types/node / @types/react / @types/react-dom | 24.13.6 / 19.2.18 / 19.2.7 | Node 24 types; React 19.2 types |
| tailwindcss / @tailwindcss/postcss | 4.3.3 | Current stable; generator's `^4` |
| eslint | 9.39.5 | Generator's `^9`. npm marks ESLint 9 end-of-life, but `eslint-config-next` 16.3.6 bundles eslint-plugin-react / -import / -jsx-a11y whose peers stop at ESLint 9 — follow-up |
| prettier | 3.9.9 | Current stable, default config |
| vitest / vite | 5.0.1 / 8.3.0 | Current stable; Vitest 5 needs `vite` as a peer |
| @sentry/nextjs | 10.75.2 | See deviation 2. 10.75.2 rather than 10.75.3 because the latter was under a day old (pnpm's supply-chain release-age window) |
| server-only | 0.0.1 | Marks the diagnostic guard server-only |
| pnpm (`packageManager`) | 10.34.5 | See deviation 1 |

Security and environment plan explained before any edit (session message): headers on a catch-all rule with the CSP limited to the Next runtime and the DSN's ingest origin, the `'unsafe-inline'` trade-off stated, Sentry scrubbing and `VERCEL_ENV` environments, the Bearer-guarded route with uniform denials, and the new env names.

### Step 2 — scaffold

- `pnpm create next-app@16.3.6 app --ts --tailwind --eslint --app --src-dir --import-alias "@/*" --use-pnpm --empty --disable-git --skip-install --yes --no-agents-md --no-react-compiler`, run in the session scratch directory (the generator refuses a non-empty folder and would otherwise write its own `AGENTS.md` / `CLAUDE.md`). Only the allowed files were brought over and adapted: `tsconfig.json` (template + `prototype` excluded), `eslint.config.mjs` (template + `prototype/**`, `coverage/**` ignored), `postcss.config.mjs` (template), `src/app/layout.tsx` / `page.tsx`, `globals.css` moved to `src/styles/`. Not copied: the generator's README, `.gitignore`, `next-env.d.ts` (generated; now ignored like the template does), `pnpm-workspace.yaml` (see deviation 1) and its sample layout (Geist fonts, "Create Next App" metadata). The empty template ships no promotional page, image or font.
- Scripts: `typecheck` = `tsc --noEmit`; `lint` = `eslint --max-warnings=0 .`; `format` / `format:check` = Prettier; `test:unit` = `vitest run` (`tests/unit/**/*.test.ts`, Node environment, `passWithNoTests: false`); `test` = `pnpm run test:unit`; `dev`, `build`, `start`. No `test:e2e` stub.
- `.prettierrc.json` = `{}` (default). `.prettierignore`: `prototype/`, `docs/` (with `docs/sprint-prompts/` and `docs/code-reviews/` named explicitly), `README.md`, `CLAUDE.md`, `AGENTS.md`, `.claude/`, build/cache output, `next-env.d.ts`, `pnpm-lock.yaml`. Only new implementation/config files were formatted.
- `.gitignore`: one line added (`next-env.d.ts`); every D-07 rule unchanged.

### Step 3 — holding page, headers, Sentry and the diagnostic route

- **Holding page** (`/`): heading "Aalishaan Studio", one line "This website is being built and is not taking orders yet." (NS-17, Proposed — needs the delivery lead's D-24 approval before Production); static, semantic, no link, form, price or payment surface; documented values only (white, `--ink`, the documented fallback font stacks, the Body and Display / h1 sizes); `noindex, nofollow` metadata; `robots.txt` allows crawling on purpose so crawlers can read the noindex (see the pre-review below). **Error boundary** `src/app/global-error.tsx`: reports the error to Sentry and shows the approved prototype 500 copy verbatim (NS-18, rule 7) with a "Return home" link — a plain anchor, so it does a full page load (a same-path client navigation cannot clear the root error boundary); its one-line `eslint-disable-next-line @next/next/no-html-link-for-pages` carries that reason.
- **Headers** (`src/lib/security-headers.ts`, applied by `next.config.ts` on source `/(.*)`): the exact policy is in `TECH-ARCHITECTURE.md` §9. Found while testing: `/:path*` does **not** match `/` in Next's own header matcher (and, per the verified research, not in Vercel's route conversion either), which would have left the holding page itself without headers — hence `/(.*)`. `img-src` was narrowed to `'self'` after the browser check showed no image or `data:` use. `X-Robots-Tag: noindex, nofollow` is added as a seventh header for the closed state; `X-Powered-By` is off.
- **Sentry** (`@sentry/nextjs` 10.75.2): `src/instrumentation.ts` initialises the server and edge runtimes from one options object inside `register()` and exports `onRequestError`; `src/instrumentation-client.ts` initialises the browser and exports `onRouterTransitionStart`; `sentry.server.config.ts` / `sentry.edge.config.ts` are therefore not created (the prompt allows omitting unused hook files; one options object avoids two diverging copies). Environment = `VERCEL_ENV` (`development` / `preview` / `production`; server at runtime, browser inlined at build as `NEXT_PUBLIC_SENTRY_ENVIRONMENT` by `next.config.ts`); release = `VERCEL_GIT_COMMIT_SHA` (the installed SDK injects `release.name` even when no release is created); `sendDefaultPii: false`; no replay or tracing sample rate; `beforeSend` drops `user`, cookies, bodies, query strings and every request header except `user-agent`, and `beforeBreadcrumb` strips query strings. `withSentryConfig` (imported from `@sentry/nextjs/config`) uploads source maps only when `SENTRY_AUTH_TOKEN` is present, deletes them after upload, and has `telemetry: false`. `agentRules: false` stops `next dev` writing its agent block into the governing `AGENTS.md` / `CLAUDE.md`.
- **Diagnostic route** `POST /api/sentry-test` (`src/app/api/sentry-test/route.ts` → `src/lib/server/sentry-test.ts`, `import "server-only"`): exact `Authorization: Bearer <token>` (RFC 6750 characters, one space, exact scheme) compared with `SENTRY_TEST_TOKEN` through SHA-256 digests and `timingSafeEqual`; an unset, empty or shorter-than-32-character configured token disables it; every denial is the same `404 {"status":"not_found"}` and emits nothing; the body and query are never read; the event is a fixed `Error` with tag `diagnostic` and fingerprint `[s0.1-sentry-test, environment, release]` (so each Preview head and Production raise a *new* issue — a shared fingerprint would stop a "new issue" alert from firing for Production after the Preview test); `Sentry.flush(2000)`; `200 {"status":"sent"}` only when the flush confirms, `503` otherwise or when Sentry is not configured; `Cache-Control: no-store`; only `POST` is exported.
- **Tests** (51): `tests/unit/sentry-test.test.ts` runs the real route and guard with only the SDK mocked — the allowed case, body/query injection ignored, unconfirmed flush, unconfigured SDK, 13 malformed/wrong credential forms, query-string token, unset / empty / short configured token, no echo of either token, other methods (405) and the route's single export; `tests/unit/security-headers.test.ts` loads the real `next.config.ts` and applies Next's own header matcher (`getPathMatch`, strict) to `/`, API, static, `robots.txt`, favicon and missing paths, checks the six headers plus `X-Robots-Tag`, the environment derivation from `VERCEL_ENV` whatever `NODE_ENV` says, and CSP properties (no `unsafe-eval` in production, no wildcard/scheme sources, only the DSN origin, never the DSN key). The first run caught two real problems, both fixed: the `/:path*` root gap, and a test that assumed a trailing space reaches the handler (the Fetch `Headers` API trims it; replaced by a tab case).

### Step 4 — Code Check, secret scan, Dependabot

- `.github/workflows/code-check.yml`: `pull_request` (every PR, no path filter), `permissions: contents: read`, job `code-check` **named "Code Check"** (the ruleset context), `ubuntu-24.04`, 15 min, `NEXT_TELEMETRY_DISABLED`. Steps: checkout `actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1` (v7.0.1, `fetch-depth: 0`, `persist-credentials: false`) → gitleaks 8.30.1 linux x64, SHA-256 `551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb` checked, `GIT_ATTR_SOURCE=<git's empty tree> gitleaks git --redact --no-banner --ignore-gitleaks-allow --exit-code 1 --log-opts="--all --full-history --diff-filter=tuxdb -m" .` (every commit of every fetched branch, merge commits included; no committed `.gitattributes` can hide content, and the step first proves the runner's Git honours `GIT_ATTR_SOURCE`; the job fails if a `.gitleaks.toml` or `.gitleaksignore` appears) → `pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413` (v6.1.0, version from `packageManager`) → `actions/setup-node@820762786026740c76f36085b0efc47a31fe5020` (v7.0.0, `node-version-file: .nvmrc`, `cache: pnpm`) → `pnpm install --frozen-lockfile` → typecheck → lint → format:check → test:unit → build → `pnpm audit --prod --audit-level=critical`. No `continue-on-error`, `--if-present`, `--passWithNoTests` or `pull_request_target`; PR checks need no secret and contact no provider (Next and Sentry build telemetry off; no upload without `SENTRY_AUTH_TOKEN`).
- `.github/dependabot.yml`: `npm` ecosystem (pnpm lockfile v9 supported) at `/` with `exclude-paths: prototype/**`, weekly, minor+patch grouped; `github-actions` at `/`, weekly.
- `.env.example`: **not created.** The operator's user-level Claude Code settings deny the agent `Read(./.env.*)`, which also covers `.env.example`; writing it through the shell would have bypassed that setting, so it was not done. The prepared names-only content (every S0.1 name blank, future integrations listed as not configured) is below under "Owner setup" and must be added before the commit.

### Checks run (exact results, 2026-09-24, local, uncommitted tree)

| Command | Result |
|---|---|
| `pnpm install --frozen-lockfile` | exit 0 (pnpm 10.34.5; lockfile v9.0) |
| `pnpm typecheck` | exit 0 |
| `pnpm lint` | exit 0 — 0 problems (`--max-warnings=0`) |
| `pnpm format:check` | exit 0 — "All matched files use Prettier code style!" |
| `pnpm test:unit` / `pnpm test` | exit 0 — 2 files, 51 tests passed (each; 50 before the pre-review fixes) |
| Empty-suite proof: `vitest run tests/unit/__no_such_file__` | exit 1 — "No test files found, exiting with code 1" |
| `pnpm build` | exit 0 — `○ /`, `○ /_not-found`, `ƒ /api/sentry-test`, `○ /robots.txt`; no `.map` under `.next/static` |
| `pnpm audit --prod --audit-level=critical` | exit 0 — "No known vulnerabilities found" |
| gitleaks 8.30.1 over the history (13 commits): first `git --redact --log-opts=--all`, then the hardened Code Check command (`GIT_ATTR_SOURCE` + `-m`, ~13.4 MB scanned) | "no leaks found", exit 0 (both) |
| gitleaks 8.30.1 `dir --redact` over every new/changed path (`src`, `tests`, `.github`, configs, lockfile, `docs`) | exit 0 for each |
| `git status --short --branch` / `git diff --name-status` / `git ls-files --others --exclude-standard` | branch `claude/s0.1-setup-scaffold`; changed and new paths exactly as listed under "Files changed"; nothing staged |
| `git diff --check` / `git diff --cached --check` | clean |
| `git check-ignore .env.local .env.production` | both ignored (`.gitignore:7`) |
| `git ls-files -- .env.local '.env*' '**/.env*'` / `git log --all --format=oneline -- .env.local` | empty / empty |
| `git diff --exit-code -- prototype/` (and `--cached`) | no difference; no untracked file under `prototype/` |

### Local runtime verification (production build, `pnpm start` on localhost)

- `curl -D -` on `/` (200), `/does-not-exist` (404), `/deeply/nested/missing` (404), `/robots.txt` (200), a `/_next/static/chunks/*.js` file (200) and `GET /api/sentry-test` (405): **all six headers plus `X-Robots-Tag` on every response**; no `X-Powered-By`.
- Diagnostic route with a throwaway local token (generated in the shell, never printed; not found in the server log afterwards): no header → 404; wrong bearer → 404; token in the query → 404; lower-case scheme → 404; correct token without a DSN → `503 {"status":"sentry_not_configured"}` (no false success); `PUT` / `DELETE` → 405; `HEAD` → 405; `OPTIONS` → 204; `Cache-Control: no-store`.
- Browser (agent-browser, Chromium) at 1440, 768 and 320 px: hydrated under the enforced CSP (`window.next.router` present, Next 16.3.6), Sentry carrier present, **no console messages, no page errors, no CSP violations**, no horizontal overflow (scroll width = viewport width), heading on two lines at 320 px, body 17 px → 16 px below 48 rem as documented, no links or form controls; the only failed request is the browser's automatic `/favicon.ico` probe (404, not logged to the console). Screenshots are in the session scratch directory, not committed.
- No-JavaScript: every `<script>` removed from the server HTML still leaves "Aalishaan Studio This website is being built and is not taking orders yet.", one stylesheet link, `lang="en"`, the viewport and `robots` metas. (A browser run with scripts blocked could not be completed: agent-browser hung on new sessions after the first — recorded as a tool limitation.)
- `next dev`: `script-src` includes `'unsafe-eval'` only there; `AGENTS.md` and `CLAUDE.md` unchanged afterwards. The browser console check in dev mode could not complete for the same agent-browser reason.
- The Vercel Preview, the deployed headers and the Sentry events are **not** verified — Pending (Steps 5–6). *(Done later the same day — "Preview record".)*

### Internal pre-review (before any handoff; not the independent Codex review)

After Steps 1–4 a separate six-lens review (route guard, headers/CSP, Sentry privacy, CI/supply chain, scope, build/runtime) read the uncommitted tree and the installed package sources; each finding was then checked by two skeptics told to refute it. 8 findings; dispositions:

| # | Finding | Verdict | Disposition |
|---|---|---|---|
| 1 | Server scrubber left the query string in `contexts.nextjs.request_path`, which `onRequestError` fills from the raw request URL | Confirmed (both skeptics); a second report of it was refuted only because the fix had landed | Fixed: `scrubEvent` strips it (`src/instrumentation.ts`) |
| 2 | `robots.txt` `Disallow: /` stops crawlers from ever reading the noindex meta and header (Google's documented behaviour) | Confirmed (two lenses, all four skeptics) | Fixed: `robots.txt` allows crawling; metadata + `X-Robots-Tag` do the noindex; a unit test keeps robots.txt from disallowing `/` while the page is noindex |
| 3 | Outgoing-request breadcrumbs keep the query and fragment in `http.query` / `http.fragment`, which the scrubber did not remove | Confirmed (both; one reproduced it with the SDK) — latent, no outgoing request exists yet | Fixed in both scrubbers |
| 4 | gitleaks could be blinded by a committed `.gitattributes` (`-diff` / `binary`) and never saw merge-commit diffs | Confirmed (both reproduced it) | Fixed: `GIT_ATTR_SOURCE` = empty tree, `--log-opts="--all --full-history --diff-filter=tuxdb -m"`, plus a runner self-check. Re-proved locally with generated fake tokens in scratch repositories: the old command missed both cases (exit 0), the new one caught both (exit 1); the real history still passes |
| 5 | The error fallback's "Return home" `<Link>` does nothing on `/` (same-path soft navigation keeps the root error boundary) | Confirmed by one skeptic from the Next 16.3.6 source, refuted by the other as unlikely in practice | Fixed (low cost): plain anchor with a reasoned one-line lint exception, declared in the PR description |
| 6 | The CSP blocks the Vercel Toolbar on Preview | Refuted (both): intended, and already an owner setting below | No code change |

After the fixes every check was re-run from a clean build (table above) and the production server probed again: seven headers on `/`, a 404, `robots.txt`, a static chunk and the API route; `robots.txt` = `User-Agent: *` / `Allow: /`; `img-src 'self'`; unauthorised POST 404, authorised without a DSN 503.

### Files changed

New: `package.json`, `pnpm-lock.yaml`, `.nvmrc`, `tsconfig.json`, `next.config.ts`, `postcss.config.mjs`, `eslint.config.mjs`, `.prettierrc.json`, `.prettierignore`, `vitest.config.ts`, `src/app/layout.tsx`, `src/app/page.tsx`, `src/app/robots.ts`, `src/app/global-error.tsx`, `src/app/api/sentry-test/route.ts`, `src/lib/server/sentry-test.ts`, `src/lib/security-headers.ts`, `src/styles/globals.css`, `src/instrumentation.ts`, `src/instrumentation-client.ts`, `tests/unit/sentry-test.test.ts`, `tests/unit/security-headers.test.ts`, `.github/workflows/code-check.yml`, `.github/dependabot.yml`, plus the prepared `docs/code-reviews/S0.1-setup-scaffold-review.md` and this record. Changed: `.gitignore`, `README.md`, `docs/TECH-ARCHITECTURE.md`, `docs/ENVIRONMENT-PARITY.md`, `docs/ENV-VARS-SAFETY.md`, `docs/error-tracking/SETUP-CHECKLIST.md`, `docs/TECHNICAL-INTEGRITY.md`, `docs/ROLLBACK.md`, `docs/templates/NEW-WEBSITE-SETUP-CHECKLIST.md`, `docs/content/new-strings.md`, `docs/PROJECT-STATUS.md`, `docs/ROADMAP.md`, and `docs/sprint-prompts/README.md` (changed by the prompt preparation and carried over, not by this run). Pending: `.env.example` (owner). Not changed: `prototype/`, `CLAUDE.md`, `AGENTS.md`, `docs/HANDOFF.md` (nothing about the Vercel or Sentry project is evidenced yet). *Later, documentation only: `.env.example` was added by the owner and committed in `0d27806`; `3f07d66` changed `docs/PROJECT-STATUS.md` and this record; the final records commit changes `docs/HANDOFF.md` §1 (the Vercel and Sentry project rows, now evidenced), `docs/ENVIRONMENT-PARITY.md`, `docs/PROJECT-STATUS.md`, `docs/ROADMAP.md`, `docs/TECHNICAL-INTEGRITY.md`, `docs/error-tracking/SETUP-CHECKLIST.md`, `docs/templates/NEW-WEBSITE-SETUP-CHECKLIST.md`, the review brief and this record — 41 paths over the whole range.*

### Deviations (each within the prompt's allowances unless stated)

1. **pnpm 10.34.5, not the newest pnpm.** Vercel's documentation lists pnpm support up to 10; "pnpm 11 and 12 not supported" is an open Vercel issue. Pinning 10.34.5 keeps local, Code Check and Vercel on one documented version. It also avoids a file outside the allowed list: pnpm 11+ reads build-script decisions only from `pnpm-workspace.yaml` and fails a frozen install without them (`ERR_PNPM_IGNORED_BUILDS`, observed in a scratch probe), while pnpm 10 reads `pnpm.ignoredBuiltDependencies` from `package.json`. pnpm 10 has no default release-age delay; Dependabot's default three-day cooldown applies to its update PRs.
2. **`@sentry/nextjs` 10.75.2, not 11.0.0.** 11.0.0 was released on 2026-09-23, one day before this run, and removes `sendDefaultPii` in favour of `dataCollection` options that collect user data, cookies, headers, bodies and query strings **by default**. For a privacy-sensitive first setup the mature v10 line with `sendDefaultPii: false` and explicit scrubbing was chosen; the v11 upgrade (with an explicit `dataCollection` configuration) is a follow-up through a reviewed Dependabot PR.
3. **ESLint 9.39.5** although npm marks ESLint 9 end-of-life — see the version table; development tool only (`audit --prod` does not cover it).
4. **No `sentry.server.config.ts` / `sentry.edge.config.ts`:** server and edge are initialised in `register()`.
5. **`next-env.d.ts` is generated and ignored**, as in the generator's template; `tsc --noEmit` does not need it.
6. **Code Check reads `.nvmrc`** instead of repeating `node-version: 24`; `TECHNICAL-INTEGRITY.md` updated.
7. **`.env.example` not created** — permission setting (Step 4).
8. **A seventh header (`X-Robots-Tag`)** for the closed state, and `agentRules: false` in `next.config.ts`.
9. **CSP `script-src 'unsafe-inline'`** (reported trade-off, `TECH-ARCHITECTURE.md` §9). A nonce needs a `proxy.ts` (outside the allowed list) and forces dynamic rendering of every page.

### Owner setup — names, scopes and settings (values never pass through Claude)

**GitHub** (after the first Code Check run on the S0.1 PR): Settings → Rules → Rulesets → "main protection" → **Require status checks to pass** → add **Code Check** → save. Keep: PR required, block force pushes, restrict deletions, bypass list empty.

**Vercel** (owner's account, Hobby):
- Add New → Project → import `86400studio/aalishaan-studio-app`; project name `aalishaan-studio-app`; Framework Next.js; **Root Directory: empty (repository root)**; build and install commands: defaults — do not override the install command (Vercel then runs pnpm 10 from the lockfile and `packageManager`); Node.js 24.x (from `engines`). Production branch: `main` (Settings → Environments → Production → Branch Tracking). The first deployment Vercel makes is of the current `main`, which has no app yet — expect it to fail or show nothing; it is not a rollback target.
- Settings → **Deployment Protection** → Vercel Authentication → **All Deployments** (included on Hobby since 2026-09-09; a new project starts on Standard, which leaves production domains public). Same page → **Protection Bypass for Automation** → create a secret; it is used only for header probes you run (never pasted into a chat, URL or screenshot).
- Settings → Environment Variables (names only here; tick exactly these scopes):

| Name | Development | Preview | Production | Notes |
|---|---|---|---|---|
| `SALES_MODE` | `off` | `off` | `off` | Plain value `off` |
| `NEXT_PUBLIC_SENTRY_DSN` | optional | ✓ | ✓ | The project's DSN (public) |
| `SENTRY_DSN` | optional | ✓ | ✓ | Same DSN |
| `SENTRY_AUTH_TOKEN` | — | ✓ | ✓ | Sentry organisation token with source-map upload rights; mark Sensitive / Secret |
| `SENTRY_ORG` | — | ✓ | ✓ | Organisation slug |
| `SENTRY_PROJECT` | — | ✓ | ✓ | Project slug |
| `SENTRY_TEST_TOKEN` | — | ✓ (value A) | ✓ (value B ≠ A) | At least 32 random characters each (for example `openssl rand -hex 32`); Sensitive / Secret; revoked in S0.2 |

- If the Vercel Toolbar shows on the Preview, it loads scripts from `vercel.live`, which this CSP blocks by design (console noise from the platform, not the app); switch the toolbar off for the project or note it separately in the Preview evidence.

**Sentry** (owner's account): one Next.js project; alert rules "Preview verification — S0.1" (new issue, environment `preview`, e-mail you) and "Production" (new issue, environment `production`, e-mail you); Project Settings → Security & Privacy → **Prevent Storing of IP Addresses** on (defence in depth). Full steps: `docs/error-tracking/SETUP-CHECKLIST.md`.

**`.env.example`** — create it at the repository root with exactly this content (names only; every value blank):

```text
# Aalishaan Studio — environment variable NAMES only. Never put a real value in this file.
# The owner enters values in Vercel (Development / Preview / Production) or in a local .env.local, which Git
# ignores. Authoritative list: docs/TECH-ARCHITECTURE.md §6; per-environment values: docs/ENVIRONMENT-PARITY.md §4.

# --- Used from S0.1 ---------------------------------------------------------------------------------------

# Server-only. off | test | live. "off" in every scope until S3.4.
SALES_MODE=

# Public (inlined into the browser bundle). Sentry DSN of the one Sentry project; empty = monitoring off.
NEXT_PUBLIC_SENTRY_DSN=
# Server-only. The same DSN for server events; falls back to NEXT_PUBLIC_SENTRY_DSN when empty.
SENTRY_DSN=
# Build-only (Vercel Preview and Production builds). Source-map upload; never needed by local or CI checks.
SENTRY_AUTH_TOKEN=
# Build-only, not secret. Sentry organisation and project slugs for the source-map upload.
SENTRY_ORG=
SENTRY_PROJECT=
# Server-only, TEMPORARY (removed and revoked in S0.2). Bearer token for POST /api/sentry-test: at least 32
# random characters, a different value in each scope; empty = the diagnostic route is disabled.
SENTRY_TEST_TOKEN=

# --- Not configured yet: later sprints add these. The S0.1 scaffold never reads them. -----------------------

# Public. Canonical site origin (D-04 domain in Production).
NEXT_PUBLIC_SITE_URL=
# Supabase — S0.2 (TEST project outside Production).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
# Razorpay — S1.6 / S1.7 (test mode outside Production).
NEXT_PUBLIC_RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=
# Shiprocket — S2.9 / S2.10.
SHIPROCKET_API_EMAIL=
SHIPROCKET_API_PASSWORD=
SHIPROCKET_WEBHOOK_TOKEN=
# Transactional email — S1.8 (names become provider-specific once D-06 is decided).
EMAIL_API_KEY=
EMAIL_FROM_ADDRESS=
# Cloudflare Turnstile — S1.6.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=
# Upstash rate limiting — S1.2 / S1.6.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# Worker route authentication — S1.8.
CRON_SECRET=
# Test harness — S0.2 (GitHub Actions secrets / local shell only, never Vercel runtime).
PLAYWRIGHT_BASE_URL=
VERCEL_AUTOMATION_BYPASS_SECRET=
```

### Pending remote evidence (Steps 5–8 — as planned before the push)

*History — items 1–3 are done since ("Remote evidence", "Preview record"); items 4–5 remain. The Preview template fields below are filled in "Preview record" (tester: the owner, recorded by Claude Code).*

1. Commit and push `claude/s0.1-setup-scaffold` (needs Commit YES / Push YES); open the PR against `main` with the description below.
2. First Code Check run on the PR → owner adds the exact "Code Check" context to the ruleset → a later authorised commit on the same PR shows the merge blocked while the check is pending and eligible only after success (run and head links, rule read-back via `gh api`).
3. Vercel Preview of the candidate head: ENVIRONMENT-PARITY §12 preflight; protected response without bypass; with the owner-run bypass, the six headers on `/` and on a missing path; desktop and 390/320 px mobile; console clean; noindex; unauthorised `POST /api/sentry-test` → 404 with no Sentry event; one authorised Preview event (environment `preview`, release = head SHA) and the owner's Preview alert e-mail; P10 and P12 records; the filled Preview template fields below.
4. Final immutable merge-base and head; the Codex brief (`docs/code-reviews/S0.1-setup-scaffold-review.md`) filled from actual PR data; GPT-6 Astra Codex review; fixes and repeat evidence when needed; the owner merges.
5. Production (from `main`): smoke on desktop and mobile, sales closed, noindex, six headers through the bypass, unauthorised route denied; one authorised Production event and alert; the Instant Rollback drill (`docs/ROLLBACK.md` → "Drill record"); tracker closure on a small docs branch (§11); S0.2 activated only after every S0.1 gate passes.

**Preview template fields** (to fill at Step 6): PR — pending · Branch `claude/s0.1-setup-scaffold` · Provider Vercel Preview · Preview URL — pending · Tested head SHA — pending · Date — pending · Tester — Claude Code with the owner-run bypass probes · Environment proof — ENVIRONMENT-PARITY §12 P10, P12 · Rows: `/` holding page, a missing path (404), `robots.txt`, `POST /api/sentry-test` denied and authorised, console, noindex — each desktop and mobile.

### Prepared PR description (for when the push is authorised)

> **S0.1 — Scaffold, Code Check, security headers, Vercel Preview & Sentry**
>
> **What.** The empty, strictly checked Next.js app: Next 16.3.6 App Router, strict TypeScript, Tailwind 4, ESLint 9, Prettier, Vitest; pnpm 10.34.5 and Node 24 pinned; a noindex holding page with no sale, form or payment surface; six security headers on every response; Sentry (one project, `development` / `preview` / `production` from `VERCEL_ENV`, scrubbed, releases = the deployed commit) with a temporary Bearer-guarded `POST /api/sentry-test`; the required Code Check with a blocking full-history gitleaks scan; Dependabot.
>
> **Why.** Sprint S0.1 — `docs/sprint-prompts/S0.1-setup-scaffold.md`.
>
> **Changes.** See the record's "Files changed". Env names added (values set by the owner in Vercel): `SENTRY_ORG`, `SENTRY_PROJECT` (build-only), `SENTRY_TEST_TOKEN` (server-only, temporary); already registered: `SALES_MODE`, `NEXT_PUBLIC_SENTRY_DSN`, `SENTRY_DSN`, `SENTRY_AUTH_TOKEN`.
>
> **Out of scope.** Supabase, integration/Playwright harness and `/api/health` (S0.2); schema (S1.1); auth (S1.2); storefront, tokens and fonts (S1.3); Razorpay and its CSP (S1.6); domain and sales opening (S3.x); `prototype/` untouched.
>
> **How tested.** Local: `pnpm typecheck` · `pnpm lint` · `pnpm format:check` · `pnpm test:unit` (51) · `pnpm test` · `pnpm build` · `pnpm audit --prod --audit-level=critical` — all pass; production server header and route probes; browser at 1440/768/320. Code Check run: link added at PR time. Preview: record link and tested head SHA added at Step 6.
>
> **Suppressions.** One: `src/app/global-error.tsx` — `eslint-disable-next-line @next/next/no-html-link-for-pages`, because the fallback's "Return home" must be a full page load (a same-path client navigation cannot clear the root error boundary). No `@ts-ignore` or `any`.
>
> **Rollback.** Revert PR + redeploy, or Vercel Instant Rollback; no database exists.
>
> 🤖 Generated with [Claude Code](https://claude.com/claude-code)

Suggested commit message (first commit, when authorised):

```text
feat(s0.1): scaffold Next.js app with Code Check, security headers and Sentry

Next 16.3.6 App Router, strict TypeScript, Tailwind 4, ESLint, Prettier and Vitest on
pnpm 10.34.5 / Node 24; noindex holding page; six security headers on every path;
Sentry with scrubbing and VERCEL_ENV environments plus a temporary Bearer-guarded
POST /api/sentry-test (removed in S0.2); Code Check workflow with a blocking
full-history gitleaks scan; Dependabot; S0.1 documentation reconciled to D-23.

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
```

### Owner actions received (2026-09-24, same session)

- `.env.example` added by the owner at the repository root (checked by name and size only — 2,289 bytes, identical in size to the prepared names-only content; the agent does not open `.env*` files); untracked, not ignored.
- NS-17 approved by the delivery lead ("Approved") — recorded in `docs/content/new-strings.md` §4 (technical-state lane, D-24 rule 1; owner review at the next check-in).
- Sentry (owner, screenshots shared in the session): project `aalishaan-studio-app`; alert rules **"Preview verification — S0.1"** and **"Production"** — WHEN a new issue is created, IF the event's `environment` attribute equals `preview` / `production` (the IF block, because the Filter Issues list only offers environments that have already sent events), THEN notify Member `86400studio`; "Send Test Notification" fired. IP storage prevention: owner step B6 reported done (not separately evidenced).
- Vercel (owner, screenshots shared in the session; names and scopes only, values never passed to Claude): `SALES_MODE=off` (Config) in Development, Preview and Production; `NEXT_PUBLIC_SENTRY_DSN`, `SENTRY_DSN`, `SENTRY_ORG`, `SENTRY_PROJECT` (Config) in Preview and Production; `SENTRY_AUTH_TOKEN` (Secret) and `SENTRY_TEST_TOKEN` (Secret) — each as separate Preview and Production entries; Vercel Toolbar Off for pre-production and production deployments; Protection Bypass for Automation secret created and kept by the owner. Project import reported done by the owner and evidenced by the PR #3 deployments; system environment variables evidenced by the full-list screenshot below; the **All Deployments** protection scope is evidenced by the owner's screenshot of Settings → Deployment Protection (below) — a Preview answers `302` under Standard Protection too, so the Preview alone could not show it.
- Vercel complete variable list (owner screenshot, later the same day): exactly nine Project entries as above — no other provider value, no branch-specific override, no manually created `VERCEL_AUTOMATION_BYPASS_SECRET` variable (the bypass secret is Vercel's own) — and "Enable access to System Environment Variables" ticked. P12 recorded PASS.
- Vercel Settings → Deployment Protection (owner screenshot, later the same day): **Vercel Authentication — Require Log In: on, scope "All Deployments"**; Password Protection off (a Pro feature); Trusted IPs off (Enterprise); Protection Bypass for Automation — one secret, designated the `VERCEL_AUTOMATION_BYPASS_SECRET` system environment variable; OPTIONS Allowlist off; **Protected Sourcemaps: Enabled** (source maps only for authenticated team members); Trusted Sources shows the project's own default OIDC entry only.
- `SENTRY_TEST_TOKEN`: the owner states that the Preview and Production values are different (generated in two separate runs; values never shown).
- Vercel build log of the tested Preview (`3f07d66`, owner paste): `[@sentry/nextjs - After Production Compile] Info: Successfully uploaded source maps to Sentry`, `✓ Completed runAfterProductionCompile in 7.8s`, the same route table as locally, "Build Completed … Deployment completed".
- **Git authorisation (owner, 2026-09-24): "Commit YES, Push YES for claude/s0.1-setup-scaffold"** — this branch only; never `main`, no merge, no force-push.
- Before the first commit: `.env.example` compared by Git object hash with the prepared names-only file — identical (no content displayed).
- Pre-commit re-verification from a clean build: every command passed except one `pnpm test:unit` run, where the first test of `tests/unit/security-headers.test.ts` hit Vitest's 5 s default while cold-importing `next.config.ts` (it loads the Sentry build tooling) — a flake that a fresh CI runner could hit. That `describe` block now has a bounded 30 s limit; three cold runs (Vitest caches cleared) then passed 51/51, the test taking about 1 s. Typecheck, lint, format, build and audit re-passed.

### Remote evidence (Step 5 onward)

- **Commit and push** (authorised, see above): explicit file list staged (40 paths, nothing else; no `prototype/` change); commit `0d27806c3cc08f9b5c57cb1640fd836d4663052c` "feat(s0.1): scaffold Next.js app with Code Check, security headers and Sentry" on base `ce41b346e2db8f3d750be0dc39d054df7a0091f9`; the hardened gitleaks command run locally over the new history before the push — no findings; `git push -u origin claude/s0.1-setup-scaffold` (new branch; nothing else pushed).
- **PR #3** — https://github.com/86400studio/aalishaan-studio-app/pull/3 — opened by Claude Code with the full description (PR template sections, deviations, the one declared suppression, rollback note).
- **First Code Check run** — https://github.com/86400studio/aalishaan-studio-app/actions/runs/36002643091 — event `pull_request`, head `0d27806`, check context **"Code Check"**, conclusion **success** in 44 s; every step succeeded: checkout (full history) → gitleaks (tarball `sha256sum --check`: OK; the `GIT_ATTR_SOURCE` self-check passed; 15 commits scanned including the PR merge commit; "no leaks found") → pnpm 10.34.5 (from `packageManager`) → Node 24.20.0 (from `.nvmrc`) → frozen install → typecheck → lint → format:check → unit tests (2 files passed) → build (same route table as local) → audit ("No known vulnerabilities found"). Warnings seen, none failing: `pnpm/action-setup` bootstraps with pnpm 11 and reports a v10 layout before switching to 10.34.5; Vite notes that a future major's native config loader will want `vitest.config.ts` loaded as ESM (follow-up).
- **Vercel Preview** for head `0d27806`: GitHub deployment `6638331522` (environment `Preview`, created by `vercel[bot]`, state success), URL `https://aalishaan-studio-3c4tp5tlp-86400studios-projects.vercel.app`. Protection without bypass, probed by Claude with no credential: `/`, `/this-page-does-not-exist` and `POST /api/sentry-test` each return **302** to `vercel.com/sso-api` (Vercel Authentication) — the protected half of P10 for the Preview. That platform response is not application evidence; headers, noindex and the route are checked through the bypass in the owner-run probe (Part F).

- **Required check** — the owner edited ruleset "main protection" (id `23884957`) after the first run (screenshot shared in the session). Read back by Claude with the owner's authenticated GitHub CLI: enforcement `active`, target `~DEFAULT_BRANCH`, `bypass_actors: []`, `current_user_can_bypass: never`; rules `deletion`, `non_fast_forward`, `pull_request` (0 approvals), `required_status_checks` → `{"context":"Code Check","integration_id":15368}` (GitHub Actions), `strict_required_status_checks_policy: false`, `do_not_enforce_on_create: false`. Before the change the active rules were `deletion, non_fast_forward, pull_request` only.

- **Blocked-then-green on PR #3** — records commit `3f07d66697f56aa11b424c96126bdd2c948db908` (docs only) pushed after the ruleset change. GitHub API (`gh pr view 3 --json mergeStateStatus`), polled by Claude: 13:08:56–13:09:33 UTC `mergeStateStatus=BLOCKED` with "Code Check" pending; 13:10:25 UTC `CLEAN` after run 36003676755 (https://github.com/86400studio/aalishaan-studio-app/actions/runs/36003676755) succeeded in 41 s. No application code was broken to show this, and no rule was weakened.

### Preview record — PR #3 (Vercel Preview template fields)

| Field | Value |
|---|---|
| PR | #3 — S0.1 — Scaffold, Code Check, security headers, Vercel Preview & Sentry |
| Branch | `claude/s0.1-setup-scaffold` |
| Provider / environment | Vercel / Preview (GitHub deployment `6638512041`, `vercel[bot]`, success) |
| Preview URL | https://aalishaan-studio-85syt0e53-86400studios-projects.vercel.app |
| Tested head SHA | `3f07d66697f56aa11b424c96126bdd2c948db908` (application code identical to `0d27806`; the only later commits change `docs/` records) |
| Date | 2026-09-24, probes 14:02 UTC |
| Tester | Owner (browser views and the value-bearing probes — secrets typed into a local PowerShell prompt, never shown), recorded by Claude Code |
| Environment proof | `docs/ENVIRONMENT-PARITY.md` §12 — P10 (Preview half) and P12 |

| Page / flow | Desktop | Mobile | Result | Notes |
|---|---|---|---|---|
| `/` holding page | ✓ | ✓ (DevTools responsive, 400 × 599) | PASS | Only "Aalishaan Studio" and "This website is being built and is not taking orders yet."; the line wraps cleanly on mobile; no link, form, price or payment control (owner screenshots) |
| Protection without bypass | — | — | PASS | `302` to Vercel login on this deployment for `/` (owner, probe 1); on the earlier `0d27806` Preview (GitHub deployment `6638331522`, same project protection) Claude also saw `302` for `/`, a missing path and `POST /api/sentry-test` with no credential ("Remote evidence") |
| Headers on `/` (through the bypass) | — | — | PASS | `200`; `Content-Security-Policy` (connect-src `'self'` + the project's US ingest origin only), `Strict-Transport-Security: max-age=63072000; includeSubDomains`, `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy`, `X-Robots-Tag: noindex, nofollow`; served from Vercel's cache (`X-Vercel-Cache: HIT`) with the headers intact |
| Headers on a missing path | — | — | PASS | `404` with the same seven headers (catch-all confirmed on Vercel) |
| `POST /api/sentry-test` without token | — | — | PASS | `{"status":"not_found"} 404` |
| `POST /api/sentry-test` with the Preview token | — | — | PASS | `{"status":"sent"} 200` |
| Sentry issue and alert | — | — | PASS | Owner received the e-mail "This email was triggered by Preview verification — S0.1": issue "Aalishaan Studio S0.1 synthetic Sentry test event", event `2927c8a7adf440918560de89e3a0d4d4`, 2026-09-24 14:02:14 UTC, project `aalishaan-studio-app`, `environment = preview`, `release = 3f07d66697f56aa11b424c96126bdd2c948db908`, tag `diagnostic = s0.1-sentry-test`, runtime node v24.20.0; stack frames show `src/lib/server/sentry-test.ts` line 88 and `src/app/api/sentry-test/route.ts` line 10 (readable frames and the build log's "Successfully uploaded source maps to Sentry"; maps are deleted after upload by configuration and, on Vercel, "Protected Sourcemaps" limits any map to authenticated team members — a direct `.map` request was not probed); suspect commit `0d27806`; Request shows URL and method only; no user data |
| Browser console | ✓ | ✓ | PASS (one expected browser request) | The only entry is the browser's automatic `/favicon.ico` request answered `404` — the holding page deliberately has no icon until the brand assets arrive (S1.3); no CSP violation, hydration or runtime error |

- First probe attempt (same session): probes 2–5 returned `400 Bad Request` from Vercel's edge because Ctrl+V into the hidden PowerShell prompt entered one control character instead of the secret (one `*` shown), so no request reached the application and nothing was sent to Sentry. The probe script was then changed to check each secret's length and characters (never its value) before sending; the second run is the one recorded above.
- Sentry event page (owner screenshot of issue `AALISHAAN-STUDIO-APP-4`, event `2927c8a7`): **HTTP Request → Headers: `User-Agent: curl/8.21.0` only** — no `Authorization`, no `x-vercel-protection-bypass`, no cookie, no query string; "Events (total) 1", "Users (30d) 0"; release `3f07d66697f5`, environment `preview`, runtime node v24.20.0, Cloud Resource vercel / `iad1`. The "User → Geography: Ashburn, United States" context is Sentry's location for the address that *sent* the event — the Vercel function in `iad1` — not a visitor: the installed SDK (10.75.2) adds a client IP to server events only when user-info collection is on (it is off, and `beforeSend` deletes `user`), and the browser client sends `infer_ip: "never"` in its SDK metadata in that case, so browser events carry no visitor IP or location. The owner also turned on the project's "Prevent Storing of IP Addresses".
- `Access-Control-Allow-Origin: *` on the static responses is added by Vercel's CDN for static files, not by the application; it has no effect on this public, credential-free holding page.

- No event from the denied probe: the owner's Sentry Feed (project `aalishaan-studio-app`, All Envs, 14 days, search cleared) lists exactly one issue — `AALISHAAN-STUDIO-APP-4`, the synthetic event (1 event, 0 users) — so the unauthorised `POST` raised nothing and the Preview produced no other error. The project's short-ID counter had reached 4; issues 1–3 do not appear in that unfiltered Feed and their origin was not identified (possibly Sentry's own test notifications) — recorded, not assumed.

**Verdict: PASS** for the Preview of `3f07d66` (2026-09-24).

### Follow-ups

- Vite's future native config loader: load `vitest.config.ts` as ESM (rename to `.mts` or set `"type": "module"`) — an allowed-file decision for a later sprint; today it is only a warning.
- S0.2: remove `POST /api/sentry-test`, `src/lib/server/sentry-test.ts` and `tests/unit/sentry-test.test.ts`; owner revokes `SENTRY_TEST_TOKEN` in every scope; normal Sentry coverage stays.
- Upgrade `@sentry/nextjs` to v11 with an explicit, minimal `dataCollection` configuration (reviewed Dependabot PR); ESLint 10 once `eslint-config-next`'s bundled plugins support it; pnpm 11/12 once Vercel supports them.
- CSP nonces or inline-script hashes revisited at S1.3 (storefront) or when Next ships hashes for inline flight scripts; Razorpay origins at S1.6; HSTS `preload` with D-04 at S3.4.
- ~~`docs/HANDOFF.md` §1: record the Vercel and Sentry project names once created.~~ Done 2026-09-24 in the final records commit.
- `docs/sprint-prompts/README.md` still reads "implementation Not Started" (prepared text; outside this sprint's allowed list).
