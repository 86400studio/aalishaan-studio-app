# S0.0 — Kickoff: decisions lock & governing docs

Prepared 2026-09-23 using `/sprint-prompt` Mode A and the canonical implementation template. **Executed 2026-09-23 (in progress) — see "Execution record — Claude Code, 2026-09-23" at the end; the preparation-time status below is kept as it was.**

Owner-selected workflow: **GPT-6 Astra Codex** writes sprint prompts and independently reviews the completed implementation; **Claude Code** implements and fixes findings; the human owner merges. Use a separate review session against the actual immutable implementation range. Planning is not a review verdict.

## A. Diagnosis

**Ready to begin S0.0; the production foundation is not complete yet.** The approved reference, architecture, 37-sprint roadmap, security rules and templates are present. Git history, the root application manifest, CI and deployments are absent, consistent with the recorded pre-S0.0 state. S0.0 establishes the governed repository; S0.1–S0.2 establish the executable foundation before product work.

Two inherited CI-template details need alignment during kickoff: `docs/TECHNICAL-INTEGRITY.md` specifies Node 20 rather than the roadmap's Node 24 lean, and makes unit tests optional despite this project's required test gate. Its formatter scope must also preserve the frozen prototype. These are bounded S0.0 documentation corrections, not reasons to redesign the plan.

## B. Sprint goal & scope

Follow `docs/ROADMAP.md` S0.0: establish the protected app repository, preserve the installed docs and frozen reference in its first setup PR, confirm D-01–D-39 as resolved decisions or dated leans, retain owner-input gates, verify placeholders and Admin byte integrity, obtain independent review, then owner merge. No scaffold/CI/hosting until S0.1; no Supabase until S0.2; no product pages, integrations, copy extraction or prototype changes. Setup as a whole remains open until S0.2 closes.

## C. Branch name

`claude/s0.0-kickoff-decisions`, based on the new app remote's existing `main`; never an unrelated local history or the published prototype repository.

## D. Step checklist

1. Inspect reference integrity, workspace state and account/access prerequisites.
2. Confirm delivery leans and consuming-sprint owner gates; reconcile the setup/CI documentation.
3. Establish GitHub base/protection and the local sprint branch without losing files.
4. Add root ignore rules and prepare the unchanged docs/reference import.
5. Run setup verification and record exact results.
6. When authorized, commit/push this branch, open its PR and bind the review brief to real SHAs.
7. Independent Codex review; Claude fixes; owner merges; record actual closure and then activate S0.1.

## E. Ready-to-copy Claude Code prompt

~~~text
You are my senior implementation engineer for the Aalishaan Studio website. CLAUDE.md governs this task.

## Context
This is the first setup sprint, S0.0, not the application scaffold. The governing docs are installed. The local prototype is frozen at E10: public E8, final Admin A2 — 86400 UI edition, and D-37–D-39 additions. No development sprint has completed. At preparation, repo/ has no Git history, root package.json, application CI or deployment.
The owner selected GPT-6 Astra Codex for sprint prompts and independent review, and Claude Code for implementation/fixes. Do not self-approve the implementation.

## Read first
- CLAUDE.md.
- docs/PROJECT-STATUS.md — §1–§6 state, §7 locked decisions, §8a D-01–D-39, §8b OI-01–OI-14, §10 known issues, §11 update rules, §12 placeholder register.
- docs/ROADMAP.md — Sprint S0.0, Setup Gate exceptions, Universal sprint exit gate and S0.1 boundary.
- docs/WORKFLOW.md; AGENTS.md; docs/TECHNICAL-INTEGRITY.md; docs/TECH-ARCHITECTURE.md.
- docs/APPROVED-INPUTS.md; docs/DESIGN.md; docs/content/locked-facts.md; docs/content/new-strings.md.
- docs/SECURITY-CHECKLIST.md; docs/ENV-VARS-SAFETY.md; docs/ENVIRONMENT-PARITY.md.
- docs/templates/NEW-WEBSITE-SETUP-CHECKLIST.md; docs/BROWSER-TOOLS.md; docs/HANDOFF.md.
- docs/templates/PR-DESCRIPTION-TEMPLATE.md; docs/sprint-prompts/README.md; docs/code-reviews/README.md.
- This sprint record and docs/code-reviews/S0.0-kickoff-decisions-review.md. No previous sprint record exists.

## Sprint / Branch
- Sprint: S0.0 — Kickoff: decisions lock & governing docs.
- Branch: claude/s0.0-kickoff-decisions, created from current main.
- Before editing, confirm the branch and inspect git status. Preserve existing user changes.
- Work from repo/, containing CLAUDE.md and docs/, not the download parent or prototype/.
- If no Git repository exists, report that expected state and complete Step 3 before claiming a branch. If one now exists, inspect its remote/base/worktree and resume; do not initialise another history.

## Goal
Produce the first reviewable setup PR with protected main, preserved reference, and every delivery decision resolved or carrying a sourced dated lean. Close S0.0 only after current-head independent review and actual owner merge. Then activate S0.1; do not close the whole Setup Gate.

## Not this sprint
- Next.js, dependencies, root manifest/lockfile, CI/secret-scan workflow, security headers, Sentry, Vercel and rollback drill — S0.1.
- Supabase, migrations, environment values and test harness — S0.2.
- UI, auth, checkout, payments, email and Admin implementation — S1.1 onward.
- Commercial terms and launch inputs — existing consuming-sprint gates and S3.1; live services S3.3; domain/go-live S3.4.
- Copy extraction, new customer wording, prototype edits/rebuilds, template redesign and invented approvals — excluded.

## Files
Inspect:
- Every Read first file and README.md.
- prototype/.gitignore, prototype/.gitattributes, prototype/package.json, prototype/package-lock.json.
- prototype/PAGE-TRACKER.md, prototype/docs/ADMIN-FINAL-FEATURES.md, prototype/docs/DEVELOPMENT-HANDOFF.md.
- prototype/admin/README.md, prototype/admin/CAPABILITY-MAP.md, prototype/admin/CHANGELOG.md, prototype/admin/CONTENTS-SHA256.txt, prototype/admin/TEST-RESULTS.md.
- prototype/data/development-baseline.json, prototype/scripts/development-baseline.cjs, prototype/scripts/source-files.cjs.
- prototype/site-manifest.json, prototype/data/products.json, prototype/data/pricing.json, prototype/assets/fonts/iFonts-License.txt.

Allowed to change:
- .gitignore — root repository hygiene and D-07 exclusions/exceptions only.
- README.md — kickoff orientation and safe bootstrap reconciliation only.
- docs/PROJECT-STATUS.md and docs/ROADMAP.md — S0.0 progress, decision confirmations, evidence and actual closure only; no premature Done/S0.1 activation.
- docs/TECHNICAL-INTEGRITY.md — align the documented Node major, mandatory unit-test command/contract, frozen-reference exclusions and D-26 setup applicability with S0.1; do not implement CI.
- docs/TECH-ARCHITECTURE.md, docs/APPROVED-INPUTS.md, docs/ENVIRONMENT-PARITY.md, docs/HANDOFF.md — narrowly propagate confirmed kickoff decisions and account ownership; names only, no invented evidence.
- docs/content/new-strings.md — supported approval/status metadata only, no new wording or fabricated owner approval.
- docs/templates/NEW-WEBSITE-SETUP-CHECKLIST.md — truthful S0.0 checkmarks and §2 alignment with the S0.0 branch/D-26; later setup checks remain open.
- docs/sprint-prompts/S0.0-kickoff-decisions.md — preserve the executed prompt and append actual results/deviations/follow-ups.
- docs/code-reviews/S0.0-kickoff-decisions-review.md — actual target/evidence and Codex's returned record saved verbatim by the builder.

The first PR also imports already-installed AGENTS.md, CLAUDE.md, remaining docs/, .claude/skills/ and D-07's prototype reference unchanged. Permission to include existing files is not permission to edit them. Include prototype text sources/manifests, its nested .github/ as inert reference, and fonts/brand exceptions; exclude other prototype/assets/, previews, generated releases, dependency folders and unapproved binary source workbooks. Keep excluded local files intact. Preserve all prototype/admin/ bytes and prototype/.gitattributes, including admin/** -text. Do not promote its Pages workflow into root .github/.

If another file is needed, stop and explain why before editing it.

## Gate 0
Before account-dependent operations, confirm the GitHub owning organisation/account and access. The recorded lean is 86400studio/aalishaan-studio-app. Confirm repository visibility if not already established; propose private unless directed otherwise. Reuse explicit answers already provided in this session. Do not invent ownership or transfer arrangements.
Do not start those operations until the owner confirms. Independent read-only checks and local document preparation may proceed. Other owner questions remain dated leans or gates at their consuming sprints; they do not all become kickoff blockers.
Use env variable names only; never request, read, or paste live values.

## Task / Steps
1. Inspect the named inputs and actual workspace. Run the read-only placeholder and reference checks below. Record whether this is the original full-asset workspace or a deliberately reduced D-07 checkout. Check existing browser-tool availability without installing anything or exposing credential-bearing MCP configuration. Missing tools are reported; browser QA itself is N/A for this docs-only sprint.
2. Review D-01–D-39 against their sources and confirm unchanged delivery leans with a date. Do not convert a delivery lean into owner approval. Put the six owner questions in PROJECT-STATUS.md §8 into one concise request for answers not already supplied; retain unanswered owner items as dated leans/gates for their consuming sprints. Preserve OI-13's post-launch deferral, OI-14's non-blocking status and the draft status of NS-12–NS-15. Confirm D-08, D-23, D-24, D-26 and D-36 explicitly without fabricating owner confirmation of D-24. Correct the setup checklist's generic S0.1 branch to S0.0 and defer required Code Check until S0.1. Align TECHNICAL-INTEGRITY.md's Node 20 example to S0.1's confirmed consistent major (lean 24), replace optional --if-present unit testing with the project's required test:unit contract, and require formatter/linter exclusions for frozen prototype/. Keep app pnpm test and CI test:unit consistent at S0.1. Record only doc decisions here, never claims that CI has run.
3. After Gate 0, establish the approved app remote with minimal README-backed main, or reuse it if it already exists. Require PRs and prevent direct/force pushes and deletion of main, checking effective administrator/bypass settings; no nonexistent Code Check requirement yet. If protection is unavailable, record the limitation and keep the gate open. GitHub CLI was absent at preparation: use available authenticated connector/browser capabilities or give the owner exact settings to apply; do not install gh or expose tokens. Fetch the remote's main and create only claude/s0.0-kickoff-decisions from it. Never create a competing local initial commit, reset/clean the workspace, or overwrite its README. If checkout collides with local files, use a separate sibling checkout within the workspace and explicitly overlay inspected allowed files while leaving the original intact; no bulk copying live env files or ignored assets. Remote bootstrap/settings are distinct from Git commit/push policy: the owner or GitHub initialisation creates the minimal base; the agent never pushes to main.
4. Add root .gitignore for node_modules/, .env* except placeholder-only .env.example, qa-evidence/, dist/, ordinary build/cache output and D-07 asset exclusions. Ensure negations permit traversal into prototype/assets/fonts/ and prototype/assets/brand/; a blanket ignored parent can defeat them. Leave prototype's ignore/attributes files unchanged. Inspect candidate paths before staging. No root .env.example is needed yet; no conversion of the frozen prototype to pnpm.
5. Run verification, inspect every candidate first-PR path, and record exact results/failures/skips. Do not display suspected secrets or open live env files. Keep S0.0 In Progress while delivery gates remain. With Commit/Push NO, leave verified work uncommitted, suggest a commit message and report remaining delivery actions; do not mark Done.
6. Only after the owner explicitly enables the Git policy for this sprint: stage enumerated inspected paths, commit, push only this branch and open the first setup PR against main using the PR template. Record the actual number/URL, never an assumed #1. Verify the Admin manifest in a fresh candidate checkout. Fill the review brief with actual changed paths, protection evidence, checks and N/A reasons. Commit metadata first, compute immutable SHAs afterward and supply final SHA values in the external review handoff; a commit cannot contain its own hash. The builder saves the returned review record later. Do not invent an SHA, CI result, Preview or verdict.
7. Exit gate: full-diff review, acceptance checks, and allowed status/roadmap updates. Hand the immutable range to GPT-6 Astra Codex in a separate findings-only review session governed by AGENTS.md. Claude fixes Blocking findings; record owner dispositions for Should-fix items. Substantive changes require fresh applicable checks and review at the new head. The owner merges. Verify that merge before marking S0.0 Done and activating S0.1; post-merge tracker flips use PROJECT-STATUS.md §11's small docs branch with its own Git authorization. CI/Preview/Production are N/A only for S0.0 (D-26); independent review and effective main protection remain mandatory.

## Per-step protocol
1. Inspect the named inputs and relevant existing implementation before editing.
2. Make the smallest safe change inside the allowed file list.
3. Run the exact applicable commands and sprint-specific checks below.
4. Review the diff for scope, regressions, live env files, and secret exposure without printing values.
5. Commit or push only when the Git action policy below explicitly says YES.
6. Return the report below, then stop for the owner's next instruction.

Remote commands: proceed · pause · status · fix the named issue · skip to the named step.
A skip requires an owner-approved deferral to a named future sprint/backlog item. Do not mark the sprint
complete while an exit criterion remains unmet.

## Locked inputs
- Approved copy: docs/APPROVED-INPUTS.md §2 maps the frozen prototype pages/build sources; docs/content/locked-facts.md; docs/content/new-strings.md includes draft E10 exceptions. No copy is implemented here.
- Approved design/mockups: docs/DESIGN.md; prototype/ public E8 and local prototype/admin/ E10, read through prototype/admin/CAPABILITY-MAP.md. Published /admin still shows A1.
- Architecture/schema/spec: docs/TECH-ARCHITECTURE.md, docs/SECURITY-CHECKLIST.md §9, docs/ENVIRONMENT-PARITY.md, docs/ROADMAP.md S0.0, PROJECT-STATUS.md decisions, prototype/docs/ADMIN-FINAL-FEATURES.md.

Do not invent copy, facts, design values, access rules, data or approvals. If inputs conflict, stop and add an
open decision to docs/PROJECT-STATUS.md only when that file is allowed.

## Sprint-specific rules
- Never port D-32 demo mechanisms, run prototype generators, use freeze:baseline, regenerate manifests or weaken checks to conceal reference drift.
- D-26 exempts S0.0 from app commands/CI/Preview/Production evidence, not independent review. Preserve all later sprint gates.
- Full prototype baseline checks include assets excluded by D-07: run on the preserved full-asset workspace, not unconditionally on a reduced Git checkout. Fresh-checkout Admin byte verification is still required.
- The roadmap anticipates Commit/Push YES, but this preparation request grants neither. The explicit policy below remains NO until the owner enables it for this sprint. This narrow handoff does not waive any delivery exit criterion or grant standing authorization.

## Safety
- Never open, read, copy, print, or modify .env.local or another live-value env file.
- Use env names and placeholder-only examples; never hardcode or echo a secret.
- Preserve auth, data, routing, security, and hosting behavior unless this sprint explicitly changes it.
- Do not add dependencies or change unlisted files without owner approval.

## Verification
- Typecheck: N/A — pnpm typecheck is introduced in S0.1 (D-26).
- Lint: N/A — pnpm lint is introduced in S0.1 (D-26).
- Tests: N/A for app pnpm test / pnpm test:e2e; setup/reference checks below still apply.
- Production build: N/A — pnpm build is introduced in S0.1 (D-26).
- Manual/responsive/form/data checks: no UI, form, database or migration changes; verify protection/reference integrity instead. No prototype generators or write-producing review scripts.

From repo/, on the complete local reference:
  node prototype/scripts/development-baseline.cjs
Expected: PASS for E10. Investigate changed/missing sources; never re-freeze here.

PowerShell equivalent of PROJECT-STATUS.md §12:
  $tokenHits = & rg -n --pcre2 '\[[A-Z][A-Za-z0-9_/ .:-]{2,}\](?!\()' README.md CLAUDE.md AGENTS.md docs .claude/skills -g '*.md' -g '!**/templates/**'
  if ($LASTEXITCODE -gt 1) { throw 'Placeholder scan failed to execute' }
  $unexpectedTokens = @($tokenHits | Where-Object { $_ -notmatch '\[(SPRINT_ID|SLUG|DATE|YYYY-MM-DD|HEAD_SHA|MERGE_BASE_SHA|BRANCH_NAME|DOMAIN|ID|PASS/FAIL/PENDING/N/A|CANDIDATE SHA|DEPLOYMENT/CALLBACK SHA|SANITIZED EVIDENCE LINK|OWNER/NEXT ACTION|EXACT_PATHS|A-Z|A-Za-z0-9_/ .:-|RESOLVED \[DATE\]|Setup Gate / Stage 0|Not Started / In Progress)' })
  if ($unexpectedTokens.Count) { $unexpectedTokens; throw 'Unregistered placeholders remain' }
  'PASS: no unregistered placeholder matches'

PowerShell Admin byte check, on the original reference AND a fresh candidate checkout:
  $adminRoot = (Resolve-Path -LiteralPath 'prototype/admin').Path
  $adminChecked = 0
  foreach ($manifestLine in (Get-Content -LiteralPath 'prototype/admin/CONTENTS-SHA256.txt' -Encoding utf8)) {
    if ([string]::IsNullOrWhiteSpace($manifestLine)) { continue }
    $hashRow = [regex]::Match($manifestLine, '^([a-fA-F0-9]{64})\s+\*?(.+)$')
    if (-not $hashRow.Success) { throw 'Malformed Admin manifest row' }
    $adminFile = [io.path]::GetFullPath((Join-Path $adminRoot $hashRow.Groups[2].Value))
    if (-not $adminFile.StartsWith($adminRoot + [io.path]::DirectorySeparatorChar, [stringcomparison]::OrdinalIgnoreCase)) { throw 'Manifest path escapes Admin root' }
    if (-not (Test-Path -LiteralPath $adminFile -PathType Leaf)) { throw ('Missing Admin file: ' + $hashRow.Groups[2].Value) }
    if ((Get-FileHash -LiteralPath $adminFile -Algorithm SHA256).Hash -ine $hashRow.Groups[1].Value) { throw ('Admin byte mismatch: ' + $hashRow.Groups[2].Value) }
    $adminChecked++
  }
  if ($adminChecked -ne 39) { throw ('Unexpected manifest count: ' + $adminChecked) }
  "PASS: all $adminChecked Admin manifest entries match"

Existing machine capabilities (no installs):
  git --version
  node --version
  pnpm --version
  claude mcp list
  agent-browser --help
Retain only MCP names/connection status, not credential-bearing configuration/URLs. Missing tools are reported honestly.

Once Git exists:
  git status --short --branch
  git branch --show-current
  git diff --name-status
  git diff --cached --name-status
  git diff --check
  git diff --cached --check
  git ls-files --others --exclude-standard
  git check-ignore .env.local .env.production prototype/assets/rooms/example.jpg node_modules/example qa-evidence/example.png dist/example
  git check-ignore prototype/assets/fonts/charsen.otf prototype/assets/brand/example.svg
  git check-attr text -- prototype/admin/index.html prototype/admin/state.js prototype/admin/e10-additions.js
  git ls-files -- .env.local '.env*' '**/.env*'
  git log --all --format=oneline -- .env.local
Expected: unsafe/local paths ignored; fonts/brand NOT ignored (exit 1 for that check-ignore is expected); Admin text attribute unset; no live env tracked/staged/history. Any existing example must contain placeholders only. Confirm real fonts/brand paths too. Inspect untracked paths: git diff alone misses the first import. Exclude live env files before content inspection; report suspected secret locations/types without values. Stage only explicit inspected paths when authorized.

For committed review, refresh origin/main through the existing authenticated remote, then:
  git rev-parse HEAD
  git rev-parse origin/main
  git merge-base origin/main HEAD
Use the actual SHAs in the external handoff and the paired review brief's immutable-range commands. Record effective protection and fresh-checkout Admin hashes for that candidate.

Do not guess commands or install dependencies to make a check run. Report any check that cannot run.

## Git action policy
- Commit: NO (default).
- Push: NO (default).

An omitted or unfilled field means NO. Never push to main or another branch, merge, force-push, reset user
work, or skip hooks.

## Report
1. Outcome and scope completed.
2. Files changed.
3. Commands/checks and exact results.
4. Manual or Preview verification.
5. Risks, open decisions, and follow-ups.
6. Branch and actual commit/push status; include SHA/message if committed, otherwise suggest a message.
7. Roadmap/status bookkeeping completed or still required.
~~~

Before merge: complete this record and obtain independent review against immutable merge-base/head SHAs. Preview is N/A only for S0.0 (D-26); substantive changes require refreshed applicable evidence and re-review.

## F. Codex review brief

Prepared from the canonical review template at [S0.0-kickoff-decisions-review.md](../code-reviews/S0.0-kickoff-decisions-review.md). Pending a real PR and immutable SHAs. Codex reviews Claude's completed implementation; this readiness assessment does not substitute for that review.

## G. Checklists

Follow `docs/WORKFLOW.md` §3–§8, `docs/QA-CHECKLIST.md`, `docs/SECURITY-CHECKLIST.md` and `docs/templates/NEW-WEBSITE-SETUP-CHECKLIST.md`, with only D-26's recorded S0.0 exceptions.

## Outcome and execution record (at preparation — superseded by the execution record at the end)

- Implementation: Not started; prompt preparation only.
- Branch / PR / merge: None in this workspace at preparation.
- App tests/build, CI, Preview, fresh Git checkout and independent implementation review: not performed or claimed.
- Deviation: Git policy stays NO/NO for this planning-only handoff; the owner can enable sprint-specific actions during execution. No exit gate is waived.
- Follow-ups: account/access and visibility; Claude S0.0 execution; real review target; Codex review; owner merge; then S0.1.
- Suggested eventual commit message: `docs: establish S0.0 kickoff decisions and frozen reference`.

### Readiness evidence — 2026-09-23

Read-only checks from prototype/ unless stated otherwise; these prove the local reference, not a production application:

| Check | Result |
|---|---|
| `node scripts/development-baseline.cjs` | PASS — E10, 821 source files |
| `node scripts/check-handoff.cjs` | PASS — 822 files, 61 document links |
| `node scripts/check-site.cjs` | PASS — 60 pages, 5,051 local references, 66 aliases |
| `node scripts/check-routes.cjs` | PASS — routes, nested 404, HEAD and query handling |
| `node scripts/check-artwork-mapping.cjs` | PASS — 22 suggested frames, 154 galleries, 11 style covers |
| `node admin/tests/check-state.cjs` | PASS — 13 original state-guard groups |
| Admin SHA-256 manifest | PASS — all 39 entries on the original workspace |
| Repo state | No Git history/root app/root CI, consistent with pre-S0.0 |
| Existing tools | Node 24.15.0, pnpm 11.3.0, Git 2.54, Claude 2.1.263, Agent Browser 0.33.0; gh absent |

MCP connectivity, browser launch, account access, effective protection and fresh-checkout hashes remain unverified. The locally installed pnpm 11.3.0 declares Node >=22.13, reinforcing the need to reconcile the inherited Node 20 CI example before S0.1. Owner commercial inputs, font licensing, visual review and draft customer wording retain their existing consuming-sprint gates; none is silently approved here.

Final prompt validation: canonical implementation/review headings preserved; Per-step protocol, Safety and Report retained; explicit allowed files and NO/NO Git policy present; exact PowerShell placeholder and Admin snippets executed successfully; all local Markdown links resolve. A separate read-only prompt-quality review found no unresolved consequential defects. This is preparation evidence, not the future implementation review.

## Execution record — Claude Code, 2026-09-23

Section E above is preserved verbatim as the executed prompt. This record states what actually happened; nothing here is an independent review verdict.

### Gate 0 and authorisations (owner, this session, 2026-09-23)

- **Repository account and visibility:** `86400studio/aalishaan-studio-app`, **public**. `86400studio` is a personal GitHub **user** account (GitHub API `type: User`), not an organisation. The owner's words on ownership: "Its my own account and I'll keep it here and no need to transfer as I'm the owner" — no transfer planned (D-02 RESOLVED, `HANDOFF.md` §1). Public was chosen after the owner was told that protecting `main` on a private repository of a personal account needs GitHub Pro.
- **Bootstrap:** the owner creates the repository in the browser (README only, no .gitignore or licence template) and protects `main` with a branch ruleset — enforcement Active, bypass list empty, target the default branch, rules Restrict deletions, Block force pushes and Require a pull request before merging (required approvals 0), **no** required status check (D-26). The agent verifies read-only and never pushes to `main`.
- **Git working tree:** in place in `repo/` (owner's choice over a sibling checkout): `git init`, fetch `main`, point `claude/s0.0-kickoff-decisions` at the fetched commit and load its tree into the index only (`git read-tree`), so no workspace file is written, reset or removed; the governing `README.md` replaces the minimal remote README in the first commit.
- **Git action policy for S0.0:** Commit YES, Push YES — `claude/s0.0-kickoff-decisions` only; never `main`, never merge, never force-push.

### Workspace and tools (Step 1)

- This is the **original full-asset workspace**: no `.git` in `repo/` or its parent, no root `package.json`, no `node_modules`, no `.env*` file anywhere in the workspace (existence checked by name only); `prototype/assets/` holds 635 files, about 557 MB.
- Tools present (no installs): Git 2.54.0.windows.1; Node v24.15.0; pnpm 11.3.0; agent-browser 0.33.0. **Correction to the readiness row above:** GitHub CLI **is** installed — gh 2.98.0 — but not logged in, so it was not used. `claude mcp list` (names and status only): Playwright — user scope, connected; two claude.ai connectors (Claude Docs, Google Drive) — connected. No project `.mcp.json`. Browser QA is N/A for this docs-only sprint.
- Git configuration relevant to bytes: `core.autocrlf=true` at system scope. `prototype/.gitattributes` ends with `admin/** -text`, so the 40 Admin files are stored and checked out byte-for-byte; root docs have no `.gitattributes` and may check out as CRLF on this machine (nothing hashes them).
- Stored GitHub credentials exist for two usernames (`86400websites` generic, `86400studio` scoped); the push URL therefore names `86400studio`. Values were not read.

### Checks run (exact results)

| Check | Result |
|---|---|
| `node prototype/scripts/development-baseline.cjs` (from `repo/`, full-asset workspace) | `PASS: E10 development reference matches all 821 source files.` |
| Admin manifest, PowerShell snippet from the prompt | `PASS: all 39 Admin manifest entries match` |
| Placeholder scan, PowerShell snippet from the prompt | **Could not run** — `rg` is not on PowerShell's PATH (no install made) |
| Placeholder scan, `PROJECT-STATUS.md` §12 bash form (the S0.0 acceptance command) | no unregistered token (85 raw matches, all registered); cross-checked with `rg` from Git Bash — 54 matching lines, none unregistered |
| Root `.gitignore`, probed with a scratch Git directory outside `repo/` | ignored: `.env.local`, `.env.production`, `prototype/assets/rooms/example.jpg`, `node_modules/example`, `qa-evidence/example.png`, `dist/example`, `prototype/data/catalogue-source.xlsx`, `prototype/preview/…` (nested ignore); not ignored: `prototype/assets/fonts/*`, `prototype/assets/brand/*`, `.env.example`, `prototype/admin/*`, `prototype/pages/assets/images/*.webp` |
| Candidate first-import set | 246 files, about 7.9 MB: root `README.md`, `CLAUDE.md`, `AGENTS.md`, `.gitignore`; `docs/` (47); `.claude/skills/` (5); `prototype/` — 186 files outside `assets/` (every file except the workbook and the ignored preview output), including the nested `.github/`, plus the 4 files in `assets/fonts/` and `assets/brand/` |
| Secret-pattern sweep over the import set (read-only reviewer) | 0 findings; 3 benign documentation mentions (a key-prefix name in two docs, a `secrets.` reference in a template) |
| `git diff --check` expectation | the frozen prototype carries pre-existing trailing whitespace (26 files) and blank lines at end of file (3 files, one an Admin manifest entry); these are reference bytes and are never "fixed" |

Typecheck, lint, tests, build, CI, Preview and Production: N/A for S0.0 (D-26) — the scripts arrive in S0.1.

### Step 2 — decisions and documentation

- Seven independent read-only reviewers checked D-01–D-39 against their sources, mapped the documentation reconciliations and audited the D-07 import. A five-lens review of the resulting edits (scope and truthfulness, facts, cross-document consistency, technical correctness, document integrity), with one skeptic per finding, then ran before commit: confirmed findings were fixed — protection and fresh-checkout statements tied to recorded evidence, a §6 row joined to its table, the remaining "transfer" wording in `HANDOFF.md`, a pre-existing short row in `new-strings.md` (NS-16, a "—" cell added), D-11 and D-16 amended instead of left open or known-wrong, the Vercel and Supabase plans registered at the S0.1 / S0.2 Gate 0, the About-hub FAQ statement corrected (search index, not visible text), and `TECHNICAL-INTEGRITY.md` tightened (verbatim records outside Prettier, S0.2 test placement, a real blocked-then-green observation). Findings the skeptics refuted were not applied.
- `PROJECT-STATUS.md` §8a: every row stamped 2026-09-23 — D-02 RESOLVED (owner); owner rows D-04, D-13, D-21 keep their interim leans, re-dated, as Gate 0 items; owner portions of D-06, D-24, D-33, D-37 and D-38 stay open at their Gate 0; D-08, D-23, D-24 (delivery-lead lean only), D-26 and D-36 explicitly confirmed; **D-11 amended** — its facts were wrong (the form is on ten pages and searches on About), so its interim lean now reproduces the approved design (keep the form, port the About search) and the S1.3 Gate 0 confirms it or records removal as a deviation; D-12 confirmed with a clarification (functional provider scripts are part of D-01); **D-16 amended** — the worker route accepts the GET Vercel Cron sends, with `CRON_SECRET` as a Bearer header, and sub-daily cadence needs a paid Vercel plan (`TECH-ARCHITECTURE.md` updated to match). Every note is in the new §8c with the sprint that carries it. OI-13's post-launch deferral, OI-14's non-blocking status and the draft status of NS-12–NS-15 are unchanged.
- Gate 0 registrations completed in `ROADMAP.md`: D-13 at S3.1 and S3.4, D-04 at S3.3, the owner's D-24 confirmation at S1.3, the Vercel plan at S0.1 and the Supabase plan at S0.2.
- `TECHNICAL-INTEGRITY.md`: Node 24 (one major, pinned at S0.1), required `pnpm test:unit` (no `--if-present`; an empty suite fails), `pnpm test` runs `test:unit` first, `prototype/` excluded from Prettier and ESLint, job named "Code Check", two-step protection under D-26. No claim that CI ran.
- Setup checklist: §2 now names `claude/s0.0-kickoff-decisions` and defers the required Code Check to S0.1; §1, §3 and §4 items ticked where their evidence exists; §2 items stay open until the repository, ruleset and branch exist; §5–§8 stay open (§8 runs per setup PR — for S0.0 its CI, Preview and Production items are N/A under D-26).
- **D-07 dispositions:** the source workbook `prototype/data/catalogue-source.xlsx` is excluded as this prompt directs (local copy kept); the four unreferenced `prototype/pages/assets/images/*.webp` (0.8 MB, part of the E10 baseline) are committed under the default rule because they sit outside `prototype/assets/`. The public repository re-publishes `charsen.otf`, the same file already public in the prototype repository (D-13 stays an owner decision).

### Files changed

`.gitignore` (new); `README.md`; `docs/PROJECT-STATUS.md`; `docs/ROADMAP.md`; `docs/TECHNICAL-INTEGRITY.md`; `docs/TECH-ARCHITECTURE.md`; `docs/APPROVED-INPUTS.md`; `docs/ENVIRONMENT-PARITY.md`; `docs/HANDOFF.md`; `docs/content/new-strings.md`; `docs/templates/NEW-WEBSITE-SETUP-CHECKLIST.md`; this record; `docs/code-reviews/S0.0-kickoff-decisions-review.md` (target and evidence). Every other file in the first import is unchanged from the workspace, and no prototype byte changed.

### Deviations

1. The PowerShell placeholder snippet could not run (no `rg` on PowerShell's PATH); the §12 bash form, which is the recorded S0.0 acceptance command, was run instead.
2. `main` is protected with a branch ruleset rather than a classic protection rule, so that its active rules can be read back anonymously on the public repository; `TECHNICAL-INTEGRITY.md` "Setup" describes the ruleset.
3. The Git working tree is initialised in place with an index-only load of the remote tree instead of the prompt's sibling-checkout fallback — chosen by the owner; it writes no workspace file.
4. D-11 and D-16 were amended rather than confirmed unchanged, because their premises were factually wrong (§8c); both amendments follow the sources — the approved prototype design (D-PRE-02) and Vercel's documented cron behaviour.

### Follow-ups outside the S0.0 file list (report only — each needs its own authorised change)

- `CLAUDE.md` line 44 says the stack is "locked at S0.0" (true once this PR merges); `AGENTS.md` names the public pages "E8" while the local generated pages are E10 with draft NS-12–NS-15 copy.
- `docs/content/locked-facts.md` cites `prototype/admin/workspace.js` lines shifted by E10 (52 → 53, 43 → 44, 60 → 61).
- `docs/ENV-VARS-SAFETY.md` (two Sentry projects) and `docs/error-tracking/SETUP-CHECKLIST.md` (§9 citation, Production-only alert, test-route removal timing) disagree with D-23 — for S0.1.
- `docs/QA-CHECKLIST.md`, `docs/WORKFLOW.md`, `CLAUDE.md` and `.claude/skills/close/SKILL.md` omit `pnpm format:check` from the local gates although the Code Check enforces it — for S0.1.
- `docs/testing-setup/templates/MORNING-CHECK-TEMPLATE.md` still uses Node 20 — the S0.2 prompt overrides it.
- `docs/LAUNCH-CHECKLIST.md` line 40 quotes D-12's "no third-party scripts" literally (see §8c).
- `docs/DESIGN.md` still calls the help-search form "decorative" (D-11) — for S1.3.
- `docs/QA-CHECKLIST.md` line 18 lets a green Code Check stand in for the local `pnpm test` box; from S0.2 `pnpm test` also runs integration tests against TEST — for S0.2.
- `prettier --check .` will also check the governing Markdown — the S0.1 prompt decides whether to format it once or ignore it (`TECHNICAL-INTEGRITY.md`).
- `docs/ROADMAP.md` line 3 says "D-36–D-39" for the three additions; several summary-table rows lag their amended briefs (S1.1, S1.6, S2.4, S2.18).
- `docs/sprint-prompts/README.md` still says implementation has not started.
- `docs/templates/NEW-WEBSITE-SETUP-CHECKLIST.md` line 3 says "each of the three PRs"; D-25 allows up to five setup PRs.

### Consolidated owner request (sent in the S0.0 report, 2026-09-23)

Answered: the GitHub repository (D-02). Still open, each a Gate 0 item at the sprint named: ownership of the Vercel and Sentry accounts (S0.1) and of Supabase, Upstash and Turnstile (S0.2), plus the Vercel plan (Production protection and cron cadence) and the Supabase plan (point-in-time recovery); the production domain and www/apex (D-04 — S3.3/S3.4); the Charsen licence or a replacement face (D-13 — S1.3 interim, S3.1/S3.4; the public repository re-publishes the demo-licensed file already public in the prototype repository); Razorpay capture mode and whether provider accounts are shared (D-21, OI-08 — S1.7); the email provider and the milestone-1 wording (D-06, OI-09 — S1.8); NS-15 (S1.3/S1.6), NS-12–NS-14 and the 90-day retention lean (S2.21), and whether payment-link orders go to the post-launch backlog (D-37, D-38); confirmation of the D-24 lane (S1.3); tags and the Promotional purpose (D-33 — S2.12); visual review of the E5/E7 mobile screens and the final Admin (OI-14, non-blocking); and acceptance of the amended D-11 interim lean for the help-search form (S1.3).

### Git bootstrap, commit, push and PR

Pending — the repository did not yet exist when this section was written. Filled below once performed.
