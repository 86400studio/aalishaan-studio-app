# Codex Review Brief — S0.2 — supabase-proof-harness

Prepared 2026-09-25 from `docs/templates/CODEX-REVIEW-PROMPT-TEMPLATE.md`. **Preparation only: the S0.2 implementation, PR, immutable review range and evidence are Pending. No implementation review or verdict has occurred.** Preparation inspected `main` at `235dedcabb34e5be402b47a2f19411353b0975f5` after PR #5; this is context, not a future merge-base or reviewed head.

> The builder fills factual evidence before review and supplies the final literal SHAs in an external handoff. The owner or builder appends the reviewer's returned record here; the independent reviewer does not edit the repository.

You are the independent, findings-only reviewer for this PR. `AGENTS.md` governs this review. Do not edit,
stage, commit, push, merge, install dependencies, or run migrations. Review issues introduced by the pinned
range; inspect enough surrounding context to validate them without starting an unrelated full audit.

Requested reviewer: GPT-6 Astra Codex, independently from Claude Code's implementation. Claude fixes findings; the human owner merges.

## Review target

- Repo: `86400studio/aalishaan-studio-app`.
- PR: Pending — no S0.2 PR exists at preparation. The builder supplies its actual number, URL, base and head in the handoff.
- Branch: `claude/s0.2-supabase-proof-harness` (context only; one PR by default).
- Merge-base SHA: Pending — compute from the actual PR base and final candidate; do not substitute the preparation SHA.
- Reviewed head SHA: Pending — the builder supplies the literal final candidate SHA in the external handoff.
- Immutable range: Pending — the literal merge-base SHA followed by `..` and the literal candidate head SHA.
- Sprint record: `docs/sprint-prompts/S0.2-supabase-proof-harness.md`.
- Expected changed paths: the sprint record's **Allowed to change** list, narrowed to the actual enumerated diff. Expected areas are Supabase baseline/configuration and rollback, browser/server clients and boundary enforcement, health/setup-proof routes and helpers, integration/Playwright/preflight/fixture scripts and tests, package/lock/test/lint/ignore configuration, the existing Code Check and disabled morning workflow, Sentry diagnostic removal, and enumerated environment/database/sprint/status documentation. The database record is `docs/database-changes/S0.2-0000-init.md`; forward SQL is `supabase/migrations/0000_init.sql`; paired down SQL is `supabase/rollbacks/0000_init.down.sql`. This summary does not expand the sprint's explicit allow-list. Actual `git diff --name-status` output and any owner-approved additions are Pending.

First confirm both SHAs and the actual changed-file list. Stop and report a target mismatch before reviewing
if the range, head, PR, or scope does not agree. Pending target fields are a handoff prerequisite, not permission to guess a range or review these planning documents as if they were completed implementation.

Commit the pre-review metadata before selecting the final candidate. A commit cannot contain its own SHA: put the final head, corresponding CI run, Preview deployment/source SHA and evidence links into the external handoff. Read-only evidence collected after that commit can accompany the handoff without another commit. The returned review record later preserves the actual reviewed range.

## Read for context

- `AGENTS.md` and `CLAUDE.md`.
- The sprint record above, including the executed prompt, allowed files, approved deviations, checks, external proof evidence, cleanup and remaining owner actions.
- `docs/PROJECT-STATUS.md` §1–§6, D-05/D-22/D-25, §8c, §9–§11; `docs/ROADMAP.md` S0.2, Setup Gate and Universal exit gate. Read the S0.1 diagnostic-removal carry-over and the post-review tracker rule.
- `docs/WORKFLOW.md` §3–§8, database protocol and Definition of done; `docs/TECHNICAL-INTEGRITY.md`; `docs/QA-CHECKLIST.md`; `docs/SECURITY-CHECKLIST.md` §1–§8 and applicable §9 invariants.
- `docs/TECH-ARCHITECTURE.md` §2, §3c, §4, §6 and hosting/recovery requirements; `docs/ENV-VARS-SAFETY.md`; `docs/ENVIRONMENT-PARITY.md` §3–§4, §7.9–§7.10 and §10–§13.
- `docs/SUPABASE-MCP-SAFETY.md` and `docs/SUPABASE-VERCEL-SETUP.md`; the filled `docs/database-changes/S0.2-0000-init.md` against `docs/templates/SUPABASE-CHANGE-TEMPLATE.md`.
- `docs/testing-setup/SETUP-CHECKLIST.md`, `docs/testing-setup/TESTING-GUIDE.md` and `docs/testing-setup/templates/MORNING-CHECK-TEMPLATE.md`. Their generic auth setup and active schedule do not expand this sprint.
- `docs/ROLLBACK.md`, `docs/templates/NEW-WEBSITE-SETUP-CHECKLIST.md` and relevant S0.1 closure evidence.
- Actual changed package/config/workflow/source/test/SQL/script files and unchanged surrounding boundaries. Read `docs/APPROVED-INPUTS.md`, `docs/DESIGN.md` and approved copy only if needed to verify the existing holding page was preserved. `prototype/` is frozen.

## Sprint intent

- Goal and exit condition: establish two isolated Supabase projects, a reviewed additive default-deny baseline, safe browser/server client boundaries, and the executable proof harness that later sprints use. Required evidence includes Preview-origin bounded synthetic write isolation, real TEST allowed/denied database behavior, P1/P8/P10/P11 and affected P2/P8b/P12, a deployed Preview smoke on desktop and 390 px, and disabled morning automation. Local checks, Code Check, candidate Preview and this independent review precede owner merge; Production smoke and accurate closure records follow. No evidence is prefilled PASS.
- Intentionally out of scope: product/catalogue/order tables and seed data (S1.1), staff auth/MFA/RBAC and role fixtures (S1.2), SSR authentication integration, product/Admin UI, payment/shipping/email delivery, live sales, production fixture writes, active morning scheduling, and frozen prototype changes. D-22 reset/seed scaffolds cover only bounded synthetic `system_checks` fixtures now. Planned added dependencies are `@supabase/supabase-js`, `@playwright/test` and the Supabase CLI; unexpected additions need justification within the allowed scope.
- Owner-authorized exceptions: none inferred. Profile A remains non-production only. Existing parity rules permit authorized local proof processes to consume existing secret stores without the agent reading values. They also permit a documented reviewed migration to leave TEST ahead of PROD before merge. These are not permission to apply a migration, reset data, expose secrets, use PROD MCP or waive missing evidence. Any actual write authorization and scope are recorded in the implementation handoff.
- Hosting/Preview state: Pending — candidate Preview URL, Vercel project/environment/source SHA, current desktop/mobile smoke and P10 evidence. Expected policy remains Vercel Authentication on All Deployments; the scoped automation bypass is separate from application authorization. Production remains protected, noindex and sales-closed with `SALES_MODE=off`; P10's public Production half stays Pending until S3.4.
- Database/migration state: Pending — owner-created `aalishaan-studio-test` and `aalishaan-studio-prod`, distinct public refs, scoped values and authorized TEST baseline application. `0000_init` is additive; PROD application is human-only. If PROD lacks the baseline pre-merge, record the exact expected difference, compatibility and deployment order. The owner may prove absence of the exact test counterpart through a recorded read-only schema-absence result where `system_checks` does not yet exist; do not invent a successful row query against a missing table.
- Temporary proof surface: planned `POST /api/setup-proof`, implemented through `src/lib/server/setup-proof.ts`, exists only for the bounded TEST proof workflow. It requires trusted `VERCEL_ENV=preview`, independent TEST-project validation and `S0_2_PROOF_TOKEN` authentication; Production/unknown/malformed requests are denied before database I/O. It may create/read/delete only its exact synthetic `system_checks` run marker. It remains re-runnable at the immutable candidate; remove it and revoke its token in S1.1, explicitly recorded as carry-over. Public `GET /api/health` stays read-only and status-only.
- Post-merge state: Pending by construction — owner merge, approved human PROD baseline application when required by dependent code, Production smoke, sales-closure verification and tracker closure. Review the safe order and concrete procedures now; do not require an unmerged Production deployment or describe S0.2 as Done beforehand.

## Checks and evidence

- Typecheck: `pnpm typecheck`.
- Lint: `pnpm lint`.
- Hermetic tests: `pnpm test:unit` — required in Code Check, including meaningful allowed/denied scaffolds and destination/boundary guards.
- TEST integration: `pnpm test:integration` — real isolated TEST behavior; `pnpm test` must run unit plus integration tests. These commands may create/clean fixtures and are **builder verification commands, not reviewer authorization to write**.
- Browser smoke: `pnpm test:e2e` — declared desktop and 390 px projects against the verified candidate Preview. Inspect its hooks for writes/secret handling before deciding whether any independent execution is allowed.
- Production build: `pnpm build`.
- Formatting: `pnpm format:check`.
- Critical production dependency audit: `pnpm audit --prod --audit-level=critical`.
- TEST preflight: `pnpm db:test:preflight`, backed by `node scripts/testing/preflight.mjs`; inspect the exact implementation before invoking even a nominally read-only check.
- Builder-only fixture/proof operations: `pnpm db:test:seed`, `pnpm db:test:reset` and `pnpm test:preview-proof`. Inspect code, exact recorded authorization, command results, marker-scoped effects and cleanup evidence; do not run these during review. No migration/down/reset command is authorized by this brief.
- Current CI evidence for reviewed head: Pending — literal run URL, source/head mapping and actual results for the mandatory Code Check. Existing uncredentialed PR CI retains `pnpm test:unit`; its mock/scaffold results are not evidence that real RLS passed. No new secret-bearing workflow is presumed or required by this prompt.
- Current tested Preview evidence for reviewed head: Pending — verified Vercel deployment/project/environment/SHA, desktop and 390 px results, unbypassed protection, sanctioned bypass behavior, expected health response, removed Sentry diagnostic behavior and bounded write/cleanup results.
- Current database/parity evidence: Pending — owner key-provenance/scope attestations without values; literal TEST commands/results and metadata comparisons; exact synthetic marker; owner-executed read-only PROD command/result; P1/P2/P8/P8b/P10/P11/P12 with date, candidate/deployment SHA, sanitized evidence and remaining actions. The runtime public client and privileged client must both be checked. An opaque secret key cannot be proved TEST by decoding a nonexistent embedded project ref.

Run commands only with the existing environment. Do not install or change anything to make a check pass.
State every command not run and why. Read package scripts first; do not execute an indirect migration, fixture mutation, setup-proof write, credential upload or provider action as a side effect of verification. Run available hermetic checks independently. Review credentialed/mutating proof code and supplied sanitized results; do not rerun them during findings-only review. Missing credentials, unavailable browsers or an absent matching checkout are limitations to report, never evidence of PASS. Required live evidence that remains Pending prevents treating the sprint exit gate as complete.

After the handoff supplies literal 40-character hexadecimal SHAs in `$reviewBaseSha` and `$reviewHeadSha`, these read-only PowerShell checks confirm scope:

```powershell
git status --short --branch
git rev-parse --verify "$reviewBaseSha^{commit}"
git rev-parse --verify "$reviewHeadSha^{commit}"
git merge-base --is-ancestor $reviewBaseSha $reviewHeadSha
git diff --name-status "$reviewBaseSha..$reviewHeadSha"
git diff --stat "$reviewBaseSha..$reviewHeadSha"
git diff --check "$reviewBaseSha..$reviewHeadSha"
git diff --exit-code "$reviewBaseSha..$reviewHeadSha" -- prototype/
git check-ignore .env.local .env.production
git ls-files -- .env.local '.env*' '**/.env*'
git log --all --format=oneline -- .env.local
```

Reconcile the immutable range with actual PR base/head metadata and the CI/Vercel candidate. Map GitHub's synthetic PR merge-test SHA to its head rather than confusing the two. Enumerate paths before reading content; do not open any live-value env file. A dirty or noncandidate checkout cannot independently prove the reviewed implementation: qualify its results without resetting/checking out files or installing packages.

## Hunt list

1. **Correctness and health.** `GET /api/health` performs a bounded server read, returns only sanitized status, exposes no rows/ref/key/upstream error, and reports missing configuration, missing schema and unavailable services honestly. It performs no write and does not cache a misleading healthy response. Empty baseline behavior and initial Production deployment order are explicit; the holding page must still build/render while approved baseline rollout is pending. Later S2.20 operational probes are not invented now.
2. **Authorization of the setup proof.** Inspect the real route/helper for a trusted Preview environment signal, exact token validation, independent expected TEST ref and fail-closed ordering before any client/network/database operation. Missing/empty/malformed token or config, Production, unknown environment and wrong project are denied. Request headers/body/query cannot choose an environment, credential, upstream URL, table or SQL. Deployment Protection bypass alone never authorizes the write. Only the current run's bounded synthetic marker is created/read/deleted; cleanup cannot delete another run's marker. Allowed and denied tests invoke the real guard, assert zero I/O on denial and cover malformed input and service failure. Errors are generic, operations bounded and responses not cached.
3. **Secrets and client boundaries.** `NEXT_PUBLIC_SUPABASE_URL` and the publishable key are the only intended Supabase browser values; `SUPABASE_SECRET_KEY` stays in a server-only module. Inspect direct and transitive imports, re-exports and build behavior as well as the lint restriction; a folder name is not enforcement. No server secret/token reaches browser bundles, props, HTML, errors, logs or reports. `S0_2_PROOF_TOKEN` exists only in Preview and the trusted invoking process, never Production/public-prefixed/client configuration. Live env files are not read or copied; `.env.example` is names/placeholders only. New configuration names and their visibility/scopes are reconciled across architecture, parity and status records.
4. **Data safety and rollback.** `0000_init` contains only the approved baseline, with default-deny RLS before data lands, minimum grants and no accidental anonymous access to `system_checks`. Helpers and privileged functions have pinned search paths and narrow execute grants; untrusted identifiers cannot become dynamic SQL. Trigger behavior is verified. The down file is outside `supabase/migrations/` so normal forward migration discovery cannot run it. CLI behavior, migration classification, non-production verification and supported rollback procedure are concrete. Down-SQL may remove data and is never auto-run merely because it exists; prefer retaining additive schema and forward-fixing. No broad drop/truncate/reset, PROD seed or customer-data copy is hidden in scripts. Host rollback cannot restore database data.
5. **Isolation and proof strength.** Before external writes, verify owner-confirmed provenance and runtime destination for both public and privileged clients, independently known unequal TEST/PROD refs, and candidate deployment/project/environment/SHA. A user-controlled URL or a `.vercel.app` hostname alone proves neither safe hosting nor safe data wiring. Satisfy the roadmap's early isolation gate with non-writing provenance preflight before table creation, then complete the scoped runtime P1/P2 checks after authorized TEST baseline application. The write must originate in the deployed Preview; a direct script write plus Preview GET is insufficient. PROD absence is exact-marker/schema-scoped, owner-run and read-only under Profile A; no negative PROD writes or new PROD MCP connection. Compare P8 definitions/grants/RLS/functions/triggers/settings, distinguish expected pending baseline from unexplained drift, and keep P10's launch-only half Pending. P11 runs before each external run and addresses actual pause/quota failures; keepalive ownership cannot depend on the disabled morning workflow.
6. **Harness and input safety.** Unknown/live/Production full-suite targets are refused using verified deployment metadata and explicit destination checks. Bypass headers go only to the exact validated Preview origin for browser and direct HTTP requests; no context-wide header leaks to third parties or redirected requests. `PLAYWRIGHT_BASE_URL` has no dangerous live/default fallback. Credentials come from an existing authorized store without printing values. Secret-bearing traces, screenshots, snapshots and raw report attachments are disabled unless proven redacted; auth state and local artifacts are ignored. A raw provider body or token URL cannot become a report artifact. Missing credentials, zero selected tests, skipped required projects/assertions and early returns fail their proof gate. Bounded retries expose flaky outcomes. Serialized fixture runs, deterministic markers, exact cleanup and residual-data records prevent one run from invalidating another.
7. **Real tests and safe CI.** Preserve mandatory uncredentialed Code Check, full-history secret scanning, frozen lockfile install and exact pinned toolchain. CI executes meaningful hermetic allowed/denied scaffolds; real TEST integration has separately recorded authorized execution and denied-RLS assertions. Do not label mocks as P8b. `pnpm test` includes the real integration command and cannot silently skip it when values are missing. Both Playwright projects actually run against the same candidate. The baseline fixture used by integration tests demonstrates authorized read/write and anonymous denial without introducing staff auth or production identities. No pull-request script gains TEST/bypass secrets from an untrusted fork; any expanded credentialed CI design needs explicit approval and narrow trust controls.
8. **Build, deployment and environment wiring.** Browser and server clients use values from the same intended project, initialized without forcing live secrets into uncredentialed build checks. Preview/Development point to TEST, Production to PROD, including branch overrides and stale deployments; changed public values require a fresh build. Dependency and lockfile changes are limited to justified S0.2 additions. Do not copy the morning template's Node 20 over locked Node 24. Existing headers, CSP, Sentry capture/scrubbing and noindex are preserved; add no broad origin merely for a future integration. The existing holding page and closed-sales behavior remain unchanged.
9. **Disabled automation and hygiene.** `.github/workflows/morning-check.yml` is unconditionally disabled: no active schedule and no manual/event path can execute probes or consume secrets. It makes no current notification/uptime/keepalive claim. Future owner-approved selection and activation remain S3.2/S3.4. Reset/seed scripts reject PROD/unknown configuration before mutation and have explicit TEST-write authorization; they do not seed future catalogue, staff or order fixtures. `.mcp.json`, if present, contains only verified project-scoped `supabase-dev` Profile A configuration and no credentials or production connection. Manual MCP approval remains enabled.
10. **Scope, cleanup and delivery evidence.** Remove `POST /api/sentry-test`, its helper and its obsolete tests; verify the owner revoked both old tokens without exposing values. Preserve ordinary Sentry/header tests. Keep the new setup-proof endpoint/token removal assigned to S1.1, and preserve re-runnable evidence until review. No product/auth/UI/prototype behavior or invented customer copy enters the diff. Status records distinguish prepared, implemented, externally verified, reviewed and merged states. Every claim names its actual SHA and evidence; missing owner actions remain Pending. Only the returned review record may be appended after review without a new independent review; tracker flips are not covered by that exemption.

Do not open a live-value env file from the worktree. Never echo a suspected secret value. Identify only
its file, line, and type and recommend rotation.
Report serious, evidence-backed issues only; no style nits. A safety failure introduced or worsened by the range is Blocking under `AGENTS.md`.

## Returned record

No returned implementation record exists yet. After receiving and confirming the actual immutable handoff, begin the returned record with:

- Confirmed range: the literal verified merge-base SHA, `..`, and literal reviewed head SHA.
- Scope match: YES or NO, with the actual path/authorization explanation.
- Files/context inspected: actual paths and relevant surrounding boundaries.
- Commands/evidence checked: exact results, source/head mappings and every skip/limitation, distinguishing independently run hermetic checks from builder/owner external evidence.

For each finding, use a numbered finding heading and the canonical fields:

- **Severity:** Blocking / Should-fix.
- **Location:** actual `path/file.ext:line` plus route/flow.
- **Issue:** one or two evidence-based sentences.
- **Failure scenario:** concrete input/state leading to the wrong outcome.
- **Suggested fix:** specific and minimal.
- **Confidence:** high / medium / low.

If there are no findings, state **No findings** and list the correctness, safety, build, database and Preview paths verified. Do not return a bare approval. For every Should-fix finding, make its disposition and whether it can be deferred explicit.

End with exactly one completed verdict: **Verdict: APPROVE** when no Blocking findings remain and every Should-fix disposition is explicit, or **Verdict: REQUEST CHANGES** when one or more Blocking findings exist, followed by one sentence explaining why. Then state the literal reviewed range, reviewer and date. These are instructions for the future returned record, not a verdict on the unimplemented sprint.

The owner or builder appends the complete returned record to `docs/code-reviews/S0.2-supabase-proof-harness-review.md`. Any substantive code, config, schema, lockfile or runtime change after the reviewed head invalidates approval and requires updated checks, a refreshed candidate Preview and independent review of the new immutable head. Apply the project's stricter `PROJECT-STATUS.md` §11 rule: **the sole no-new-review commit is an append of the returned review record to this file only**, with the reviewed SHA and documentation-only scope stated. Any other post-review change, including tracker or sprint-record changes, requires a new review before merge. Approved/Done tracker flips ride the post-merge docs branch. Do not commit new metadata after review merely to embed the head into its own record.

## Builder status — 2026-09-25 (before any push; not a review target yet)

- The implementation is committed on `claude/s0.2-supabase-proof-harness` (created from `main` = `origin/main` = `235dedcabb34e5be402b47a2f19411353b0975f5`, PR #5) and pushed as **PR #6 (https://github.com/86400studio/aalishaan-studio-app/pull/6)**, opened 2026-09-25 by the builder with the prepared description after the owner's Commit/Push authorisation. Merge-base: `235dedcabb34e5be402b47a2f19411353b0975f5`. Head: the branch tip at handoff — quoted literally in the handoff once the evidence is complete (`git rev-parse origin/claude/s0.2-supabase-proof-harness`); the branch carries one content commit. Do not review until the builder supplies the handoff: the TEST apply and integration evidence, the Preview record and proof run, and a green Code Check on the reviewed head (the first run failed on a scanner false positive in the preserved prompt text — sprint record, Deviation 11).
- Expected changed paths (to be confirmed against `git diff --name-status` of the real range): the "Files changed" list in the sprint record's execution record, plus `.env.example` once the owner replaces its content (the agent's permission settings deny it `.env*` paths) and `.mcp.json` once the owner supplies the TEST ref.
- Local evidence (sprint record → "Checks run" and "Local runtime verification"): typecheck, lint, format, 333 hermetic unit tests, the production build with no environment value, `audit --prod`, the Playwright smoke 12/12 in **local mode** (not Preview evidence), `pnpm test` failing closed without TEST inputs, and every harness command refusing with names only. The deviations and their reasons are listed there (the extra harness input `PLAYWRIGHT_TARGET_MODE`; the directive-aware lint boundary; the host-scoped bypass cookie instead of a context-wide header; `.env.example` and `.mcp.json` left to the owner; the CRLF working-tree note; nothing applied or deployed).
- Internal pre-review (2026-09-25, before this handoff): a six-lens adversarial pass over the uncommitted tree produced 21 raw findings, tabled with dispositions in the sprint record → "Internal pre-review results" (17 fixed, 2 owner items, 1 accepted as designed, 1 duplicate) plus one database-lens change (`service_role` keeps EXECUTE on the trigger function). Reviewers may use that table as a starting point for the hunt list, not as a substitute for it.
- Database state at hand-off preparation: `0000_init` drafted and verified against the pinned CLI's discovery rules; **not applied** to TEST or PROD — the owner's projects do not exist yet. The change record (`docs/database-changes/S0.2-0000-init.md`) carries the exact TEST and human PROD procedures and the read-only verification SQL; its "Applied and verified" rows are Pending.
- Still to supply at handoff: the PR number and URL, literal merge-base and head SHAs, the Code Check run, the TEST apply and verification evidence, `pnpm test:integration` results, the Preview record (URL, head SHA, both projects' smoke, the unbypassed 302, the proof run's marker and statuses, the owner's read-only PROD negative control), P1/P2/P8/P8b/P10/P11/P12 in `ENVIRONMENT-PARITY.md` §12, and the owner's P12 confirmation (names only).
- Reviewer note for the eventual handoff (learned at PR #4): Codex runs in the builder's working tree — inspect with `git diff`, `git show <sha>` and `git show <sha>:<path>` only; never `git checkout` or `git switch`.
