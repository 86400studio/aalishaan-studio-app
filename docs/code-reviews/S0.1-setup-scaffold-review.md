# Codex Review Brief — S0.1 — setup-scaffold

Prepared 2026-09-23; handoff checked 2026-09-24 from `docs/templates/CODEX-REVIEW-PROMPT-TEMPLATE.md`. **Implementation and review pending.** The builder supplies the actual PR, immutable range and measured evidence before use. No implementation verdict is issued by this preparation.

You are the independent, findings-only reviewer for this PR. AGENTS.md governs this review. Do not edit,
stage, commit, push, merge, install dependencies, or run migrations. Review issues introduced by the pinned
range; inspect enough surrounding context to validate them without starting an unrelated full audit.

Requested reviewer: GPT-6 Astra Codex, in a separate review session from Claude Code's implementation. Claude fixes findings; the human owner merges.

## Review target

- Repo: `86400studio/aalishaan-studio-app`.
- PR: PENDING — builder supplies the actual number and URL; never assume the next number.
- Branch: `claude/s0.1-setup-scaffold` (planned; context only).
- Merge-base SHA: PENDING — compute from the refreshed main reference and actual candidate.
- Reviewed head SHA: PENDING — final committed implementation head, supplied in the external handoff.
- Immutable range: PENDING — the supplied literal merge-base SHA followed by `..` and the supplied literal head SHA.
- Sprint record: `docs/sprint-prompts/S0.1-setup-scaffold.md`.
- Expected changed paths: the sprint record's explicit Allowed to change list, narrowed to the actual enumerated diff. It permits the root Next.js/pnpm/TypeScript/Tailwind/ESLint/Prettier/Vitest configuration, minimal `src/app` holding page, six-header configuration, Sentry hooks and temporary route/helper, two unit-test files, Code Check/Dependabot, `.env.example`, ignore/README updates and the enumerated S0.1 documentation. No prototype changes, database/auth/browser harness, product pages or provider SDKs beyond Sentry. Builder supplies `git diff --name-status` for the pinned range; directory names here do not expand the explicit file allow-list.

First confirm both SHAs and the actual changed-file list. Stop and report a target mismatch before reviewing
if the range, head, PR, or scope does not agree. If the target/evidence remains pending, report the missing inputs; never review a guessed range or issue an implementation verdict on these unexecuted prompts.

The preparation-time main SHA is not the review target. Commit metadata before calculating the final handoff SHAs; a commit cannot contain its own hash. The builder appends the actual reviewed range with the returned record later.

## Read for context

- `AGENTS.md`.
- The sprint record above, including the exact executed prompt, approved deviations, command results and Preview evidence.
- `docs/PROJECT-STATUS.md` §1–§6, §8a/§8c D-01/D-03/D-07/D-23–D-26, §9/§11; `docs/ROADMAP.md` S0.1/S0.2, Setup Gate and Universal exit gate.
- `CLAUDE.md`; `docs/WORKFLOW.md`; `docs/TECHNICAL-INTEGRITY.md`; `docs/TECH-ARCHITECTURE.md` §2, §3c, §6–§7 and hosting/rollback requirements.
- `docs/SECURITY-CHECKLIST.md` §1–§2, §5–§9; `docs/ENV-VARS-SAFETY.md`; `docs/ENVIRONMENT-PARITY.md` §2–§4 and §12.
- `docs/ROLLBACK.md`; `docs/HANDOFF.md`; `docs/error-tracking/SETUP-CHECKLIST.md`; `docs/templates/NEW-WEBSITE-SETUP-CHECKLIST.md`.
- `docs/APPROVED-INPUTS.md`; `docs/DESIGN.md`; `docs/content/locked-facts.md`; `docs/content/new-strings.md` for any visible holding/error wording.
- Actual root package/lock/config/ignore files, workflow, Dependabot, implementation and tests in the diff. Inspect unchanged `prototype/.gitattributes`/Admin manifest only as needed to verify preservation; do not run write-producing prototype scripts.

## Sprint intent

- Goal and exit condition: establish the empty, strictly checked app and required six-check Code Check with blocking secret scanning; deploy an isolated, verified candidate Preview with six effective headers, noindex holding state and a protected Sentry test route whose Preview alert reached the owner. Owner merge then permits Production smoke, the Production alert and the real rollback drill; S0.1 is Done only after those later facts are recorded.
- Intentionally out of scope: Supabase, migrations, auth, integration/Playwright harness, morning-check, storefront/Admin/product functionality, tokens/fonts, payment/transactional providers, live sales, domain/DNS and prototype changes.
- Owner-authorized exceptions: none inferred. Existing decisions permit the scaffold on the recorded development plan, defer the diagnostic route removal to S0.2 and leave the public Production half of P10 until launch. S0.0's D-26 exemptions and Git authorization do not apply. Check any additional exception against actual owner evidence.
- Hosting/Preview state: PENDING — actual Vercel project/deployment URL, protection policy, branch/head, desktop/mobile, header/noindex and Sentry proof must be supplied before review. Production deploys from `main` only. Domain remains D-04; a provider URL is valid for this sprint.
- Database/migration state: N/A — none permitted. `business_rules` does not exist; sales closure is the holding page plus `SALES_MODE=off`, with deployment protection where supported.
- Post-merge state: Production smoke/alert and Instant Rollback are PENDING at pre-merge review by construction. Review code and the concrete procedure now; do not require an unmerged Production deploy or accept a fabricated post-merge result. These gates still prevent a premature Done.

## Checks and evidence

- Typecheck: `pnpm typecheck`.
- Lint: `pnpm lint`.
- Tests: `pnpm test:unit` and `pnpm test` — actual hermetic Vitest tests, same suite in S0.1; no secrets/network, no optional/empty-suite success. `pnpm test:e2e` is not implemented until S0.2.
- Production build: `pnpm build`.
- Formatting: `pnpm format:check`.
- Critical production audit: `pnpm audit --prod --audit-level=critical`.
- Current CI evidence for reviewed head: PENDING — supply run URL, check context/conclusion, source SHA, six checks and redacted gitleaks result.
- Current tested Preview evidence for reviewed head: PENDING — supply deployment/SHA, URL, actual desktop/mobile observations, six headers, noindex, diagnostic denied/allowed evidence, Sentry environment/release/issue reference and owner-confirmed email receipt.
- Required-check enforcement: PENDING — same PR's first workflow run, exact Code Check required in the existing main ruleset with no bypass, subsequent pending/failing state blocking merge, then green. The candidate must end green. A green job alone is insufficient.
- Env/hosting provenance: PENDING — owner names/scopes inspection and qualified P10/P12 evidence; all three scopes sales-closed, Sentry development/preview/production distinguished, no values recorded. Public Production P10 remains pending until launch.
- Production smoke / Production Sentry alert / rollback drill: PENDING post-merge, with owner and procedure recorded before merge and actual deployment IDs/SHAs/results afterwards.

Run commands only with the existing environment. Do not install or change anything to make a check pass.
State every command not run and why. Read package scripts before execution; do not run credentialed uploads or external probes as a side effect of local verification. Do not trigger Sentry or change host/ruleset settings during review: inspect the supplied sanitized evidence/read-only deployment views. Missing external evidence is a stated limitation, never an independent-execution claim.

With the supplied literal 40-character hexadecimal SHAs assigned to PowerShell variables `$reviewBaseSha` and `$reviewHeadSha`:

```powershell
git status --short --branch
git rev-parse --verify "$reviewBaseSha^{commit}"
git rev-parse --verify "$reviewHeadSha^{commit}"
git merge-base --is-ancestor $reviewBaseSha $reviewHeadSha
git diff --name-status "$reviewBaseSha..$reviewHeadSha"
git diff --stat "$reviewBaseSha..$reviewHeadSha"
git diff --check "$reviewBaseSha..$reviewHeadSha"
git diff --exit-code "$reviewBaseSha..$reviewHeadSha" -- prototype/
git check-ignore .env.local .env.production
git ls-files -- .env.local '.env*' '**/.env*'
git log --all --format=oneline -- .env.local
```

Reconcile the supplied range with the PR base/head, CI source SHA and Vercel source commit. GitHub's synthetic PR merge-test SHA must be mapped to its candidate head, not mistaken for a mismatched implementation without inspection. A noncandidate or dirty worktree is not independent test evidence: use the already available matching checkout or qualify the limitation, without checkout/reset/install operations. Enumerate paths before reading changed contents; exclude live-value env files, and redact suspected secrets. The permitted `.env.example` is inspected for blank/placeholders only. A reduced Git checkout lacks D-07 assets and need not pass the full prototype asset baseline; no reference file may change.

## Hunt list

1. Correctness: the holding page renders at 320/768/1440, has no sale/payment/form controls or invented commercial promise, is readable without JS and has working noindex on Preview and closed Production. Framework runtime, hydration and browser/server error capture must work under the actual CSP; config text alone is insufficient. Inspect the allowed minimal `src/app/global-error.tsx` boundary for React render-error capture and its registered generic fallback; the designed product 500 page belongs to S1.3.
2. Authorization: inspect the actual `POST /api/sentry-test` handler and server-only guard. `SENTRY_TEST_TOKEN` is required before any capture; absent/empty server token and absent/malformed/wrong credentials fail closed. GET/HEAD/other methods do not emit. No query-string token, public-prefix secret, public trigger button, client trust or reliance on platform protection as application auth. Allowed/denied tests execute the real handler path and assert denied requests emit no event. Synthetic content is fixed, response generic, caching off, flush bounded; route removal/token revocation are explicitly assigned to S0.2.
3. Secrets/env: no live env values, credentials, tokens, private keys or private URLs in tracked source, logs or docs. `.env.example` has only names/placeholders. Inspect server/build/client boundaries for `SENTRY_TEST_TOKEN`, `SENTRY_AUTH_TOKEN`, `SALES_MODE` and the automation bypass; only the intended public DSN/site URL may reach clients. Source-map upload credentials remain build-only and maps are not served publicly. No real credential is required by PR checks.
4. Data safety: no database, order, customer or provider-write path slipped into the scaffold. Sentry lacks customer/user PII, replay, cookies, Authorization and token-bearing URLs; review scrubbing in every SDK runtime. Denied diagnostic traffic cannot become synthetic alert spam through another capture path. Do not claim a database-backed sales gate exists yet.
5. Input safety: diagnostic requests cannot supply arbitrary event text, redirect destinations or upstream URLs. Environment/URL handling does not trust request headers to select Production, canonical origins or credential destinations. Bearer comparison handles malformed/unequal-length input without throwing or leaking internal errors.
6. Build/deploy: verify locked package manager, committed pnpm lockfile, consistent Node 24, strict TS and compatible SDK hooks. `test` actually runs `test:unit`; neither silently passes an empty suite. Unit tests never run Playwright or call providers. Check all six Code Check commands, frozen-lockfile install, all-PR trigger, exact required context and enforced non-bypass rules; no skip/continue-on-error/path filter that produces false green. Full-history gitleaks is redacted and blocking, scanner/action pinned, permissions minimal, untrusted PR code never runs in a privileged pull_request_target workflow. Dependabot scopes the new root app/Actions, not the frozen prototype.
7. Scope/content: actual paths fit the sprint's explicit allow-list; prototype bytes, governing template skeletons and old verbatim records remain untouched. Formatter/linter/type/test configuration excludes frozen reference material. New holding/error strings have a registered source/approval under D-24; no default scaffold marketing, fonts or speculative design system. D-23 corrections are narrow and consistent: one Sentry project, distinct environment tags, names-only setup, Preview and Production alerts at their required times, removal in S0.2.
8. Regressions and headers: D-07 ignore/asset exceptions and prior main protections are preserved. All six headers cover the app, including error/not-found paths; no conflicting duplicate config or protection-page response presented as app evidence. CSP permits only actual runtime/ingestion needs, with no speculative provider origins or blanket relaxation; production unsafe-eval is absent. HSTS/framing/referrer/permissions policies are coherent with the holding surface and observed on deployment.
9. Sentry provenance: Preview and Production are distinguished by trusted deployment context, not NODE_ENV alone. Releases match the candidate commit; issue receipt and owner email receipt are evidenced separately. Missing optional SDK configuration is not an application outage, and cannot be reported as successful monitoring. Production-only alert rules cannot satisfy the required Preview proof.
10. Hosting/rollback and evidence: Vercel source root is the Git root, Production branch is main, each PR Preview is isolated, SALES_MODE is off in all scopes and noindex remains. Deployment protection and safe bypass use match recorded plan capabilities; any limit is explicit. No unmerged Preview promotion to satisfy a stale Sentry instruction. The rollback procedure obtains two eligible harmless main-derived artifacts, records actual IDs/SHAs/result and restores the intended deployment; post-merge checks remain pending until executed. P10's launch half and future database/provider proofs are not falsely marked PASS. Done/next-sprint activation awaits all S0.1 gates.

Do not open a live-value env file from the worktree. Never echo a suspected secret value. Identify only
its file, line, and type and recommend rotation.
Report serious, evidence-backed issues only; no style nits. Scope findings to defects introduced or worsened by the pinned range. Do not turn later-sprint absences into current defects.

## Returned record

Begin with:

- Confirmed range: the actual literal merge-base SHA, `..`, and reviewed head SHA.
- Scope match: YES or NO, with explanation.
- Files/context inspected: actual paths and governing context inspected.
- Commands/evidence checked: exact results, skipped commands/reasons, CI/Preview provenance and any access limitation; identify post-merge evidence still due.

For each finding, use a numbered Finding heading and:

- **Severity:** Blocking / Should-fix.
- **Location:** actual path and line plus route/flow.
- **Issue:** one or two evidence-based sentences.
- **Failure scenario:** concrete input/state leading to the wrong outcome.
- **Suggested fix:** specific and minimal.
- **Confidence:** high / medium / low.

If there are no findings, state **No findings** and list the correctness, safety, build, and Preview paths
verified. Do not return a bare approval. Every Should-fix has an explicit disposition; deferral requires recorded owner/reason.

End with exactly one verdict: **APPROVE** (no Blocking findings and explicit Should-fix dispositions) or **REQUEST CHANGES** (one or more Blocking findings), followed by a one-line reason. Restate the actual immutable reviewed range, reviewer and date. An implementation verdict is not Production smoke, rollout approval or sprint completion.

The owner or builder appends this returned record to `docs/code-reviews/S0.1-setup-scaffold-review.md`.
Any substantive change after the reviewed head invalidates approval and requires updated checks, a refreshed Preview,
and independent review of the new immutable head. A commit that only appends this review record may be
exempt when its documentation-only scope and reviewed head are recorded.

## Builder status — 2026-09-24 (before any push; not a review target yet)

- The implementation exists as an **uncommitted** working tree on the local branch `claude/s0.1-setup-scaffold`, created from `main` = `origin/main` = `ce41b346e2db8f3d750be0dc39d054df7a0091f9`. S0.1 Git policy was Commit NO / Push NO, so there is no PR, no immutable range, no Code Check run and no Preview yet. Do not review until the builder supplies them below.
- Expected changed paths (to be confirmed against `git diff --name-status` of the real range): the "Files changed" list in the sprint record's execution record, plus `.env.example` once the owner adds it (the agent's permission settings deny it `.env*` paths).
- Local evidence (sprint record → "Checks run" and "Local runtime verification"): the six commands, `pnpm test` (51 tests), the empty-suite failure, the build, `audit --prod`, a redacted gitleaks scan of the history and the new files, header and route probes against `next start`, and a browser check at 1440 / 768 / 320. Deviations and their reasons are listed there (pnpm 10.34.5, `@sentry/nextjs` 10.75.2, ESLint 9, CSP `'unsafe-inline'`, no separate Sentry server/edge config files).
- Still to supply at handoff: PR number and URL, literal merge-base and head SHAs, the Code Check run and the required-check proof, the Preview record (URL, head SHA, headers, noindex, desktop and mobile, denied and authorised route, Sentry issue and the owner's alert e-mail), P10/P12, and the owner's NS-17 approval.

## Review history

PENDING — no implementation review has run. Append each returned review verbatim, preserving earlier ranges, findings and dispositions.
