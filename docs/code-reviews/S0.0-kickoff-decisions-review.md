# Codex Review Brief — S0.0 — kickoff-decisions

Prepared 2026-09-23 from `docs/templates/CODEX-REVIEW-PROMPT-TEMPLATE.md`. **Implementation committed 2026-09-23; review has not run.** The final head SHA and the PR number arrive in the external review handoff. The builder fills the actual evidence and appends the returned record. This preparation is not approval.

You are the independent, findings-only reviewer for this PR. AGENTS.md governs this review. Do not edit,
stage, commit, push, merge, install dependencies, or run migrations. Review issues introduced by the pinned
range; inspect enough surrounding context to validate them without starting an unrelated full audit.

Requested reviewer: GPT-6 Astra Codex, in a separate review session from Claude's implementation. Claude fixes findings; the owner merges.

## Review target

- Repo: `86400studio/aalishaan-studio-app` — public, on the owner's own personal GitHub account `86400studio` (D-02 resolved 2026-09-23); the remote `origin` of the builder's working tree.
- PR: opened by the owner in the browser from the pushed branch; its actual number and URL are supplied in the external review handoff — do not assume a number.
- Branch: `claude/s0.0-kickoff-decisions` (context only).
- Merge-base SHA: `5d98958942377c242e88b48a03228baccc3010bb` — the owner's README commit, `origin/main` when the branch was created; re-confirm against current `main`.
- Reviewed head SHA: the final branch head, supplied in the external review handoff. The content commit is `c00846c6f765df74f028a60310eee5e64dbdde1a`; a metadata commit on top records this evidence and touches no file under `prototype/`.
- Immutable range: `5d98958942377c242e88b48a03228baccc3010bb..` followed by the supplied head SHA.
- Sprint record: `docs/sprint-prompts/S0.0-kickoff-decisions.md`.
- Expected changed paths: `.gitignore`; installed root `README.md`, `CLAUDE.md`, `AGENTS.md`; installed `docs/` and `.claude/skills/`; D-07's unchanged prototype text sources/manifests and fonts/brand. The first import makes existing files appear newly added. Permitted edits within that import are only the sprint prompt's Files list. Builder supplies the actual enumerated file list. No root app/CI, migrations, live env, bulk artwork assets, previews, releases or unapproved workbook binaries. Actual: 246 paths in `c00846c` (245 added; `README.md` modified from the owner's one-line README), plus the metadata commit's docs (`docs/sprint-prompts/S0.0-kickoff-decisions.md`, this file, `docs/PROJECT-STATUS.md`); enumerate with `git diff --name-status` over the range.

First confirm both SHAs and the actual changed-file list. Stop and report a target mismatch before reviewing
if the range, head, PR, or scope does not agree. If the PR or either SHA remains PENDING, request the concrete target; do not issue a verdict on an uncommitted workspace.

Final SHA values may arrive in the external review handoff after the candidate commit. A commit cannot contain its own SHA; the builder later saves the actual reviewed range in the returned record.

## Read for context

- `AGENTS.md` and the sprint record above, including its exact executed prompt and check snippets.
- `docs/PROJECT-STATUS.md` §1–§8, §10–§12; `docs/ROADMAP.md` S0.0, Setup Gate exception and Universal gate.
- `CLAUDE.md`; `docs/WORKFLOW.md`; `docs/SECURITY-CHECKLIST.md` §1/§9; `docs/ENV-VARS-SAFETY.md`.
- `docs/TECH-ARCHITECTURE.md`; `docs/TECHNICAL-INTEGRITY.md`; `docs/APPROVED-INPUTS.md`; `docs/ENVIRONMENT-PARITY.md`; `docs/content/new-strings.md`.
- `docs/templates/NEW-WEBSITE-SETUP-CHECKLIST.md`; `docs/HANDOFF.md`; `docs/BROWSER-TOOLS.md`.
- `.gitignore`; `prototype/.gitignore`; `prototype/.gitattributes`; `prototype/admin/CONTENTS-SHA256.txt`; `prototype/data/development-baseline.json`; `prototype/scripts/source-files.cjs`.
- `prototype/PAGE-TRACKER.md`; `prototype/admin/CHANGELOG.md`; `prototype/admin/CAPABILITY-MAP.md`; `prototype/docs/ADMIN-FINAL-FEATURES.md`; `docs/content/locked-facts.md` as needed to validate reference/decision claims.

## Sprint intent

- Goal and exit condition: establish the governed app repository and effective main protection; preserve the docs/E10 reference; record all D-01–D-39 decisions as resolved or sourced dated leans, retaining owner gates. Before merge verify scope, protection, placeholder hygiene and candidate-checkout Admin hashes. Owner merge follows independent review; only then activate S0.1.
- Intentionally out of scope: scaffold/dependencies/CI/hosting/Sentry (S0.1), database/harness (S0.2), product implementation, prototype changes, copy extraction, customer wording and launch approval.
- Owner-authorized exceptions: D-26 makes app commands/CI/Preview/Production N/A for S0.0; Code Check becomes required at S0.1. Verify D-26 was ratified in the execution record. Independent review and PR-required protection are not exempt; no other exception is assumed.
- Hosting/Preview state: N/A — no application or deployment in S0.0 (D-26). Prototype localhost is not deployed-app evidence.
- Database/migration state: N/A — none.
- Main-protection evidence: ruleset `23884957` "main protection", enforcement active, target the default branch; active rules on `main` read back anonymously from the GitHub API on 2026-09-23 — `deletion`, `non_fast_forward`, `pull_request` (0 required approvals); no required status check (D-26). Bypass list empty per the owner's settings screen (not visible to anonymous API readers). Details: sprint record, "Git bootstrap, commit, push and PR".
- Commit/push authorization evidence: the owner set Commit YES / Push YES for `claude/s0.0-kickoff-decisions` only on 2026-09-23, in the implementation session (sprint record, "Gate 0 and authorisations"); the prompt's default NO/NO was superseded by that instruction. Reviewer performs neither.

## Checks and evidence

- Typecheck: N/A — `pnpm typecheck` from S0.1 (D-26).
- Lint: N/A — `pnpm lint` from S0.1 (D-26).
- Tests: N/A for app `pnpm test` / `pnpm test:e2e`; applicable setup checks below remain required.
- Production build: N/A — `pnpm build` from S0.1 (D-26).
- Current CI evidence for reviewed head: N/A — Code Check introduced in S0.1.
- Current tested Preview evidence for reviewed head: N/A — D-26.
- Placeholder/ignore/attribute checks: §12 bash scan — no unregistered token (the PowerShell `rg` form could not run: `rg` is not on PowerShell's PATH); `git check-ignore` / `check-attr` / env-tracking results as expected — exact outputs in the sprint record. Rerun using existing tools.
- Candidate-checkout Admin integrity: fresh clone of `c00846c` (with `core.autocrlf=true`) → `PASS: all 39 Admin manifest entries match`; 40 Admin files, none with a CR byte. Re-check the final head the same way. Reviewer does not create a checkout or alter the index; use the supplied candidate checkout or committed blobs and qualify any unverified checkout claim.
- Full E10 baseline: `PASS: E10 development reference matches all 821 source files.` on the preserved full-asset workspace; on the reduced fresh clone, exactly 632 `Missing` (631 D-07 assets plus the excluded workbook), 0 `Changed`, 0 `Added`. Method: `node prototype/scripts/development-baseline.cjs` on the preserved full-asset reference only. D-07 omits most assets from Git; a reduced checkout cannot run this whole check. Verify the committed text subset against the baseline and the Admin manifest; state any limitation. Never regenerate the baseline.

Validate and assign supplied literal hexadecimal SHAs to PowerShell variables `$reviewBaseSha` and `$reviewHeadSha`, then run:

```powershell
git status --short --branch
git rev-parse --verify "$reviewBaseSha^{commit}"
git rev-parse --verify "$reviewHeadSha^{commit}"
git merge-base --is-ancestor $reviewBaseSha $reviewHeadSha
git diff --name-status "$reviewBaseSha..$reviewHeadSha"
git diff --stat "$reviewBaseSha..$reviewHeadSha"
git diff --check "$reviewBaseSha..$reviewHeadSha"
git ls-tree -r --name-only $reviewHeadSha
```

Confirm actual PR head/base metadata using available read-only GitHub access and verify the supplied base is the actual merge-base. Identify/exclude live env paths before opening diff content; report such paths without reading them. Do not dump suspected secrets. Run the prompt's exact placeholder/Admin/ignore/attribute checks only on a worktree matching the reviewed head; otherwise inspect immutable blobs or record the limitation. Current local state is not committed-head evidence.

Run commands only with the existing environment. Do not install or change anything to make a check pass.
State every command not run and why.

## Hunt list

1. Correctness: first import is self-contained; sourced decisions/gates agree; setup checklist uses S0.0/D-26; Node/test contract is consistent with S0.1; no fabricated merge, deployment, owner answer or S0.1 activation.
2. Authorization: actual account/visibility and Git authorizations; effective main protection; branch shares remote main history. No direct-push/unrelated-history workaround. Runtime auth is N/A, but governing RBAC requirements remain intact.
3. Secrets/env: no live values or credential-bearing MCP configuration in import/evidence; nested env paths ignored; examples placeholder-only. Report suspected leaks by file/line/type only.
4. Data safety: frozen reference bytes intact; excluded local files preserved; no replaced baseline, destructive bootstrap or invented approval. Database checks N/A.
5. Input safety: no D-32 prototype mechanism promoted to a production requirement; NS-12–NS-15 remain draft without owner approval. No public write/sink is built here.
6. Build/deploy: no app/CI implementation or dependency change; nested prototype Pages workflow remains inert; ignore rules retain required text/manifests/fonts/brand; nearer admin/** -text effective; formatter/linter policy excludes prototype; commands runnable or honestly skipped.
7. Scope/content: exact permitted edits/import; approved capability/design scope distinguished from pending visual, commercial and owner-only decisions. No invented price, term, licence or production configuration.
8. Regressions: D-26 confined to S0.0; later CI/Preview/current-head review and owner merge gates preserved. Substantive post-review changes require re-review.

Do not open a live-value env file from the worktree. Never echo a suspected secret value. Identify only
its file, line, and type and recommend rotation.
Report serious, evidence-backed issues only; no style nits.

## Returned record

Begin with the actual confirmed immutable range; YES/NO scope match with explanation; files/context inspected; exact commands/evidence checked with results/skips. Populate these from execution, not PENDING fields.

For each finding, use a numbered finding heading and:

- **Severity:** Blocking / Should-fix.
- **Location:** actual path:line plus affected setup flow.
- **Issue:** one or two evidence-based sentences.
- **Failure scenario:** concrete input/state leading to wrong outcome.
- **Suggested fix:** specific and minimal.
- **Confidence:** high / medium / low.

If none, state **No findings** and list correctness, safety, reference-integrity and protection checks, plus justified D-26 N/A items. Never return bare approval.

End with exactly one **Verdict: APPROVE** or **Verdict: REQUEST CHANGES**, a one-line reason, full reviewed range, reviewer identity and actual date. State every Should-fix disposition. No verdict is issued during preparation.

The owner or builder appends this returned record here. Substantive changes after the reviewed head invalidate approval and require updated applicable checks and independent review at the new head. Later sprints also refresh Preview; S0.0 has none. A commit only appending the returned review record may be exempt when its documentation-only scope and reviewed head are recorded.

## Review history

No independent implementation review has run. Implementation committed 2026-09-23; awaiting the pushed head, the PR and the GPT-6 Astra Codex review.
