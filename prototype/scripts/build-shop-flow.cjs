const fs=require('fs');
const icon=require('./site-icons.cjs');
module.exports=({shell,hero,link})=>{
 const journey=fs.readFileSync('index.html','utf8').match(/<section class="section section--ink journey"[\s\S]*?<\/section>/)[0];
 const note='<p class="flow-preview">Design preview · No order is placed or payment collected. Use sample details.</p>';
 const top=(title,intro)=>hero(title,intro)+note;
 const help='<p class="flow-help">A question before you continue? <a href="pages/contact/index.html">Contact & Support →</a></p>';
 const totals=`<dl class="flow-totals"><div><dt>Artwork subtotal</dt><dd data-subtotal>₹0</dd></div><div><dt>Delivery charge</dt><dd>To be confirmed</dd></div></dl><p class="flow-fine">Delivery charges and tax treatment will be confirmed before live checkout opens.</p>`;
 const checker=`<form class="delivery-check" id="cart-delivery" data-delivery-check novalidate><label for="cart-pincode">Can we deliver to you?</label><div class="delivery-check__row"><input id="cart-pincode" name="pincode" inputmode="numeric" autocomplete="postal-code" maxlength="6" placeholder="6-digit PIN code" aria-describedby="cart-delivery-result"><button type="submit" class="button button--secondary">Check</button></div><p class="delivery-check__result" id="cart-delivery-result" data-delivery-result role="status" aria-live="polite"></p></form>`;
 shell('pages/cart/index.html','Your bag','The artwork you love. The frame that makes it yours.','<h1 class="checkout-heading">Your bag</h1>'+`<div class="flow-layout"><section><div class="flow-section-title"><h2>Your artworks</h2><a class="text-link" href="shop-all/index.html">Keep exploring</a></div><div id="cart-page-items"></div><button class="flow-sample" id="sample-bag" type="button">Add a sample artwork</button><p id="bag-status" class="flow-fine" role="status"></p></section><aside class="flow-panel flow-summary"><span class="eyebrow">One step closer</span><h2>Your selection</h2>${totals}${checker}<div class="intake-slot" data-intake-slot hidden></div><div id="checkout-link">${link('pages/checkout/index.html','Continue to checkout →')}</div><p class="flow-fine">Continue as a guest. No account needed.</p>${help}</aside></div>`);
 const field=(label,name,type='text',attrs='')=>`<label>${label}<input name="${name}" type="${type}" ${attrs} aria-describedby="error-${name}"><span id="error-${name}" class="field-error"></span></label>`;
 shell('pages/checkout/index.html','Checkout','Your artwork, made for you.',require('./checkout-r9.cjs')());
 shell('pages/order/demo/index.html','Order preview','A beautiful choice. Here is what comes next.',`<section class="confirmation-heading"><h1>A beautiful choice.</h1><p>Made with care. Soon, part of your home.</p></section><div class="receipt-machine"><div class="receipt-printer"><div class="receipt-machine-header"><div><strong>Aalishaan Studio</strong><p id="receipt-print-status" role="status">Your order receipt</p></div><span>✦</span></div><div class="receipt-slot" aria-hidden="true"></div></div><div class="receipt-output"><div id="confirmation" class="receipt-paper"><h2>No order preview yet.</h2><p>Complete the sample checkout to see your receipt.</p>${link('pages/cart/index.html','Return to your bag')}</div></div></div><div class="receipt-actions" id="receipt-actions" hidden><button type="button" class="button button--primary" id="download-receipt">Download receipt (PDF)</button><button type="button" class="button button--secondary" id="print-receipt">Print / Save PDF</button><a class="button button--secondary" href="pages/track/index.html?reference=AS-PREVIEW-001">Track your artwork →</a></div>${journey}<div class="receipt-help">${help}</div>`);
 shell('pages/track/index.html','Track your artwork','From our studio to your wall. Follow every step.',`<div class="flow-layout tracking-layout"><section><form id="track-form" class="flow-panel utility-form"><span class="flow-symbol">${icon('shipping')}</span><h1>Track your artwork</h1><div class="track-tabs" role="tablist" aria-label="Find order by"><button type="button" role="tab" aria-selected="true" data-track-mode="order">Order ID</button><button type="button" role="tab" aria-selected="false" data-track-mode="awb">AWB</button><button type="button" role="tab" aria-selected="false" data-track-mode="mobile">Mobile number</button></div><label><span id="track-label">Order reference</span><input name="reference" required placeholder="e.g. AS-PREVIEW-001" autocomplete="off" aria-describedby="track-hint track-error"></label><p id="track-hint" class="flow-fine">Find your order ID in your confirmation email. Your AWB is available once a courier is assigned.</p><p id="track-error" class="field-error" role="status"></p><button class="button button--primary" type="submit">Track artwork →</button><button type="button" class="flow-sample" id="track-sample">Explore a sample journey</button></form>${help}</section><section id="tracking-result" class="flow-panel tracking-result" aria-live="polite"><p>Enter your reference to see the current stage, what happens next and where to find help.</p><div class="tracking-placeholder">${icon('studio')}<span>Studio</span><i></i>${icon('shipping')}<span>Your home</span></div><p class="flow-fine">This preview shows a sample journey, not live courier tracking.</p></section></div>`);
 for(const code of [404,500])shell(`pages/${code}.html`,`${code} — ${code===404?'Page not found':'Something went wrong'}`,'Find your way back to the artwork.',`<section class="recovery"><div class="recovery-art"><img src="assets/site/Hero%20Artwork.png" alt="A framed blossom artwork" width="1199" height="1312"></div><div><span class="eyebrow">${code===404?'404 · Page not found':'500 · A temporary interruption'}</span><h1>${code===404?'A little off the beaten path.':'Let’s try that again.'}</h1><p>${code===404?'This page may have moved, but there is still plenty of beauty to discover.':'Something interrupted this page. Return home and try opening it again.'}</p><div class="utility-actions">${link('index.html','Return home')}${link('shop-all/index.html','Explore artworks',true)}</div><p class="flow-fine">${code===404?'Looking for something specific?':'Still having trouble?'} <a href="pages/contact/index.html">Contact & Support →</a></p></div></section>`);
};
