# The frozen, checksummed reference is never formatted (TECHNICAL-INTEGRITY.md).
prototype/

# Documentation keeps its existing formatting (S0.1 choice). The verbatim prompt and review records are
# named explicitly so they stay excluded even if docs/ is formatted later.
docs/
docs/sprint-prompts/
docs/code-reviews/
README.md
CLAUDE.md
AGENTS.md
.claude/

# Generated files and caches (also ignored by Git)
.next/
out/
build/
coverage/
next-env.d.ts
pnpm-lock.yaml

# Supabase CLI link cache (git-ignored by supabase/.gitignore; written by `supabase link`)
supabase/.temp/
